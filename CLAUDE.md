# STARBUCKS HELPER 작업 안내

## WORKLOG HARD RULE

1. **[WORKLOG_READ_FIRST]** 저장소의 코드/설정/페이지를 수정하기 전에 반드시 `WORKLOG.md` 최신 항목을 먼저 읽고 현재 버전, 최근 변경, 미해결 이슈, 다른 작업자의 진행 상태를 확인한다. 이 확인 전에는 수정 작업을 시작하지 않는다.
2. **[WORK_ORDER_FIRST]** 사용자의 현재 요청을 기준으로 목표 / 대상 파일 / 허용 변경 / 건드리지 않을 것 / 완료 조건 / 실제 테스트 기준을 정리한다.
3. **[PRESERVE_EXISTING]** 기존 기능, 데이터 구조, 디자인을 최대한 유지하고 요청 범위 밖의 변경을 하지 않는다. 다른 작업자의 미완료 변경이나 관련 없는 변경을 덮어쓰지 않는다.
4. **[FINAL_RECHECK]** 수정 후 최초 요청과 WORKLOG 제약을 다시 읽고 실제 결과를 회귀검수한다. 검수 전 완료 선언 금지.
5. **[WORKLOG_WRITE_LAST]** 검수 완료 후, 사용자에게 완료를 선언하기 전에 `WORKLOG.md` 최상단에 새 작업완료일지를 추가한다. 날짜/작성자, 요청과 목적, 이전→신규 버전, 수정 파일, 실제 변경 내용, 검증 결과, PC/모바일 영향, commit SHA, 배포 상태, 남은 문제를 기록한다.
6. **[WORKLOG_READBACK]** WORKLOG 저장 후 다시 읽어 새 기록이 최상단에 실제 반영됐는지 확인한다.

## 작업지시서 / WORKLOG 분리 HARD RULE

1. **[INSTRUCTION_ROLE]** `COUPON_WORK_INSTRUCTIONS.md`와 `image-extractor.html`이 생성하는 프롬프트는 실제 쿠폰 작업을 수행하기 위한 **작업지시서**다. 추출/검수/크롭/서버 반영 같은 실행 규칙을 관리한다.
2. **[WORKLOG_ROLE]** `WORKLOG.md`는 저장소 개발·수정의 **작업일지**다. 요청 이유, 이전→신규 버전, 수정 파일, 실제 변경, 검증, commit SHA, 배포상태, 남은 문제를 기록한다.
3. **[NO_MIX]** 생성된 쿠폰 작업지시서 전문, 원본 쿠폰 데이터 전체, 배치 실행용 상세 프롬프트를 WORKLOG에 복사하지 않는다. 반대로 개발 변경 이력과 commit 정보는 쿠폰 작업지시서 본문에 섞지 않는다.
4. 쿠폰 배치 실행 결과가 WORKLOG에 필요할 때는 산출물명, 건수, 성공/오류 여부 정도의 짧은 결과 요약만 남긴다.

이 규칙은 ChatGPT / Codex / Claude 등 어떤 작업자가 작업하더라도 동일하게 적용한다.
