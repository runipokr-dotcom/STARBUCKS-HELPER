# STARBUCKS HELPER · 쿠폰 작업지시서 v4.0

업데이트: 2026-09-04

## 0. 작업 목적
스타벅스 쿠폰 원본 이미지에서 쿠폰 날짜, Coupon No. 17자리, 쿠폰 등록코드 8자리를 정확히 추출·검수하고, 장기 보관용 경량 크롭 이미지와 CSV를 생성한다.

최우선 목표는 **속도보다 정확성**이다.

## 1. SOURCE OF TRUTH
- 최종 정답의 기준은 반드시 원본 쿠폰 이미지다.
- 기존 CSV / JSON / OCR / 이전 추출 데이터는 참고자료일 뿐 최종 정답으로 사용하지 않는다.
- OCR 결과가 형식상 정상이어도 검증되지 않은 값을 확정값으로 취급하지 않는다.

## 2. 기본 데이터 단위
**원본 이미지 1장 = 쿠폰 데이터 1건 = 크롭 이미지 1장**

같은 이미지에서 다음 정보를 하나의 Pair로 묶는다.
- 쿠폰 날짜
- Coupon No. 17자리
- Registration Code 8자리

서로 다른 이미지의 Coupon No.와 등록코드를 절대 조합하지 않는다.

## 3. 정렬 및 번호 부여
압축파일 내부 저장 순서가 아니라 **원본 이미지 파일명/촬영시간 오름차순**으로 정렬한다.

정렬 후 001, 002, 003 ... 순으로 번호를 부여하고 CSV·검수·크롭 이미지에 동일한 번호 체계를 사용한다.

## 4. DATE MAP
전체 이미지의 날짜 변경 지점을 확인하여 먼저 DATE MAP을 만든다.

예:
- 1~25 : 7월25일 / Coupon No. 앞 5자리 53021
- 26~33 : 7월26일 / Coupon No. 앞 5자리 53024

DATE MAP은 이미지 누락, 정렬 오류, 번호 뒤바뀜을 찾는 보조 검증 자료로 사용한다.

## 5. 1차 데이터 추출
전체 이미지에서 다음을 추출한다.
- No.
- Date
- Coupon No.
- Registration Code

형식:
- Coupon No. = 숫자 정확히 17자리
- Registration Code = 숫자 정확히 8자리

OCR/자동 판독은 1차 초벌 입력 도구로만 취급한다.

## 6. CHECK 1 — 독립 2차 판독
1차 결과를 그대로 재사용하지 않고 전체 이미지를 대상으로 별도의 독립 판독을 한 번 더 수행한다.

비교 항목:
- 날짜
- Coupon No. 17자리
- Registration Code 8자리

1차와 2차가 완전히 동일한 건은 정상 후보로 분류하고, 불일치·미인식·형식 이상 건은 전부 예외 목록으로 분리한다.

## 7. CHECK 2 — 예외 원본 직접 확인
다음은 반드시 원본 이미지를 다시 열어 직접 확인한다.
- 두 판독 결과 불일치
- 숫자 일부 판독 실패
- 17자리/8자리 형식 오류
- 날짜 불일치
- Coupon No. 패턴 이상
- 중복 발생
- 이미지 순서 의심
- 기타 확신할 수 없는 경우

예외는 자동 추측으로 보정하지 않는다. **원본 이미지에 실제 표시된 숫자를 기준으로 확정한다.**

## 8. 전체 데이터 무결성 검사
최종 CSV 전에 반드시 확인한다.
- 원본 이미지 수 = CSV 데이터 수
- Coupon No. 개수 = 원본 이미지 수
- Registration Code 개수 = 원본 이미지 수
- Coupon No. 전부 17자리
- Registration Code 전부 8자리
- Coupon No. 중복 0건
- Registration Code 중복 0건
- 빈 값 0건
- 작업번호 연속
- 날짜 구간과 DATE MAP 일치

하나라도 맞지 않으면 완료 처리하지 않는다.

## 9. 보관용 이미지 크롭
데이터 추출 및 검수 후 원본에서 **향후 사람이 다시 검증하는 데 필요한 영역만 남긴 경량 크롭본**을 만든다.

반드시 온전히 남길 영역:
- `Coupon No.` 라벨
- Coupon No. 17자리 전체
- Registration Code 8자리 전체
- 숫자 위·아래 안전 여백

필요하면 위치 판단을 위해 바코드 하단 등 주변 요소를 소량 포함할 수 있다.

### 크롭 금지
- 숫자 한 자리라도 잘림
- 숫자 경계에 붙는 과도한 타이트 크롭
- 모든 이미지에 동일 좌표를 무조건 적용하고 종료
- 위치가 다른 이미지를 강제로 동일 좌표 처리
- 결과를 확인하지 않고 완료 처리

## 10. 크롭 방식
기본 방식은:

**자동/반자동 안전 여백 크롭 → 결과 전수검수 → 예외 개별 재크롭**

고정 좌표가 대부분에 적용 가능하더라도 결과 확인 없는 일괄 크롭은 허용하지 않는다.

## 11. 크롭본 전수검수
생성된 크롭 이미지 전부를 다시 확인한다.
- Coupon No. 17자리 전체 표시
- Registration Code 8자리 전체 표시
- 글자 잘림 없음
- 좌우 끝 숫자 정상
- 위아래 안전 여백 충분
- 잘못된 영역 크롭 없음
- 판독성이 원본 대비 훼손되지 않음

문제가 있으면 원본으로 돌아가 개별 재크롭하고 다시 확인한다.

## 12. 크롭 이미지 파일명
CSV No.와 반드시 대응시킨다.

기본 권장:
- `001.jpg`
- `002.jpg`
- `003.jpg`

필요하면 `001_53021994875212612.jpg`처럼 Coupon No.를 추가할 수 있다. 파일명 자체를 정답 근거로 사용하지 않는다.

## 13. 이미지 품질
- 원본 픽셀을 유지한 단순 크롭 우선
- 과도한 JPEG 압축 금지
- 숫자가 뭉개지는 리사이즈 금지
- AI 보정/숫자 재생성 금지
- 숫자 형태를 바꿀 수 있는 과도한 샤프닝 금지

**용량보다 판독성을 우선한다.**

## 14. CSV 형식
컬럼:

`No,Date,CouponNo,RegistrationCode`

예:

`1,20260725,53021994875212612,41134073`

CSV는 UTF-8로 저장한다. Coupon No.와 Registration Code는 자리수가 변형되지 않도록 문자열 식별자로 취급한다.

## 15. CSV 파일명
형식:

`ai1_수량_작업날짜_쿠폰시작날짜-쿠폰종료날짜.csv`

예:

`ai1_198_260904_260725-260802.csv`

## 16. Google Drive 저장 위치
STARBUCKS HELPER 최종 결과물은 Google Drive의 **helper** 폴더에 저장한다.

대상:
- CSV
- 필요 시 검수 기록
- 경량 크롭 이미지

원본 ZIP은 별도 지시가 없으면 임의 삭제하거나 변경하지 않는다.

## 17. FAIL-SAFE RULE
자동 판독이 불안정하거나 이미지 구조가 예상과 다르면 억지로 자동화하지 않는다.

우선순위:

**원본 정확성 > 검수 가능성 > 작업 속도 > 파일 용량**

확신할 수 없는 데이터는 추측하지 않고 예외로 표시한 뒤 원본을 확인한다.

## 18. 완료 조건
아래를 모두 만족해야 완료로 보고한다.
1. 원본 전체 처리
2. 1차 전체 판독
3. 독립 2차 전체 판독
4. 불일치/누락 예외 원본 확인
5. 데이터 개수 원본과 일치
6. Coupon No. 형식 전건 정상
7. Registration Code 형식 전건 정상
8. 중복 검사 완료
9. 누락 0건
10. CSV 생성
11. 보관용 크롭본 전체 생성
12. 크롭본 전수 결과검수
13. 이상 크롭 개별 수정
14. Google Drive `helper` 폴더 저장

## 핵심 원칙
- **OCR은 입력 도구이지 정답지가 아니다.**
- **원본 이미지 1장 = 데이터 1건 = 크롭 이미지 1장.**
- **자동화 결과는 반드시 검수한다.**
- **애매한 숫자는 추측하지 않는다.**
- **크롭본은 용량 절감용이면서 향후 재검증 가능한 증빙 자료여야 한다.**
- **최종 정답은 항상 원본 이미지다.**
