# STARBUCKS HELPER · 쿠폰 작업지시서 기준 v4.1

업데이트: 2026-09-06

## 0. 문서 역할
이 문서는 **쿠폰 실작업을 수행하는 작업지시서 기준 문서**다.

- 여기에는 쿠폰 추출 / 검수 / 크롭 / 서버 반영 방식 같은 **실행 규칙**만 적는다.
- 개발 변경 이력, 누가 무엇을 수정했는지, commit SHA, 다음 작업자 인계 등은 `WORKLOG.md`에만 기록한다.
- `WORKLOG.md` 내용을 작업지시서 본문으로 복사하지 않는다.
- 반대로 실제 쿠폰 배치의 상세 지시문이나 원본 데이터 전체를 `WORKLOG.md`에 넣지 않는다.

## 1. 작업 목적
스타벅스 쿠폰 원본 이미지에서 다음을 정확히 추출·검수한다.

- Coupon No. 17자리
- Registration Code 8자리
- 사용 유효기간 시작일

그리고 향후 원본대조가 가능한 검수 크롭 ZIP과 필요한 경우 CSV를 만든다.

최우선 목표는 **정확성을 유지하면서 불필요한 반복 작업을 줄이는 것**이다.

## 2. SOURCE OF TRUTH
- 최종 정답의 기준은 반드시 원본 쿠폰 이미지다.
- 기존 CSV / JSON / OCR / 이전 추출 데이터는 참고자료일 뿐 최종 정답으로 사용하지 않는다.
- OCR 결과가 형식상 정상이어도 검증되지 않은 값을 확정값으로 취급하지 않는다.

## 3. 기본 데이터 단위
**원본 이미지 1장 = 쿠폰 데이터 1건 = 검수 크롭 이미지 1장**

같은 원본 이미지에서 다음을 한 Pair로 묶는다.
- Coupon No. 17자리
- Registration Code 8자리
- 사용 유효기간 시작일

서로 다른 이미지의 Coupon No.와 등록코드를 절대 조합하지 않는다.

## 4. 정렬 및 번호 부여
- 원본 이미지 파일명 / 촬영시간 등 실제 원본 순서를 기준으로 일관되게 정렬한다.
- 정렬 후 001, 002, 003 ... 순으로 작업번호를 부여한다.
- CSV, 검수 크롭, 서버 신규 데이터는 동일한 순서를 유지한다.

## 5. 효율화 HARD RULE
정확성을 낮추지 않는 범위에서 아래 절차를 우선한다.

### [ONE_DOWNLOAD_ONE_UNZIP]
- 원본 ZIP은 한 번만 다운로드하고 한 번만 해제한다.
- 같은 원본을 추출 / 검수 / 크롭 단계마다 반복 다운로드하지 않는다.

### [SINGLE_PASS_BUILD]
1차 순회에서 동시에 수행한다.
- Coupon No. 초벌 추출
- Registration Code 초벌 추출
- 사용 유효기간 시작일 초벌 추출
- 원본대조 검수 크롭 생성

같은 이미지를 작업 목적별로 처음부터 반복 처리하지 않는다.

### [BATCH_WITH_ORDER]
- 대량 작업은 25~50장 단위로 처리할 수 있다.
- 배치가 나뉘어도 전역 no와 원본 순서를 고정한다.
- 배치 경계에서 Pair가 뒤바뀌지 않게 검사한다.

### [AUTO_INTEGRITY_FIRST]
1차 추출 직후 다음은 스크립트로 한 번에 검사한다.
- Coupon No. 17자리 여부
- Registration Code 8자리 여부
- 빈 값
- 숫자 외 문자
- 중복
- 개수
- no 연속성

이 결과로 예외 목록을 먼저 만든다.

### [FULL_VISUAL_RECHECK_ONCE]
- 최종 확정 전 모든 건을 원본 또는 원본에서 만든 판독 가능한 검수 크롭과 1:1로 **한 번 전수 육안대조**한다.
- 2차 검수에서 전체 OCR을 처음부터 다시 돌리는 독립 재추출은 기본 절차로 사용하지 않는다.
- 샘플 검수 금지.

### [EXCEPTION_FOCUS]
다음 건만 원본 확대 / 추가 판독에 집중한다.
- OCR 불일치
- 미인식
- 저화질
- 자리수 이상
- 패턴 이상
- 중복 의심
- 날짜 불확실

정상 판독 가능한 건을 불필요하게 반복 확대하지 않는다.

### [ERROR_SCOPE_RECHECK]
- 이미 전건 육안대조가 끝난 뒤 개별 숫자 오류 1건을 수정했다면, 수정 건 원본 재확인 + 인접 순번 Pair 확인 + 전체 기계적 무결성 검사만 다시 한다.
- 단, 오류가 **순서 / Pair / 건수 / 배치 경계**에 영향을 줄 가능성이 있으면 전체 육안대조를 다시 수행한다.
- 개별 오타 하나 때문에 정상 200~500건을 무조건 처음부터 세 번째 전수판독하는 방식은 피한다.

## 6. 숫자 검수 HARD RULE
- Coupon No.는 정확히 17자리 숫자다.
- Registration Code는 정확히 8자리 숫자다.
- 특히 다음 혼동을 확대 확인한다.
  - 0 / 6 / 8 / 9
  - 1 / 7
  - 2 / 7
  - 3 / 8
  - 5 / 6 / 9
- 한 자리라도 확실하지 않으면 추측하지 않는다.

## 7. 전체 데이터 무결성 검사
최종 확정 전에 확인한다.
- 원본 이미지 수 = 최종 데이터 수
- Coupon No. 수 = 원본 수
- Registration Code 수 = 원본 수
- Coupon No. 전부 17자리
- Registration Code 전부 8자리
- Coupon No. 중복 0건
- Registration Code 중복 0건
- 빈 값 0건
- no 연속
- 원본 순서와 Pair 순서 일치

하나라도 맞지 않으면 완료 처리하지 않는다.

## 8. 사용 유효기간 / 파일명
- 모든 원본에서 **사용 유효기간 시작일**을 YYMMDD 6자리로 추출한다.
- 유효기간 시작일은 CSV / 서버 JSON 필드에 저장하지 않는다.
- 전체 시작일 최소값~최대값을 배치 파일명에만 사용한다.

배치명:

`총개수_작업일자_시작일최소~시작일최대`

예:

`259_260906_260807~260812`

한 건이라도 시작일이 불확실하면 파일명을 추측해서 확정하지 않는다.

## 9. CSV 형식
CSV 필드:

`no / coupon_number / registration_code`

유효기간 필드 금지.

Coupon No.와 Registration Code는 문자열 식별자로 취급하여 자리수가 변형되지 않게 한다.

## 10. 원본대조 크롭
데이터 추출 1차 순회에서 함께 만든다.

최종 크롭에는 다음을 충분히 남긴다.
- 쿠폰명
- 수신일시
- 사용 유효기간
- 바코드 전체
- Coupon No. 전체
- Registration Code가 같은 화면에 보이면 해당 영역

### 크롭 원칙
- 숫자 한 자리라도 잘리면 안 된다.
- 바코드 일부 잘림 금지.
- 판독 가능한 원본 픽셀 우선.
- 과도한 JPEG 압축 / 리사이즈 / AI 숫자 보정 금지.
- 동일 레이아웃끼리는 안전 여백 좌표를 재사용할 수 있다.
- 단, **최종 크롭 결과는 전건 확인**하고 예외만 개별 재크롭한다.

파일명:
- `001.jpg`
- `002.jpg`
- ...

최종 no와 1:1 대응한다.

## 11. Google Drive 보관 정책
### 서버 미반영
Drive에 저장:
- `배치명.csv`
- `배치명_verified.zip`

별도 JSON 백업 생성 금지.

### 서버 1 / 2 반영
Drive에 저장:
- `배치명_verified.zip`만

CSV / 별도 JSON 백업을 Drive에 저장하지 않는다.

### 공통
- 원본대조 크롭 ZIP은 서버 반영 여부와 관계없이 항상 보관한다.
- 불필요한 중간파일을 Drive에 반복 업로드하지 않는다.

## 12. 서버 선택
서버 파일명은 반드시 다음을 사용한다.
- 1서버: `coupons1.json`
- 2서버: `coupons2.json`

`coupon1.json`, `coupon2.json` 같은 단수형 파일명 사용 금지.

서버 반영 전 반드시 현재 파일을 먼저 읽는다.

## 13. 서버 반영 방식 — 반드시 선택
서버 반영을 선택하면 아래 두 방식 중 하나를 명시적으로 선택한다.

### A. 기존 뒤에 이어붙이기
- 현재 서버 JSON을 먼저 읽는다.
- 기존 레코드는 수정하지 않는다.
- 현재 마지막 no를 확인한다.
- 신규 배치는 마지막 no 다음 번호부터 연속으로 추가한다.
- 기존 데이터와 신규 배치 사이 Coupon No. / 등록코드 중복을 교차검사한다.
- 중복이 있으면 반영 중단 후 보고한다.
- 반영 후 기존 prefix가 그대로 보존됐는지 READBACK 검증한다.

### B. 전체 통으로 교체
- 현재 서버 JSON을 먼저 읽어 대상 파일과 구조를 확인한다.
- 현재 내용을 이번 최종 검수 배치로 완전히 교체한다.
- 신규 no는 1부터 연속으로 사용한다.
- 반영 후 서버 건수와 이번 배치 건수가 같은지 READBACK 검증한다.

## 14. 서버 READBACK
서버 업데이트 후 반드시 다시 읽는다.

이어붙이기:
- 기존 구간 불변
- 신규 구간 수량 일치
- 신규 no 연속
- coupon_number 일치
- registration_code 일치

전체 교체:
- 서버 총건수 = 이번 배치 건수
- no 1부터 연속
- coupon_number 전건 일치
- registration_code 전건 일치

READBACK 전 완료 선언 금지.

## 15. 원본 / Drive 안전 HARD RULE
### [NO_SOURCE_DELETE]
- 원본 ZIP은 작업 중 임의 삭제 금지.
- 기존 Drive 파일 임의 삭제 / 이름변경 / 덮어쓰기 금지.
- 자동 정리 금지.
- 사용자가 삭제를 지시한 경우에도 **정확한 파일명 + file ID를 재확인한 그 파일만** 처리한다.
- 원본과 산출물의 이름이 비슷하다는 이유로 추측 삭제하지 않는다.

## 16. FAIL-SAFE
다음이 하나라도 남아 있으면 서버 반영 또는 완료 처리하지 않는다.
- 중복
- 누락
- 건수 불일치
- 원본 불일치
- Pair 불확실
- 숫자 불확실
- 유효기간 시작일 불확실

우선순위:

**원본 정확성 > 검수 가능성 > 작업 속도 > 파일 용량**

단, 정확성에 영향을 주지 않는 중복 OCR / 중복 다운로드 / 불필요한 전체 재작업은 제거한다.

## 17. 완료 조건
1. 원본 전체 수량 확인
2. 1차 동시 추출 + 검수 크롭 생성
3. 자동 무결성 검사
4. 예외 목록 원본 확인
5. 전건 1:1 육안대조 1회 완료
6. Coupon No. / 등록코드 형식 전건 정상
7. 중복 / 누락 검사 완료
8. 사용 유효기간 시작일 범위 확정
9. 원본대조 크롭 ZIP 생성 및 전건 결과검수
10. 서버 미반영이면 CSV 생성 및 Drive 저장
11. 서버 반영이면 선택한 이어붙이기 / 전체교체 방식으로 반영
12. 서버 반영 시 READBACK 완료
13. 원본 ZIP 및 관련 없는 Drive 파일 무변경 확인

## 18. 최종 보고
다음을 보고한다.
- 원본 수량
- 최종 검수 수량
- 정상 수량
- 오류 수량 / 오류 순번
- 수정 여부
- 미확인·불확실 수
- 유효기간 시작일 최소~최대
- 최종 파일명
- Drive 저장 상태
- 서버 반영 여부
- 서버 번호
- 이어붙이기 / 전체 교체 여부
- 서버 READBACK 결과

## 핵심 원칙
- **OCR은 입력 도구이지 정답지가 아니다.**
- **원본 이미지 1장 = 데이터 1건 = 검수 크롭 1장.**
- **전건 대조는 유지하되 중복 재처리는 줄인다.**
- **애매한 숫자는 추측하지 않는다.**
- **원본대조 크롭 ZIP은 항상 남긴다.**
- **서버 반영 시 이어붙이기 / 전체교체를 반드시 구분한다.**
- **원본과 관련 없는 Drive 파일은 건드리지 않는다.**
- **작업지시서와 WORKLOG는 서로 다른 문서다.**
