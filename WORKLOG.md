# WORKLOG

STARBUCKS HELPER 저장소 작업 기록. 코덱스(로컬)와 클로드(클라우드)가 같은 저장소를 나눠서 작업하기 때문에, **작업을 끝낼 때마다 이 파일 맨 위에 새 항목을 추가**해서 서로(그리고 다음 세션의 나 자신)가 어디까지 됐는지 알 수 있게 합니다.

## 항목 작성 규칙
- 최신 항목이 맨 위로 오게 추가 (역순 정렬)
- 형식: `## YYYY-MM-DD — 작성자(Claude/Codex/ChatGPT)`
- 내용: 무엇을 왜 바꿨는지, 건드린 파일, 다음에 이어서 할 일 / 미해결 이슈
- 다른 쪽이 작업을 시작하기 전에 이 파일을 먼저 읽고 "현재 상태"를 파악할 것
- 커밋 메시지는 간단히, 자세한 맥락과 배경은 여기에
- STARBUCKS HELPER 작업은 시작 전 `[작업지시서]`, 종료 전 `[종료 전 최종검수]`, 종료 후 `[작업완료일지]`를 남긴다. 검수 전에 완료 선언 금지.

---

## 2026-09-06 — ChatGPT (쿠폰 259건 서버 미반영 배치)

### [작업완료일지]

**작업지시서**
- 원본: Google Drive `259.zip` / 259장.
- 선택 작업: Coupon No.(17자리) + 등록코드(8자리) + 사용 유효기간 시작일 전수 추출 및 원본 재대조.
- 서버 정책: **미반영**. `coupons1.json` / `coupons2.json` 등 GitHub 서버 JSON 수정 금지.
- 최종 보관: Google Drive에 CSV + 원본대조 크롭 ZIP만 저장.

**실제 처리 / 검수**
- 원본 ZIP 내부 이미지 수 259장 확인.
- 259건 전체를 원본 이미지 기준으로 2-pass 재추출/대조하고, 1차·2차 결과가 다른 항목은 더 넓은 원본 영역으로 재검수.
- 최종 충돌/불확실 항목은 원본 확대 확인으로 확정.
- 최종 건수: 259 / 259.
- Coupon No. 259개 모두 17자리, 중복 0, 누락 0.
- 등록코드 259개 모두 8자리, 중복 0, 누락 0.
- 최종 no 1~259 순서 유지.
- 유효기간 시작일 범위: **2026-08-07 ~ 2026-08-12** → 파일명 `260807~260812`.
- CSV 필드는 `no / coupon_number / registration_code`만 저장. 유효기간 필드 없음.
- 원본대조 크롭 259장 생성, `001.jpg` ~ `259.jpg`로 no와 1:1 대응 확인.

**최종 산출물**
- CSV: `259_260906_260807~260812.csv`
- 원본대조 ZIP: `259_260906_260807~260812_verified.zip`
- Google Drive CSV ID: `19D6OwCOUEjrTbe5qa9k3P5L5ooFKwBOP`
- Google Drive verified ZIP ID: `1PslIP1tOBV7mH0J0tVIdj0cmMNySO-rc`
- GitHub 서버 JSON: **미반영 / 수정 없음**.

**종료 전 최종검수**
- CSV 행수 259 확인.
- Coupon No. unique 259 / 등록코드 unique 259 확인.
- ZIP 내부 파일 259개 확인, 첫 파일 `001.jpg`, 마지막 `259.jpg` 확인.
- 작업지시서의 서버 미반영 조건 준수.
- Google Drive에 CSV와 verified ZIP 업로드 성공 확인.

**주의 / 남은 정리**
- Drive 업로드 과정에서 verified ZIP이 한 번 중복 업로드되어 두 번째 파일은 `DUPLICATE_DELETE_ME_259_260906_260807~260812_verified.zip`으로 명확히 이름 변경함. 커넥터의 영구삭제 안전 차단으로 자동 삭제는 못 했으므로 필요 시 해당 파일만 삭제할 것. 정상 보관본 ID는 `1PslIP1tOBV7mH0J0tVIdj0cmMNySO-rc`.

---

## 2026-09-06 — ChatGPT (이미지 추출기 v2.5→v2.8 / WORKLOG 절차 강화)

### [작업완료일지]

**요청 / 목적**
- 쿠폰 작업 결과물을 나중에 다시 원본대조할 수 있도록 이미지 추출기 작업 흐름을 단순화하고 보관 규칙을 확정.
- Coupon No. / 등록코드 외에 사용 유효기간 시작일을 원본에서 전수 추출하되, 유효기간은 데이터 필드가 아니라 파일명 범위 계산에만 사용하도록 변경.
- 이후 ChatGPT / Codex / Claude가 이전 작업을 놓치지 않도록 `WORKLOG.md`를 작업 전 읽고 작업 후 반드시 기록하는 절차를 HARD RULE로 강화.

**현재 버전 / 최종 상태**
- `image-extractor.html`: **v2.8 / Updated 2026-09-06**.
- v2.5 커밋: `ba1ce68e04a1bc2dc2e88e58117adc9bdbce2ce5`
- v2.6 커밋: `0d8f0166f7e8145db2c7d0c57644c4c8d887b90f`
- v2.7 커밋: `4c00c9c73089222f66b3224ddc6e6673120ee27b`
- v2.8 커밋: `5230d72672eb02d861ca8dcbe26822f77a1e877e`
- WORKLOG 절차 강화: `AGENTS.md` 커밋 `1c6907d71a6229d805a295cf914b7c0b33701e09`, `CLAUDE.md` 커밋 `41f20ea1456ee4a7deba61821cd7b9be3f1470f3`.

**버전별 변경 내용**
1. **v2.5 — 배치 파일명 / 검수 산출물 도입**
   - 배치명 규칙 `총개수_작업일자_유효기간시작일최소~최대` 도입.
   - 원본대조용 크롭 ZIP과 CSV를 같은 배치명으로 관리하는 규칙 추가.
   - 서버 JSON 파일명 `coupons1.json`, `coupons2.json` 복수형 규칙 명시.

2. **v2.6 — 유효기간 시작일 자동 추출**
   - 사용자가 유효기간 범위를 직접 입력하는 UI를 제거.
   - ChatGPT가 모든 원본 쿠폰에서 사용 유효기간 시작일을 전수 확인하여 최소~최대값을 계산하도록 변경.
   - 날짜 하나라도 불확실하면 추측하지 않고 파일명 확정을 중단하도록 규칙 추가.

3. **v2.7 — 단일 선택 / 자동 산출물 구조**
   - `이미지 크롭` / `쿠폰 데이터 추출`을 동시에 선택하지 못하고 하나만 선택하도록 변경.
   - ZIP / CSV / Drive 저장 체크박스 제거. 쿠폰 데이터 추출 시 필요한 결과물을 자동 결정하도록 변경.
   - 서버 반영도 `미반영 / 1서버 / 2서버` 중 하나만 선택.
   - 데이터 추출 작업에서도 원본대조 크롭을 자동으로 생성하도록 고정.

4. **v2.8 — 최소 보관 정책 확정**
   - 서버 미반영: Google Drive에 **CSV + 원본대조 크롭 ZIP**만 보관.
   - 1/2서버 반영: Google Drive에 **원본대조 크롭 ZIP만** 보관하고 JSON은 GitHub 서버 파일에 직접 반영.
   - 별도 JSON 백업 파일은 Drive에 저장하지 않음.
   - 원본대조 크롭 ZIP은 서버 반영 여부와 관계없이 항상 보관.
   - 유효기간 시작일은 CSV/서버 JSON 필드에 넣지 않고 파일명 계산에만 사용.

**수정 파일**
- `image-extractor.html` — v2.5 → v2.8.
- `AGENTS.md` — WORKLOG HARD RULE 강화.
- `CLAUDE.md` — WORKLOG HARD RULE 강화.
- `WORKLOG.md` — 본 작업완료일지 추가.

**WORKLOG HARD RULE — 앞으로 필수**
1. `[WORKLOG_READ_FIRST]` 코드/설정/페이지 수정 전에 `WORKLOG.md` 최신 항목을 먼저 읽고 현재 버전 / 최근 변경 / 미해결 이슈를 확인한다.
2. `[WORK_ORDER_FIRST]` 현재 요청의 목표 / 대상 파일 / 허용 변경 / 건드리지 않을 것 / 완료조건 / 실제 테스트 기준을 확인한다.
3. `[PRESERVE_EXISTING]` 기존 기능·데이터 구조·디자인 및 다른 작업자의 미완료 변경을 보존한다.
4. `[FINAL_RECHECK]` 수정 후 최초 요청과 WORKLOG 제약을 다시 대조하고 회귀검수한다. 검수 전 완료 선언 금지.
5. `[WORKLOG_WRITE_LAST]` 완료 선언 전에 WORKLOG 최상단에 작업완료일지를 남긴다. 이전→신규 버전 / 수정 파일 / 변경 내용 / 검증 / 영향 / commit SHA / 배포 / 남은 문제를 포함한다.
6. `[WORKLOG_READBACK]` WORKLOG 저장 후 다시 읽어 실제 반영을 확인한다.

**검증 / 영향**
- `image-extractor.html` v2.8 GitHub 저장 후 READBACK으로 버전/헤더 반영 확인.
- 기존 HELPER 메인 구조 및 다른 업무도구 파일은 수정하지 않음.
- 모바일 중심 UI 구조 유지. 선택지를 줄여 잘못된 산출물 조합이 생성될 가능성을 낮춤.

**남은 문제 / 다음 작업자 참고**
- 새 쿠폰 작업을 실행할 때 생성된 v2.3 지시서가 위 v2.8 저장 정책과 일치하는지 실제 결과물 기준으로 계속 검증할 것.
- HELPER 코드 수정은 반드시 위 WORKLOG HARD RULE 절차를 따른다.

---

## 2026-09-04 — ChatGPT (야간 추가 작업 / 최종 인계)

**현재 버전 / 최종 상태**
- 고객용 페이지 `share.html`: **v1.2 / Updated 2026-09-04**.
- 고객 모바일 UI 압축 반영 커밋: `f3c05e7` (`Compact customer catalog on mobile`).
- 모바일 견적서 JPG 다운로드 수정 최종 반영 커밋: `cd041b2` (`Fix mobile quote image download`).
- 위 작업을 위해 사용한 one-shot GitHub Actions workflow는 작업 후 삭제 완료. 최종 정리 커밋: `d6940c3`, `3e54144`.
- 현재 고객 페이지와 에디터의 기존 기능/데이터 구조를 유지하는 것을 HARD RULE로 함.

**이번 추가 작업 핵심**
1. **고객 모바일 페이지 UI 압축 — `share.html` v1.1**
   - PC 화면은 변경하지 않고 `@media (max-width:720px)` 범위만 조정.
   - 대분류/소분류/정렬 필터를 가로 스크롤형으로 압축.
   - 필터 패딩·간격, 제안가 표시, 선택 도구 영역 높이를 축소.
   - 상품 2열은 유지하면서 카드 여백/가격 간격을 줄이고 상품명은 최대 2줄 표시.
   - 하단 선택 바를 얇게 하고 ↑ 버튼이 하단 바와 겹치지 않도록 위치 조정.
   - 목적: 모바일 첫 화면에서 필터가 대부분을 차지하던 문제를 줄이고 첫 상품 행이 더 빨리 보이게 함.
   - 기능/Firestore/상품 선택/정렬/저장 로직은 변경하지 않음.

2. **모바일 `저장하기 → 견적서 JPG 다운로드` 수정 — `share.html` v1.2**
   - 모바일에서 다운로드 페이지/저장 동작까지 갔지만 실제 JPG 다운로드가 막히는 증상 대응.
   - 기존 비동기 Blob 다운로드 흐름 대신 `canvas.toDataURL("image/jpeg", 0.92)` 기반 동기 Data URL 생성으로 변경.
   - `저장하기` 클릭 후 첫 `await` 이전에 `downloadBasketImage()`를 실행하여 브라우저의 사용자 클릭 제스처 안에서 다운로드가 시작되도록 순서 변경.
   - 이후 Firestore 주문 저장은 기존대로 계속 진행.
   - 상품 선택, 수량, 공급가액/부가세/합계, 고객정보, 주문 저장 기능 유지.
   - 코드 기준 구조/반영 검수 완료. 단, 모바일 브라우저별 실제 다운로드 허용 동작은 실기기 재확인 필요.

3. **오늘 중간 카탈로그 보강 작업**
   - 스타벅스 food 상세 추출 지원: `e7002d1`.
   - 반복/중복 카탈로그 이미지 제거 및 과거 중복 이미지 스캔: `1e70441`, `5544997`, `7734b37`.
   - 앞선 카테고리/무신사 다중 이미지/PC 로컬 추출기 수정 내용은 아래 기존 ChatGPT 항목 참고.

**이번 추가 작업에서 직접 바뀐 핵심 파일**
- `share.html` — 모바일 UI 압축 + 모바일 견적서 다운로드 수정, 현재 v1.2.
- 작업용 임시 workflow — 사용 후 삭제 완료, 저장소에 남기지 않음.

**오늘 남은 작업 / 다음 작업자 TODO — 우선순위 순**

### TODO 1 — 카탈로그 에디터 성능 최적화 (최우선)
사용자 체감: 현재 HELPER의 `catalog-editor.html`이 이전보다 무거워진 느낌이 있음.

점검 대상:
- `catalog-sync.js`의 `autosave()` 후 전체 catalog deep clone / stringify / Firestore write 빈도.
- 현재 450ms debounce가 실제 편집 패턴에서 충분한지 확인하고 연속 변경을 더 효율적으로 묶을 수 있는지 검토.
- 데이터가 실제로 바뀌지 않았을 때 Firestore write 생략 가능 여부.
- 자기 기기가 쓴 Firestore snapshot을 다시 받아 전체 catalog 교체 + `render()` 하는 불필요한 왕복 여부.
- 상품 하나 수정에도 전체 가격 규칙 적용 / 전체 DOM render가 반복되는지.
- `remoteImages/images` 등으로 동기화 payload가 과도하게 커지는지.
- snapshot/event listener 중복 등록 여부.
- PC/모바일에서 필요 없는 초기 기능까지 동시에 로딩하는지.

성능 최적화 HARD RULE:
- UI 디자인 변경 금지.
- 기존 기능 삭제/축소 금지.
- 상품 데이터/카테고리/가격 계산/정렬/이미지/URL 상품등록/무신사·스타벅스 추출/모바일-PC 동기화/고객 공유/저장 기능 전부 유지.
- Firestore 데이터 구조 임의 변경 금지.
- 데이터 손실 금지.
- 추측으로 뜯어고치지 말고 **실제 병목 확인 → 최소 수정 → 회귀검사** 순서로 진행.

### TODO 2 — 기존 상품 카테고리 소급 보정 확인
- 기존 콜드컵/워터보틀/글라스 등 과거 등록 상품이 신규 자동분류 규칙과 다르게 남아있는지 확인.
- 현재 데이터가 이미 정상이라면 불필요한 전체 rewrite를 하지 말 것.

### TODO 3 — JS 캐시버스터 운영 정리
- `catalog-editor.html`에서 `catalog-online-import.js`, `catalog-sync.js` 수정 시 `?v=...`를 함께 올려 모바일/PC에 구버전 JS가 남는 문제 방지.
- service worker 캐시와 중복/충돌 여부도 같이 확인.

### TODO 4 — 실사용 회귀 테스트
- 모바일 고객 페이지: 필터/정렬/선택/수량/바구니/↑ 버튼/저장하기/JPG 실제 다운로드.
- Firestore 주문 저장과 JPG 다운로드가 둘 다 성공하는지 실기기 확인.
- PC 에디터: 로컬 helper 기반 무신사 추출, 스타벅스 추출, 다중 이미지, 이미지 재검수, 자동분류, 가격정책, 게시하기.
- 모바일 에디터: Vercel 온라인 추출 및 PC와 동기화.

**다음 작업자 필수 절차**
1. 이 `WORKLOG.md` 최신 항목을 먼저 읽는다.
2. 작업 시작 전에 `[작업지시서]`를 작성한다: 목표 / 대상 파일 / 허용 변경 / 건드리지 않을 것 / 완료조건 / 실제 테스트 기준.
3. 작업 후 원래 작업지시서를 다시 읽고 `[종료 전 최종검수]`를 수행한다.
4. 오류/미완료가 있으면 수정 후 다시 검수한다.
5. 마지막에 `[작업완료일지]`를 이 WORKLOG 맨 위에 추가한다: 변경 파일 / 실제 변경 / 테스트와 결과 / PC 영향 / 모바일 영향 / commit SHA / 배포상태 / 남은 문제.
6. 검수 전에는 완료 선언하지 않는다.

---

## 2026-09-04 — ChatGPT

**이번 작업 핵심**
- 카탈로그 정렬을 `카테고리 우선순위 → 선택한 정렬 기준`의 2단계 구조로 변경함. 우선순위는 `텀블러/보온병 → 머그 → 액세서리 → 티바나(차) → 시럽 → 우산 → 비아 → 원두 → 리유저블 → 기타` 순서 (`553847f`).
- 카테고리 별칭 정규화 추가: `악세사리/악세서리 → 액세서리`, `티바나 → 티바나(차)`, `텀블러 → 텀블러/보온병`.
- 무신사 다중 이미지 누락 원인 확인: 일부 상품은 대표 이미지 외 추가 이미지가 `prd_img` 경로에 있어 기존 `thumbnailImageUrl + goodsImages` 방식만으로는 1장만 추출됨.
- Vercel 온라인 추출기 `api/extract.js`를 v1.1로 수정하여 동일 상품번호의 `goods_img/prd_img` 이미지를 보조 수집하고 최대 5장까지 반환하도록 변경 (`35b53f9` 포함, 최종 반영 범위 `0874303`).
- `catalog-sync.js`에 **이미지 재검수** 기능 추가: 기존 상품의 `sourceUrl`을 다시 조회하고 상품번호를 우선 매칭한 뒤, 가격·카테고리·태그·수량 등 기존 데이터는 유지하면서 부족한 원본 이미지만 최대 5장까지 보강. 처리 결과를 `검사 / 보강 / 정상 / 매칭불일치 / 실패 / 원본링크없음`으로 표시 (`0874303`).
- 신규 등록 상품 자동분류 규칙 강화: `콜드컵/콜드 컵/워터보틀/워터 보틀/텀블러/보온병 → 텀블러/보온병`, `글라스/머그 → 머그` (`8091c6a`).
- PC에서 무신사 가져오기는 Vercel API가 아니라 로컬 `local-md-helper.py`를 사용하므로, PC용 추출기도 v1.2로 맞춰 동일 상품번호의 `prd_img`를 최대 5장까지 추출하도록 수정 (`cb6c957`).
- 실제 테스트 대상 무신사 상품 `5492977`은 페이지상 대표 포함 총 4장의 갤러리 이미지가 확인됐고, 로컬 추출기 재시작 후 이미지 재검수로 기존 1장짜리 상품을 보강하는 흐름을 사용함.

**바뀐 파일**
- `catalog-sync.js`
- `api/extract.js`
- `catalog-online-import.js`
- `local-md-helper.py`

**주의 / 미해결**
- 기존 등록 상품 전체의 카테고리를 상품명 기준으로 소급 재분류하는 로직은 `catalog-sync.js`의 `normalizeCatalogCategories()`에 아직 최종 반영되지 않음. 현재 `8091c6a`는 신규 온라인 등록 쪽 자동분류 강화가 중심이므로, 기존 콜드컵/워터보틀/글라스 데이터가 잘못 분류돼 있으면 별도 소급 보정이 필요함.
- `catalog-editor.html`의 외부 JS 쿼리 버전(`?v=...`)을 수정 때마다 올리는 캐시버스터 운영은 아직 미적용. 수정 후 브라우저에서 이전 JS가 남는 현상이 있었으므로 다음 작업에서 같이 반영 권장.
- `local-md-helper.py` 수정 후에는 이미 떠 있는 로컬 추출기 프로세스를 재시작해야 새 코드가 적용됨.
- 이미지 재검수 버튼이 안 보이면 브라우저 캐시 영향 가능성이 있으므로 강력 새로고침 필요.

**다음 작업자 참고**
- 작업 시작 시 `WORKLOG.md`를 먼저 읽고 위 미해결 2개(기존 상품 소급 카테고리 재분류, JS 캐시버스터)를 우선 확인할 것.
- 이미지 관련 수정 시 온라인 API와 PC 로컬 추출기 두 경로를 항상 같이 점검할 것.

## 2026-09-04 — Codex

**후속 수정**
- 에디터 첫 화면에서도 최상단 버튼이 바로 보이도록 스크롤 표시 조건을 제거함. 고객 페이지도 동일하게 항상 표시 (`a16a7e8`).

**최종 VER**: `VER-2026.09.04-a16a7e8`
**기능 완료 커밋**: `a16a7e8` — 최상단 이동 버튼 항상 표시 최종 조정

**오늘 반영한 내역**
- 카탈로그 편집 기능 확장: 대·소분류 관리, 정렬, 가격 정책, 웹판매가, 10원 단위 반올림 (`223d568`~`31ccd32`)
- 고객용 상품 안내 및 Firestore 게시·주문 저장, 견적서 이미지 저장, 고객 화면 정렬·선택·드래그 기능 (`368e058`~`1b23273`)
- 모바일·PC 간 카탈로그 동기화와 기존 데이터 보호 (`8526c1a`~`d442e0e`)
- Vercel 온라인 상품 추출 API 및 모바일 무신사·OneLink·스타벅스 링크 가져오기 (`c8dfba5`~`3d70c7b`)
- 쿠폰 문구 제거, 상품명 줄바꿈, 카테고리 자동 분류와 가격 정책 보정 (`3d70c7b`, `304a8bf`)
- 고객 링크 미리보기 문구를 `디스이스마인 제공`으로 변경 (`db46136`)
- 고객 상품 이미지 회전 보기와 중복 상품 경고 추가 (`c3d3cf4`)
- 에디터·고객 페이지에 작은 고정 ↑ 버튼 추가. 300px 아래에서 표시되며 누르면 부드럽게 최상단으로 이동 (`e60441c`)
- 에디터에서 상품 가져오기 성공 시 주소 입력칸 자동 초기화. 모바일 온라인 가져오기의 기존 초기화 동작도 유지 (`e60441c`)
- Claude와 Codex가 공통으로 읽고 갱신할 `WORKLOG.md`, `CLAUDE.md`, `AGENTS.md` 추가 (`e60441c`)

**검증 및 배포 상태**
- `catalog-editor.html`, `share.html` 인라인 스크립트 문법 검사 통과
- 변경 공백 검사 통과
- GitHub `main` 반영 예정

**다음 작업자 참고**
- 작업 시작 전에 이 파일의 최신 항목과 현재 Git 상태를 확인할 것.
- 새 기능의 최종판이 나오면 `VER-날짜-기능완료커밋` 형식으로 커밋 번호를 함께 기록할 것.
- `catalog-preview.html` 및 `.bak`, `__pycache__/`는 기존 미완료/비관리 파일이므로 이번 커밋에서 제외함.

## 2026-09-04 — Claude

**바뀐 파일**: `catalog-editor.html`, `share.html`, `firestore.rules`(이전 세션에서 반영 완료)

**이번에 한 작업**
- `cleanProductName()` 정규식이 "에코텀블러음료쿠폰"만 매칭해서 "+텀블러음료쿠폰(카톡발송)"처럼 "에코" 없는 변형은 안 지워지던 버그 수정 (에코를 옵션으로 변경). 단, 이미 등록된 기존 상품명은 소급 반영 안 됨 — 이름 필드 재저장 필요.
- `share.html`(고객용 페이지)에 "제안가 표기" 체크박스를 필터 줄 오른쪽 끝에 추가 (기존에 바구니 안에 있던 "제안가 포함하기"를 이걸로 통합·이동).
- "저장하기" 클릭 시 실제 파이어스토어 주문 기록 저장은 유지하면서, 추가로 **견적서 이미지(JPG) 자동 다운로드** 기능 구현. 캔버스에 텍스트만 그려서 만들기 때문에(상품 사진 없음) 무신사 이미지 CORS 문제와 완전히 무관함. 포함 내용: 선택 상품명(2줄 자동 줄바꿈)·수량·라인별 금액, 공급가액/부가세/합계, 상호명·이메일·연락처.
- `share.html`의 상품 카드 레이아웃을 에디터(`catalog-editor.html`)와 동일하게 맞춤: 카드 상단에 드래그 손잡이(☷) + 선택 체크박스 + 번호. 대분류 필터 아래에 소분류 필터, 정렬(가격높은순/최근추가순/최근수정순), 전체선택/전체선택해제 버튼 추가. 드래그앤드랍 순서변경 추가 — 고객이 바꾼 순서는 `customOrder`(id 배열)로 로컬에 기억해서, 파이어스토어 실시간 동기화로 새 데이터가 들어와도 순서가 안 깨지게 처리함.
- `catalog-editor.html`: "고객 링크 복사" 버튼 → "게시하기"로 이름 변경. `publishShare()` 페이로드에 상품별 `updatedAt` 추가 (share.html의 "최근수정순" 정렬에 필요).

**다음에 참고할 것**
- 이 클라우드 세션은 `gstatic.com`/`firestore.googleapis.com` 접속이 막혀 있어서 실제 파이어스토어 저장/실시간동기화/이미지다운로드 흐름은 직접 테스트 못 함. 코드 리뷰 + Playwright로 캔버스 렌더링·정렬·드래그 로직만 검증했음. **실사용 테스트는 항상 사람이 직접 확인 필요.**
- `catalog-editor.html.bak`, `local-md-helper.py.bak`, `__pycache__/`가 저장소 폴더에 남아있음 — 클라우드 세션 쪽 도구로는 삭제 권한이 없어서 그대로 둠. 필요하면 직접 삭제.
- 미해결/보류 논의 없음 — 이번 라운드 요청은 다 반영 완료.