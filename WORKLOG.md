# WORKLOG

## 2026-09-25 — ChatGPT (PIXEL BUNKER v0.3 렌더 시퀀스 전환)

### 작업 전 기록
- 사용자 확정: 긴 설명 대신 실제 구현 진행. v0.2의 실시간 3D 중심 구조를 **렌더 배경 + 굴착기/크레인 프레임 시퀀스 재생** 중심으로 전환.
- 목표:
  - 웹 퍼블리싱 시안처럼 1프레임 화면 유지.
  - phase별 배경 렌더 이미지 교체.
  - 장비 애니메이션은 WebP/PNG 프레임 시퀀스를 canvas에서 재생.
  - 굴착기 굴착 / 타워크레인 인양을 별도 animation key로 관리.
  - 실제 렌더 자산이 아직 없을 때도 UI와 phase 진행은 동작하도록 fallback 제공.
  - 자산을 `assets/bg/`, `assets/anim/` 아래에 넣기만 하면 코드 수정 없이 재생.
- 파일 구조:
  - `bunker-game/app.js`
  - `bunker-game/data/phases.json`
  - `bunker-game/data/animations.json`
  - `bunker-game/assets/README.md`
- 기존 공개 URL `/bunker-game/`, HELPER PIN 예외, QR 카탈로그 및 업무 데이터에는 영향 금지.

## 2026-09-25 — ChatGPT (PIXEL BUNKER v0.2 버그픽스)

### 작업 전 기록
- 사용자 요청: PIXEL BUNKER v0.2 버그픽스.
- 실제 코드 재검토에서 우선 수정 대상 확인:
  1. Three.js CDN에서 `OrbitControls.js`를 직접 URL import하면 해당 모듈 내부의 bare specifier `three`를 브라우저가 해석하지 못할 수 있어 GitHub Pages에서 3D 씬이 로드 실패할 가능성이 있음.
  2. 마지막 `완공 / 최종 검사` 스테이지에 도달하면 현재 조건 `state.stage >= STAGES.length - 1` 때문에 작업 버튼이 바로 비활성화되어 최종 검사를 실제로 수행할 수 없음.
  3. 완공 상태를 stage index만으로 표현해 마지막 단계 도달과 실제 완료가 구분되지 않음.
- 수정 목표:
  - import map 기반 Three.js / addons 로딩으로 GitHub Pages 정적 배포 호환성 확보.
  - `completed` 상태를 별도 관리해 마지막 최종 검사까지 실제 실행 후 완료 처리.
  - 기존 저장 데이터(v2)에 `completed`가 없어도 기본값으로 안전하게 병합.
  - 완공 후 UI/scene/진행률이 100%로 일관되게 표시.
- 공개 게임 URL, HELPER PIN, QR 카탈로그, 상품/쿠폰/Firestore 데이터는 변경하지 않는다.


### 작업 후 기록
- Three.js 모듈 로딩 수정:
  - `bunker-game/index.html`에 import map 추가.
  - `three` / `three/addons/` 경로를 jsDelivr CDN에 매핑.
  - `game.js`는 bare import `three`, `three/addons/controls/OrbitControls.js`를 사용하도록 변경.
  - 목적: OrbitControls 내부의 bare specifier를 브라우저가 해석하지 못해 3D 씬이 로드 실패할 수 있는 문제 방지.
- 최종 공정 로직 수정:
  - 저장 state에 `completed:false` 기본값 추가.
  - 마지막 stage 도달만으로 작업 버튼을 막지 않고 **최종 검사 작업까지 실제 실행** 가능.
  - 최종 검사 완료 후에만 `completed=true`, 진행률 100%, 작업 버튼 `완공` 상태로 전환.
  - 전체 공정률 계산을 9개 stage 기준으로 수정해 마지막 stage 진입만으로 100%가 되던 문제 해결.
  - 기존 v2 저장 데이터에 completed 필드가 없어도 기본값 병합으로 계속 사용 가능.
- READBACK 정적검수:
  - import map / module script / Three.js bare import / OrbitControls addon 경로 확인.
  - completed 기본값 / 최종검사 실행조건 / 완료처리 / 100% 진행률 확인.
  - `bunker-game/index.html`에 auth-gate가 없는 공개 페이지 상태 유지 확인.
- 공개 GitHub Pages URL은 현재 웹 검증 도구에서 직접 접근할 수 없어 실브라우저 렌더 확인은 미검증. 소스/배포 구조 기준 검수 완료.
- commit:
  - import map `9a396f96cfa90f2379c589dc1b4de9555d95cabb`
  - completion flow `65bf3ccb7712bf83e4f947a5a09e87f3e225224c`


## 2026-09-25 — ChatGPT (PIXEL BUNKER v0.2 1-frame 3D rebuild)

### 작업 전 기록
- 사용자 확정 방향: 기존 세로형 2D 프로토타입보다 **웹 퍼블리싱 시안처럼 한 화면 1프레임 안에 들어오는 구성**이 우선이며, 중앙 공사 현장은 광고 이미지처럼 3D 렌더링 느낌이 필요함.
- 목표:
  - 데스크톱 기준 1440×900 안에서 스크롤 없이 한 화면 구성.
  - 중앙 Three.js 3D viewport + 좌/우 HUD + 하단 공정 컨트롤.
  - 굴착 → 기초 → 철골 → 쉘 → 설비 → 완공 단계에 따라 실제 3D 구조물이 변함.
  - 로우폴리 장비/지형/구조물에 조명·그림자·재질을 적용하여 정적 Canvas보다 광고형 렌더에 가까운 느낌.
  - 모바일에서도 100dvh 기준 한 화면에 최대한 압축 배치.
  - 게임 로직과 3D scene 코드를 분리 가능한 구조로 유지.
- 웹 퍼블리싱 원칙:
  - `bunker-game/` 독립 공개 폴더 유지.
  - HELPER auth-gate/PIN 미적용.
  - GitHub Pages / Vercel / Netlify 정적 배포 가능.
  - 외부 의존성은 Three.js CDN 1개만 사용하고 빌드 도구 없음.
- 기존 QR 카탈로그, HELPER PIN, 상품/쿠폰/Firestore 데이터에는 영향 금지.


### 작업 후 기록
- `bunker-game/index.html`: 1440×900 기준 한 화면 1프레임 대시보드 구조로 전면 재배치. 좌측 토질/장비, 중앙 3D viewport, 우측 timeline/status, 하단 operation console.
- `bunker-game/styles.css`: 데스크톱은 스크롤 없는 고정형 웹퍼블리싱 프레임, 1050/760/500px 브레이크포인트에서 100dvh 안에 재배치.
- `bunker-game/game.js`: 기존 2D Canvas 렌더를 Three.js로 교체.
  - PerspectiveCamera + OrbitControls.
  - Hemisphere/Directional light, soft shadow, fog, ACES tone mapping.
  - low-poly primitive 기반 굴착기/크레인/지형/기초/철골/벙커 쉘/설비.
  - 공정 단계에 따라 실제 3D 구조물과 장비 visibility가 변경.
  - 드래그 회전 / 휠 줌.
  - 자원/안전/품질/장비/날씨/시간/localStorage 세이브 유지.
- 외부 의존성은 Three.js CDN만 사용. 별도 빌드 과정 없음.
- 공개 게임 페이지이므로 auth-gate/PIN 미적용 유지.
- 기존 HELPER/QR 카탈로그/상품·쿠폰·Firestore 데이터 변경 없음.
- READBACK: module script, 1-frame CSS, Three.js import, OrbitControls, stage scene swap, 공개 예외 확인.
- v0.2 commit:
  - HTML `c8819efe2b00d8133b70006c6ff2595373955a49`
  - CSS `f3034da87a4b300e3439f986905dfc2b137a289f`
  - Three.js `3193a4fee508703e1036cc7598556e32507c8df4`
  - README `ecc7a073f621cbe3f2f703d655ee81f4d333d03d`
- 다음 품질 업그레이드: 실제 GLB 장비 모델/PBR 텍스처/환경광/포스트프로세싱으로 광고형 3D 렌더 퀄리티 강화.


## 2026-09-25 — ChatGPT (PIXEL BUNKER 웹게임 프로젝트 착수)

### 작업 전 기록
- 사용자 요청: 광고 이미지처럼 공사 단계가 실제로 진행되는 벙커/건설 게임을 만들어 보고 싶으며, 처음부터 **웹 퍼블리싱 가능한 구조**로 판을 깔 것.
- 방향:
  - HELPER 루트에 게임 코드를 섞지 않고 `bunker-game/` 독립 폴더로 분리.
  - `bunker-game/index.html + styles.css + game.js`만으로 동작하는 정적 웹게임으로 구성.
  - 외부 프레임워크/빌드 도구 없이 GitHub Pages, Vercel, Netlify 등 정적 호스팅에 바로 배포 가능하게 함.
  - 게임은 공개 콘텐츠로 취급하여 HELPER 내부 PIN 게이트를 넣지 않는다.
  - HELPER `index.html`에는 내부 진입용 링크만 추가하고 기존 기능은 변경하지 않는다.
- 1차 MVP 게임루프:
  - 부지 조사 → 부지 정리 → 굴착 → 지반 다짐 → 기초 타설 → 철골/크레인 → 벙커 쉘 → 설비 → 완공.
  - 자원(예산/연료/강재/콘크리트), 안전/품질, 날씨, 장비, 작업자, 진행률을 포함.
  - 모바일 우선 UI + 데스크톱 대응.
  - 브라우저 localStorage 저장/이어하기/초기화.
- 시각 방향: 사용자가 보여준 광고의 “건설 프로젝트 보드 + 중앙 공사현장 + 장비/토질/공정 체크리스트” 감성은 참고하되, 광고 이미지를 복제하지 않고 독자 UI/그래픽으로 구현.
- 공개 URL 목표: `https://runipokr-dotcom.github.io/STARBUCKS-HELPER/bunker-game/`.
- QR 카탈로그 `share.html?id=c6xxh9`와 기존 HELPER 잠금 정책은 변경하지 않는다.


### 작업 후 기록
- 신규 독립 폴더 `bunker-game/` 생성:
  - `index.html`: 퍼블리싱 가능한 게임 UI/메타/레이아웃.
  - `styles.css`: 건설 프로젝트 보드 스타일 + 모바일/데스크톱 반응형.
  - `game.js`: 공정/자원/날씨/안전/품질/장비/Canvas 현장 렌더링 및 localStorage 세이브.
  - `README.md`: GitHub Pages/Vercel/Netlify 배포 방법과 확장 방향.
- 1차 플레이 루프 구현:
  - 부지 조사 → 부지 정리 → 굴착 → 지반 다짐 → 기초 타설 → 철골 세우기 → 벙커 쉘 → 설비 → 완공.
  - 예산/연료/강재/콘크리트 자원, 안전/품질/장비 상태, 랜덤 날씨, 보급/정비, 단계별 비용과 진행률.
  - Canvas에서 굴착기/크레인/기초/철골/쉘이 공정 단계에 따라 시각적으로 변화.
  - 브라우저 localStorage로 진행 자동 저장 및 초기화 지원.
- HELPER `index.html` 업무 도구에 **벙커 프로젝트** 링크 추가. 게임은 공개 독립 페이지로 열림.
- `START_HERE.md`에 `bunker-game/`을 PIN 예외 공개 웹게임으로 명시. HELPER 내부 데이터/Firebase에 종속시키지 않는 규칙 추가.
- 공개 목표 URL: `https://runipokr-dotcom.github.io/STARBUCKS-HELPER/bunker-game/`.
- 기존 QR 카탈로그 주소/HELPER PIN/상품·쿠폰·Firestore 데이터 변경 없음.
- 주요 commit:
  - game shell `5291f9603811b9979509fa5b859de5fb2c3e3b8a`
  - styles `2673e58816011658ee6a994e4579d1df34bf6419`
  - game logic `a6daec2535264098178ce3fa4c7188d02b6fbda2`
  - publishing README `89f1e1f3604e06f695a1f74de374bfc579b574ae`
  - HELPER link `01cd3c4e67917824e2d8ada5efb4598738a58420`
  - START_HERE public exception `c2bf0bb11df7ebcfbbe9e7d27c10b3fb253a030d`
- 다음 확장 후보: 작업자/장비 카드, 랜덤 사고 이벤트, 업그레이드, 지하층 확장, 효과음/BGM, 스테이지 맵, 점수/랭킹, 서버 세이브.


## 2026-09-25 — ChatGPT (HELPER PIN 접근 규칙 정리)

### 작업 전/후 기록
- 사용자 요청: ChatGPT/Work가 HELPER를 작업할 때 PIN이 필요한 범위를 명확히 정리해 운영 기준에 반영.
- 확정 규칙:
  - **브라우저로 내부 HELPER 페이지를 직접 열어 사용/검수**할 때는 PIN 입력 필요.
  - **GitHub 저장소 소스 읽기·수정·커밋**은 브라우저 페이지 접근이 아니므로 PIN 입력 불필요.
  - **공개 QR 상품 카탈로그 `share.html?id=c6xxh9`**는 PIN 없이 공개 유지.
- 작업자에게 중요한 해석:
  - 브라우저 자동화/실배포 검수에서 PIN 화면이 보이는 것은 정상 보호 동작.
  - 코드 작업은 GitHub connector로 계속 진행 가능하며 PIN 때문에 막히지 않음.
- `START_HERE.md`의 내부 접근 잠금 항목에 위 규칙 추가.
- 기능/상품/쿠폰/Firestore 데이터 변경 없음.
- START_HERE commit: `ccfb71e6b85ecce17fb9fe27a59f1e13de9f8a6b`.

---

## 2026-09-25 — ChatGPT (휴대폰 잠금화면형 6자리 PIN UI)

### 작업 전/후 기록
- 사용자 요청: 기존 브라우저 `prompt()` 비밀번호 입력을 휴대폰 잠금 해제처럼 6자리 숫자 PIN UI로 변경.
- 비밀번호 값은 기존 `999999` 유지. 저장 해시 변경 없음.
- 신규 `pin-pad.js`:
  - 6개 점 표시.
  - 0~9 원형 숫자 키패드.
  - ⌫ 한 자리 삭제.
  - 6자리 입력 즉시 인증 확인.
  - 오답 시 짧은 흔들림 + 입력 초기화.
- `auth-gate.js`:
  - 기존 `window.prompt()` 루프 제거.
  - `pin-pad.js`를 로드한 뒤 PIN UI 콜백으로 기존 SHA-256 검증 로직 사용.
  - 성공 시 기존 sessionStorage 인증 유지.
  - 3회 실패 또는 '상품 카탈로그로 이동' 선택 시 공개 QR 카탈로그로 이동.
- READBACK:
  - auth-gate → PIN pad 연결 확인.
  - pin-pad UI 파일 존재 확인.
  - `share.html`에는 auth-gate 미삽입 상태 유지.
- QR 공개 URL `share.html?id=c6xxh9` 변경 없음.
- 상품/쿠폰/Firestore 데이터 변경 없음.
- commit: PIN UI `88469ce09d2893fc674f6839ce7411ecd1e3a6ae`, 연결 `4419ea8012e85890a05dd550a4be8ff3c7ffe926`.

---

## 2026-09-25 — ChatGPT (HELPER 내부 비밀번호 변경)

### 작업 전/후 기록
- 사용자 요청: 내부 HELPER 비밀번호를 `999999`로 변경.
- `auth-gate.js`의 저장 해시를 새 비밀번호의 SHA-256 값으로 교체.
- 비밀번호 평문은 저장소에 기록하지 않음.
- 공개 QR 카탈로그 `share.html?id=c6xxh9`는 계속 비밀번호 없이 공개 유지.
- 다른 기능/상품/쿠폰/Firestore 데이터 변경 없음.
- commit: `cedcdabf6ac57fb97c4ef2d79684884a51d7f2c8`.

---

## 2026-09-25 — ChatGPT (HELPER 내부 접근 비밀번호 적용 완료)

### 작업 후 기록
- 신규 `auth-gate.js`: 내부 HELPER용 정적 비밀번호 게이트.
- 저장소에는 비밀번호 평문을 넣지 않고 SHA-256 해시만 저장.
- 같은 탭 세션에서 한 번 통과하면 `sessionStorage`로 내부 페이지 이동 시 재입력하지 않음.
- 비밀번호 3회 실패 또는 취소 시 공개 고객 카탈로그 `share.html?id=c6xxh9`로 이동.
- 보호 적용 HTML 15개:
  - index, calculate, catalog-editor, catalog-preview, coupon1/2/3, image-arrange, image-extractor, image-maker, md-extractor, price, prompt, quote, territory-game.
- **share.html은 auth-gate 미삽입 / 공개 유지**를 READBACK으로 확인.
- QR 고정 URL 및 shareId 변경 없음.
- 상품/쿠폰/Firestore 데이터 변경 없음.
- 보안 한계: GitHub Pages 정적 게이트이므로 일반 접근 차단/오접근 방지용. 저장소가 public이라 소스 분석까지 막는 강한 인증은 아님. 강한 보안이 필요하면 서버 인증으로 별도 이전 필요.
- 주요 커밋: auth-gate `f274be16c9f5ce29b208b4f9bb0056cd728cbcaf`, index 적용 `da5f823bae4f8349bdc791c2214b5b43bd9f454b` 등 각 내부 HTML 순차 반영.
- START_HERE 정책 반영 commit: `0a6e45730e4487116ea9d538b374de95623ab1a8`.

---

## 2026-09-25 — ChatGPT (HELPER 내부 비밀번호 게이트 착수)

### 작업 전 기록
- 사용자 확정: **고객용 QR 카탈로그 `share.html?id=c6xxh9`는 비밀번호 없이 공개 유지**, 그 외 HELPER 내부 업무 페이지는 비밀번호 필요.
- 현재 GitHub Pages는 정적 호스팅이고 저장소가 public이므로 이번 단계는 서버 인증이 아닌 **정적 페이지 접근 게이트**다. 일반 접근 차단/오접근 방지 목적이며, 저장소 소스 분석까지 막는 강한 인증은 아님.
- 구현 방식:
  - 루트 `auth-gate.js` 신규.
  - 내부 HTML의 `<head>` 초반에 gate script 삽입해 나머지 페이지 스크립트보다 먼저 실행.
  - 비밀번호 평문은 저장소에 넣지 않고 SHA-256 해시만 저장.
  - 통과 상태는 `sessionStorage`에 저장하여 같은 탭 세션에서 내부 페이지 이동 시 재입력 최소화.
  - 실패/취소가 반복되면 공개 QR 카탈로그로 이동.
- 보호 제외 HARD RULE: `share.html`은 수정/잠금 금지, `shareId=c6xxh9` 유지.
- 대상: 저장소의 HTML 전반(share.html 제외), `auth-gate.js`, `START_HERE.md`, `WORKLOG.md`.
- 상품/쿠폰/Firestore 데이터 및 기존 기능 로직은 변경하지 않는다.

## 2026-09-25 — ChatGPT (카탈로그 2차 최적화 + 읽기 전용 무결성 검사)

### 작업 전/후 기록
- 사용자 요청: 카탈로그 후속 최적화와 버그픽스를 계속 진행.
- 원칙: 자동 삭제/자동 보정 없이 **읽기 전용 진단**을 우선 추가하고, 기존 데이터 구조·UI·QR 주소·게시/동기화 기능은 유지.
- `catalog-editor.html`:
  - `autosave()`의 localStorage 전체 `JSON.stringify`를 즉시 매번 실행하지 않고 180ms debounce로 묶음.
  - 탭/페이지 이탈(`pagehide`, hidden)과 게시하기 직전에는 `flushLocalSave()`로 강제 저장하여 데이터 손실 방지.
  - 신규 **카탈로그 점검 → 무결성 검사** 버튼 추가.
  - 검사항목: 상품명 없음, 음수/비정상 가격, updatedAt 누락, 등록되지 않은 카테고리, tags 타입 이상, 이미지 없음/형식 이상/5장 초과/중복, sourceUrl 중복, 상품명+판매가 중복.
  - 검사는 데이터에 손대지 않고 최대 80건까지 화면에 보여주며 초과 건수만 요약.
- `catalog-sync.js` 기능/Firestore 구조는 이번 단계에서 변경하지 않음. cachebuster만 `v=20260925-2`로 올림.
- QR 고정 공개 주소 `share.html?id=c6xxh9`와 `share.html`은 변경하지 않음.
- 운영 상품/쿠폰/Firestore 데이터 직접 수정 없음.
- 코드 커밋: `e00b8d7d8e083710d86f8b3d46b7f5a6fc92dce4`.
- 남은 점검: 실제 배포 후 무결성 검사 결과를 보고 **문제가 실제 데이터 오류인지 의도된 중복인지 구분한 뒤** 별도 수정. 자동 정리 금지.
- 다음 성능 후보: 상품 수가 더 늘어도 전체 `render()`가 필요한 경로(카테고리/태그/정렬/상품명)를 카드 단위 patch로 더 줄일 수 있는지 측정.

---

## 2026-09-25 — ChatGPT (QR 카탈로그 주소 고정 규칙 추가)

### 작업 전/후 기록
- 사용자 확인 요청: 앞서 QR로 만든 HELPER 상품 카탈로그 주소가 최근 최적화/버그수정으로 바뀌지 않았는지 확인하고, 앞으로도 실수로 변경되지 않게 할 것.
- 확인한 운영 URL: `https://runipokr-dotcom.github.io/STARBUCKS-HELPER/share.html?id=c6xxh9`.
- 최근 최적화 기준 commit `0c73cc2...` 이후 최종 `8371205...`까지 변경 파일을 compare한 결과 `share.html`은 변경 대상이 아니며, `shareId`/공개 카탈로그 URL 변경도 없음.
- `START_HERE.md`에 QR 고정 URL HARD RULE 추가: 사용자가 명시적으로 요청하지 않는 한 `shareId=c6xxh9`, `share.html` 경로, GitHub Pages 프로젝트 경로 변경 금지. 카탈로그 수정 후 기존 QR URL 동작 확인을 회귀검수 항목으로 포함.
- 기능/상품/쿠폰/Firestore 데이터 변경 없음.
- START_HERE 규칙 commit: `4ad1fd67d9e4f65d9fd7216f0e1eb452d6ef6136`.

---

## 2026-09-25 — ChatGPT (START_HERE 도입 + 카탈로그 체감 성능 최적화 착수)

### 작업 전 기록
- 사용자 목적: WORKLOG는 단순 역사 기록이 아니라 **반복 실수를 막기 위한 운영 기억장치**다. ChatGPT Work / GPT-6 Sol / Codex / Claude가 HELPER 작업 전 핵심 흐름·금지사항·현재 이슈를 즉시 읽을 수 있도록 루트에 별도 `START_HERE.md`를 둔다.
- `AGENTS.md`, `CLAUDE.md`에서 HELPER 작업 전 `START_HERE.md` → `WORKLOG.md` 최신 항목 순으로 읽도록 강제한다.
- 사용자 정정: **쿠폰3 서버는 별도 양식이며 Coupon No. 16자리가 정상**이다. 17자리 규칙은 쿠폰1/2 계열에 적용하며 쿠폰3의 16자리를 오류로 취급하거나 자동 보정하지 않는다.
- 카탈로그 문제: 상품 업데이트가 누적될수록 `catalog-editor.html`에서 수정/업데이트 시 로딩이 길어지는 체감 문제가 지속됨. 기존 기능·데이터 구조·디자인을 유지하면서 성능을 우선 개선한다.
- 실제 병목 확인:
  - `autosave()`마다 전체 catalog를 JSON.stringify해 localStorage 저장.
  - cloud sync는 autosave마다 450ms debounce 후 전체 catalog를 deep clone/정규화하여 Firestore에 전송.
  - 판매가/매입가 변경도 전체 `render()`를 호출해 모든 상품 카드 DOM을 재생성.
  - 이미지 이전/다음도 전체 `render()` 호출.
- 이번 최소 수정 목표:
  1. `START_HERE.md` 도입 및 작업자 필수 진입점 고정.
  2. `index.html` 서버 표시 토글의 실제 중복 listener 버그 수정.
  3. 카탈로그 연속 변경 cloud write를 더 크게 묶고 동일 payload 재전송 방지.
  4. 판매가/매입가 및 이미지 이전/다음처럼 전체 DOM 재생성이 필요 없는 변경은 카드 단위 갱신.
- 보존: 상품 데이터, 정렬/필터/카테고리/태그/가격 계산식, PC 로컬 추출, 모바일 링크 큐, 이미지 재검수, 고객 공유, Firestore 문서 구조, UI 디자인 전부 유지.
- 이번 단계에서는 Firestore Security Rules 구조 변경은 하지 않는다. 보안 개편은 인증/공개 share 영향이 커 별도 단계로 진행한다.

### 작업 후 기록
- 신규 `START_HERE.md`: HELPER 업무 흐름, 반복 금지 실수, 쿠폰1/2와 쿠폰3 자리수 규칙, 카탈로그/이미지/견적 HARD RULE, 살아있는 이슈를 현재 운영 기준으로 정리.
- `AGENTS.md`, `CLAUDE.md`: 작업 순서를 **START_HERE → WORKLOG 최신 → 실제 main 코드**로 고정. 기존 WORKLOG_READ_FIRST 문구도 이 순서와 충돌하지 않도록 정리.
- 사용자 정정 반영: 쿠폰3 서버 16자리 Coupon No.는 정상 별도 양식으로 확정. 앞선 감사 기록의 “52건 이상” 판정을 정정하고 재검수 TODO에서 제거.
- `index.html`: 서버 표시 토글의 `addEventListener`를 `apply()` 밖 초기화 구간으로 이동. `apply()` 반복 호출 시 listener가 누적되지 않도록 수정.
- `catalog-editor.html`:
  - 판매가/매입가 수정 후 전체 `render()` 대신 해당 카드의 판매가·매입가·제안가·웹판매가와 바구니만 갱신.
  - 이미지 이전/다음 클릭 후 전체 `render()` 대신 해당 카드 이미지와 장수 표시만 갱신.
  - 상품명 변경처럼 카테고리/필터 구조에 영향을 줄 수 있는 변경은 기존 전체 render 유지.
  - `catalog-sync.js` cachebuster를 `v=20260925-1`로 갱신.
- `catalog-sync.js` v1.4:
  - cloud payload signature를 저장해 동일 데이터의 Firestore 재전송을 생략.
  - 초기 로컬/클라우드 payload가 같으면 페이지 진입 시 불필요한 전체 rewrite 생략.
  - 다른 기기 snapshot이 현재 로컬과 동일하면 전체 data 교체/render 생략하고 stamp만 갱신.
  - 연속 수정 debounce를 450ms → 900ms로 늘려 빠른 연속 변경을 한 번의 cloud write로 묶음.
- 기존 상품 데이터/Firestore 문서 구조/카테고리/태그/정렬/가격 계산/PC 추출/모바일 큐/이미지 재검수/고객 공유 UI는 변경하지 않음.
- 데이터 변경: 상품/쿠폰 JSON 및 운영 Firestore 데이터 직접 수정 없음.
- 정적 READBACK 검수:
  - index listener 등록이 `apply()` 외부 1회인지 확인.
  - 판매가/매입가 및 이미지 이전/다음 경로에 전체 render가 제거됐는지 확인.
  - 상품명/카테고리/태그 등 구조 변경 경로는 기존 render 유지 확인.
  - sync signature skip / 900ms debounce / 초기 동일 payload skip 확인.
  - cachebuster 반영 확인.
- 공개 GitHub Pages는 현재 검증 도구에서 직접 접근할 수 없어 실배포 클릭/체감 성능은 미검증. 실제 브라우저에서 첫 사용 시 판매가 수정, 이미지 넘김, 빠른 연속 수정, 다른 기기 동기화를 확인할 것.
- 관련 commit:
  - START_HERE 신규: `184b91eca673617fc166a2c408a65fbd5c3cf7a2`
  - AGENTS/CLAUDE 진입 규칙: `fd59ad21b7321b770544aa778b5fd18da54862dc`, `33cd98aee3f460f3cc7acf7092504b6c84123d83`, 후속 정리 `7d6dd10d9eb81179162973baf609d407762bb97b`, `04b9462ceec8f8ec40d2b566aec01239cf9e5f78`
  - index 최종 listener 수정: `ccee3c32b1410bad289ecd182bb6176e22db24eb`
  - catalog-sync 1차/최종: `9fadd8dc0d380f2a529cb820a06665d3636b4718`, `7df5deeb18e3f75b4643f09732de7463027c32b8`
  - catalog-editor 부분 렌더: `a313360ced6482ab35c431694905d0a0f7c9ad22`
  - 쿠폰3/우선순위 기록 정정: `3777e5be4b23ade723173adc08755341235a0879`
- 남은 핵심: 실제 운영 체감이 여전히 느리면 다음 단계는 **localStorage 전체 stringify 빈도와 visible 전체 DOM 생성 자체를 계측**하고, 상품 카드 단위 patch 범위를 태그/카테고리까지 안전하게 확대하는 것. Firestore 보안 개편은 별도 작업으로 유지.

## 2026-09-25 — ChatGPT (HELPER 전반 감사 / 업무·Work 이미지 컨텍스트 통합)

### 작업 전 기록
- 사용자 요청: STARBUCKS HELPER 전반의 버그와 개선점을 찾고, 사용자의 실제 판매·사입·쿠폰·견적·상품 이미지 업무 흐름을 기준으로 리포트를 만든다. 또한 이전 ChatGPT Work에서 진행한 이미지/각인 시안 작업의 맥락까지 복원해 다음 작업자가 이어받을 수 있는 공용 WORKLOG를 남긴다.
- 이번 단계는 **감사(audit)와 인수인계 정리**가 목적이다. 기능 코드·상품 데이터·쿠폰 데이터·Firestore 데이터는 수정하지 않는다.
- 저장소 기준: `runipokr-dotcom/STARBUCKS-HELPER` main. 기존 원칙인 “HTML=프로그램 / JSON=데이터”, GitHub Pages 호환, 기존 기능·UI 최대 유지, 요청 범위 밖 리팩터링 금지를 유지한다.
- 사용자 실제 업무 흐름 기준: 스타벅스 중심 사입/리셀 → 판매가·마진 계산 → 상품 원본/이미지 수집 → 카탈로그 편집/공유 → 고객 견적/거래명세 → 쿠폰 추출·검수·서버 운영 → 상품 이미지/각인 시안 → 프롬프트·TO DO 관리.
- Work 이미지 컨텍스트 핵심:
  - 상품 사진 작업은 **원본 상품 픽셀/형태/로고/비율을 바꾸지 않는 것**이 우선이며 생성형 재해석과 레이아웃 오버레이 작업을 분리해야 한다.
  - 기본 상품 이미지 규격은 1000×1000, 순백 배경, 균일 여백, 중앙 배치, 원근/수평 보정 중심.
  - SS 블랙 매트 데비 텀블러 각인 시안은 원본 텀블러를 유지하고 로고 위 약 5mm, 가로 최대 25mm 기준으로 `Noah’s First / Birthday / 2026.10.22` 문구를 배치하는 작업이었으며 전체시안/확대시안과 AI8 호환 아웃라인 파일까지 이어졌다.
  - 과거 반복 문제는 제품 자체가 다른 이미지로 바뀌는 것, 25mm/5mm 실측 비율이 화면에서 임의로 보이는 것, 위치 수정 때 원본이 흐려지거나 재생성되는 것. 향후 HELPER 이미지 도구는 “원본 이미지 잠금 + 실측 기준 오버레이”가 기본 원칙이어야 한다.

### 감사 결과

#### P0 — 보안 / 데이터 노출
1. `firestore.rules`
   - `couponWork/{documentId}`: `allow read, write: if true`.
   - `catalogShares/{shareId}`: `allow read, write: if true`.
   - `catalogShares/{shareId}/orders/{orderId}`: `allow read, create: if true`.
   - 현재 HELPER가 Firebase Authentication 없이 동작하기 때문에 공개 웹 클라이언트가 경로를 알면 내부 작업 데이터·공유 카탈로그·주문 데이터에 접근하거나 일부 데이터를 덮어쓸 수 있는 구조다.
   - 특히 quote.html에서 사용하는 견적 템플릿/사업자등록증/통장사본/최신 견적 파일도 `catalogShares` 경로에 저장하는 구조이므로 공개 쓰기/읽기 정책과 함께 재설계가 필요하다.
   - **주의:** Firebase 웹 config의 apiKey 자체는 비밀키로 취급할 대상이 아니며 핵심 문제는 Security Rules/인증 구조다.

#### P1 — 현재 main에 실제 존재하는 UI 버그
2. `index.html` 서버 표시 설정
   - 현재 코드의 `apply()` 내부에서 `[data-status-toggle]` 버튼마다 익명 `click` listener를 다시 등록한 뒤, listener 내부에서 다시 `apply()`를 호출한다.
   - 따라서 클릭할 때마다 새 익명 listener가 누적될 수 있다.
   - 2026-09-21 WORKLOG에는 “중복 등록 버그가 현재 main에 존재하지 않는다”고 기록되어 있으나 **현재 실제 main 코드와 불일치**한다.
   - 다음 수정 시 이벤트 등록을 초기화 시 1회로 분리하고 `apply()`는 표시 상태만 갱신하도록 해야 한다. WORKLOG의 과거 판정도 “당시 확인과 현재 코드가 불일치”로 해석할 것.

#### P1 — 쿠폰 데이터 형식 확인
3. 현재 서버 JSON 형식 검사 및 사용자 정정
   - `coupons1.json`: 66건, no 1~66 연속, Coupon No. 17자리 전건, 등록코드 8자리 전건, 중복 0.
   - `coupons2.json`: 41건, no 1~41 연속, Coupon No. 17자리 전건, 등록코드 8자리 전건, 중복 0.
   - `coupons3.json`: 52건, no 1~52 연속, 등록코드 8자리/중복 0, Coupon No. 16자리 전건.
   - **사용자 확정: 쿠폰3 서버는 별도 양식이며 Coupon No. 16자리가 정상이다.** 17자리 규칙을 쿠폰3에 적용하거나 자동 보정하지 않는다.

#### P1 — 카탈로그 동기화 / 성능
4. `catalog-editor.html` + `catalog-sync.js`
   - 에디터의 `autosave()`는 편집마다 전체 `data`를 `JSON.stringify`하여 localStorage에 저장한다.
   - cloud wrapper는 autosave마다 450ms debounce 후 `cloudSafeData()` 전체 catalog를 Firestore `setDoc(..., merge:true)`로 전송한다.
   - 원격 snapshot 적용 시 전체 data 교체 → 카테고리 정규화/가격 규칙 적용 → localStorage 전체 저장 → 전체 `render()` 흐름이다.
   - 상품/이미지가 늘수록 “작은 수정 하나 = 전체 직렬화/전체 클라우드 payload/전체 렌더” 비용이 누적되는 구조다.
   - 기존 WORKLOG의 성능 TODO가 아직 구조적으로 유효하다. 기능 삭제나 데이터 모델 전면 변경 대신 fingerprint/dirty flag 기반 **실제 변경 없음 write 생략**, 렌더 범위 최소화, snapshot self-echo 방지 강화부터 검토한다.

#### P1 — 공개 주문/견적 흐름
5. `share.html`
   - 고객 저장 시 JPG 다운로드를 먼저 시도한 뒤 Firestore 주문 저장을 수행한다. 다운로드 성공 후 주문 저장이 실패할 수 있어 사용자가 “견적서 파일은 받았지만 주문은 서버에 없음” 상태가 생길 수 있다.
   - 반대로 저장 성공 메시지는 다운로드 성공 여부와 함께 표현하지만 두 작업은 하나의 원자적 트랜잭션이 아니다.
   - 주문 저장에는 회사명·이메일·전화번호가 들어가며 현재 rules상 읽기가 공개되어 있다. 보안 P0와 직접 연결된다.
   - 개선 시 모바일 브라우저의 사용자 제스처 제한을 유지하면서도 UI에서 “파일 저장”과 “주문 접수” 상태를 분리 표시할 필요가 있다.

#### P2 — 이미지/시안 도구 방향
6. 현재 `image-arrange.html`, `image-maker.html`은 상품 이미지 배열/상세 이미지 제작에는 유용하지만, Work에서 반복된 **각인 실측 시안** 문제를 직접 해결하는 도구는 아니다.
   - 향후 별도 “각인 시안” 모드/페이지에서는 원본 이미지를 배경 레이어로 잠그고 상품 픽셀 편집 금지.
   - 실제 제품 높이 또는 기준 길이 1개를 사용자가 지정하면 px↔mm scale을 계산해 “폭 25mm”, “로고 위 5mm”를 실제 비율로 표시.
   - 문구 레이어만 이동/크기/폰트 조정하고, 전체시안 + 확대시안을 동시에 내보내며 각인용 벡터/AI8 전달 파일은 이미지 시안과 별도 단계로 관리.
   - 생성형 이미지 기능과 동일 버튼/흐름으로 섞지 않는다.

#### P2 — 구조/운영 개선
7. 공통 Firebase 설정과 동기화 코드가 여러 HTML/JS에 반복되어 있어 버전 드리프트 가능성이 높다. 단, 현재 독립 HTML/GitHub Pages 원칙 때문에 무리한 모듈화는 금지하고 **작은 공통 모듈 또는 빌드 없는 include 전략**만 검토한다.
8. `sw.js`는 사실상 네트워크 `no-store` 패스스루이며 설치 시 이전 cache를 지운다. 현재 PWA service worker가 주는 이점이 작고, JS cachebuster 운영과 역할이 중복된다. 유지 목적을 명확히 하거나 제거 여부를 별도 검토한다.
9. `api/extract.js`는 허용 host를 스타벅스/무신사로 제한하고 HTTPS/응답크기/timeout을 두고 있어 SSRF 범위는 비교적 좁다. 추출 실패 시 외부 사이트 DOM 구조 변경에 취약하므로 extractor version/health 및 실패 원인 로그를 운영상 계속 노출하는 것이 필요하다.
10. WORKLOG가 11만 자 이상으로 커졌고 과거 판정이 현재 코드와 달라지는 사례가 생겼다. 최신 상태를 별도 `CURRENT_STATE.md` 또는 WORKLOG 최상단 “현재 확정 상태” 블록으로 요약하고, WORKLOG는 변경 이력으로 유지하는 구조를 권장한다.

### 6 Sol / 다음 작업자 권장 순서
1. **P0 Security 설계**: 공개 고객 페이지가 필요한 범위와 내부 운영 데이터를 분리. Firebase Auth 또는 서버 프록시/Vercel API를 사용해 내부 write/read를 보호하고 공개 share는 필요한 문서만 read, 주문은 create-only + read 비공개로 제한.
2. **index 이벤트 중복 버그 최소 수정 + 회귀 테스트**.
3. **카탈로그 성능 계측 후 최소 최적화**: write 횟수, payload 크기, render 횟수 측정 → no-op write 제거 → self snapshot/render 억제.
4. **share/quote 보안 분리**: 주문 개인정보 및 견적 원본 파일의 public read 제거.
5. **각인 시안 전용 도구 설계**: 원본 잠금, px/mm 기준점, 25mm/5mm 실측 오버레이, 전체/확대 동시 출력.
6. **START_HERE 운영 기준 유지**. 확정 규칙은 START_HERE, 상세 이력은 WORKLOG에 기록.

### 이번 감사에서 실제 변경
- 수정 파일: `WORKLOG.md`만.
- 기능 코드/JSON/Firebase 데이터/Firestore Rules 수정 없음.
- 기존 UI/배포 동작 변경 없음.
- main 현재 코드와 WORKLOG를 교차검사해 위 이슈를 기록.
- 데이터 검사는 현재 GitHub main의 `coupons1/2/3.json`만 대상으로 형식/연속성/중복을 확인했으며, 원본 이미지 대조는 수행하지 않음.
- 다음 단계에서 실제 수정 작업을 시작할 경우 이 항목을 먼저 읽고, 각 수정 전에 대상 범위와 회귀테스트를 별도로 기록한다.

---

## 2026-09-22 — Codex (견적서 선택 UI·품목 드래그·완성본 배치 수정)

### 작업 전 기록
- 사용자 요청: 문서 종류 드롭다운을 최상단 좌우 `견적서 / 거래명세서` 클릭 박스로 교체하고, 품목 번호 앞 체크박스를 작게 한 줄로 맞춘다. 품목은 직접 드래그해 순서를 바꿀 수 있어야 한다.
- 실제 완성 XLSX의 도장이 공급자 성명을 가리거나 카드 선택에 따라 멀어 보이지 않도록 도장 자체 좌표를 고정한다. 작성일은 `2026년 9월 26일` 형식으로 표시하고 마지막 합계행은 제거한다.
- 대상: `quote.html`, `WORKLOG.md`. 사용자 원본 XLSX 서식·회사정보·계좌정보, 품목 5개 필드, 카드 수수료, 증빙 선택, Firestore 한 건 덮어쓰기, 미리보기, 복사하기와 다른 HELPER 기능은 유지한다.

### 작업 후 기록
- `quote.html` v3.1.0: 상단 2분할 문서 선택 버튼, 수신인·이메일·작성일 한 줄 배치, 16px 품목 선택 체크박스와 번호·드래그 손잡이 한 줄 표시를 적용했다. HTML5 드래그로 변경한 배열 순서를 실제 XLSX 품목 행에도 그대로 사용하며 기존 위/아래 버튼은 모바일 대체 조작으로 유지했다.
- 실제 XLSX 내부 도장 drawing anchor를 공급자 회사명과 성명을 모두 가리지 않는 오른쪽 빈 공간의 고정 위치와 크기로 변경하고 미리보기는 그 실제 좌표를 추가 보정 없이 표시한다. 카드 선택 여부는 도장 좌표에 영향을 주지 않는다.
- XLSX A10/A13 작성일을 한국어 날짜 문자열로 기록한다. 원본 마지막 28행은 값 제거 후 숨김 처리하고 미리보기에서도 제외했다. 상단 합계금액, 23~25행 품목·카드 요약, 26~27행 계좌정보는 유지했다.
- 검수: 인라인 JavaScript 문법, `git diff --check`, 브라우저 초기화·2분할 문서 전환·3개 품목 한 줄 배치·계산 반영 확인. 로컬 Firestore 저장은 연결 지연으로 완료 미리보기까지 확인하지 못했으므로 배포 후 공개 Pages에서 실제 저장·도장·날짜·합계행을 재검수한다. 신규/삭제 파일 없음. 다른 HELPER 기능 변경 없음.

## 2026-09-22 — Codex (사용자 요청으로 이전 프롬프트 저장소 복원)

### 작업 전 기록
- 사용자: 무료 사용을 예상했으나 별도 API 비용 때문에 실행 기능을 사용하지 않기로 결정하고 이전 형식 복원을 요청.
- 프롬프트 저장소를 실행 기능 추가 전(845927e의 부모) 원본으로 복원. 이번에 추가했던 API, 실행 JS, 전용 테스트/배포 안내를 제거한다. 다른 프로젝트 기능과 운영 Firestore의 모든 프롬프트는 유지. 키 폐기/회전은 사용자가 원하지 않으므로 하지 않는다.
- Vercel 함수 삭제로 현재 운영 서비스에서 유료 실행 경로를 제거한다. Vercel에 저장된 환경변수는 다른 설정과 별개로 그대로 두며 호출 코드가 없어 사용되지 않는다.

### 작업 후 기록
- prompt.html을 실행 기능 추가 전 원본과 바이트 단위로 일치하게 복원. api/run-prompt.js, prompt-runner.js, 전용 테스트 2개, 배포 안내 제거. vercel.json은 상품 추출 함수 설정만 유지.
- 검수: 기존 버전과 일치, 인라인 JavaScript 문법 및 git diff --check 통과. 기존 저장·검색·전체보기·복사·삭제/Firebase 문서 구조 유지. 운영 데이터 변경 0건. 원격 배포 검증 진행.

---

## 2026-09-22 — Codex (API 키 입력 복구 및 실제 연결 확인)

### 작업 전 기록
- 사용자 API 키가 Vercel 환경변수의 Key 칸에 들어가 변수명 오류 발생. 키 값은 출력하지 않고 OPENAI_API_KEY의 Value로 이동. 기존 RUN_PROMPT_ACCESS_TOKEN은 보존. 사용자는 기존 키 사용을 명시적으로 요청함.
- Production에 두 Secret 변수의 저장을 재열어 확인하고 8b00ab9를 재배포(Ready, FiwdddAUjHCLdchrLCghKxpdLJZZ). 공개 Pages에서 기존 항목의 실행 미리보기만 짧은 테스트로 바꾸어 호출, 저장 원문 변경 없음.
- 실제 OpenAI 호출이 HTTP 429 반환. OpenAI Billing에서 Free trial / Credit remaining $0.00 확인. 크레딧 부족은 재시도로 해결되지 않으므로 안전한 오류 코드 분류를 추가한다. 키/오류 원문/계정 정보는 응답이나 로그에 포함하지 않는다.

### 작업 후 기록
- api/run-prompt.js에서 insufficient_quota/billing_hard_limit_reached와 일시 rate limit을 구분. 모의 API 테스트 6개 및 git diff --check 통과. 키 값의 저장소 기록 없음.
- 공개 Pages에서 저장 목록 및 실행 UI, Vercel Production Ready, 실제 API 인증 이후 429 응답을 확인. 생성 성공은 크레딧 부족으로 미확인. 결제는 사용자에게 인계하며 자동 결제하지 않는다.

---

## 2026-09-22 — Codex (ChatGPT 단독 연결)

### 작업 전 기록
- 사용자 변경: Claude는 사용하지 않고 ChatGPT만 연결한다.
- 변경 범위: prompt.html의 Claude 선택 및 다른 모델 실행 버튼 제거, prompt-runner.js 이벤트와 안내 문구 정리, 관련 테스트/설정 안내 업데이트. 기존 저장·수정·삭제·검색·즐겨찾기 및 실행 암호 보호 유지. 기존 서버 Anthropic 어댑터는 호출하지 않으며 등록을 요구하지 않는다.
- Vercel 실제 환경변수 등록 상태를 재확인하고 사용자 키 입력이 필요한 경우 그 단계만 인계한다.

### 작업 후 기록
- prompt.html에서 Claude 선택/다른 모델 실행 버튼을 제거하고 prompt-runner.js의 대응 이벤트를 정리. 기존 저장 기능과 ChatGPT 실행/복사/재생성 유지. 문법/공백 검사 및 독립 Chrome 회귀검수 통과. 운영 데이터 변경 없음.
- Vercel 환경변수 0개 확인. OPENAI_API_KEY와 RUN_PROMPT_ACCESS_TOKEN 입력 화면을 준비했으며 키 입력·저장은 사용자 대기. Claude 키 요구 없음.

---

## 2026-09-22 — Codex (실행형 프롬프트 저장소)

### 후속 배포 진행 (사용자: 그럼 언제 완료할거여)
- 기능 커밋 845927e를 main에 push 완료. 작업 도중 추가된 견적서 커밋 ef94157/be09e1b를 rebase로 보존. Vercel API 운영 주소에서 실행 암호 미설정 503 확인. Pages build 성공, deploy 진행 중. Vercel connector 팀 0개, 브라우저 로그인 필요로 환경변수 검증은 사용자 로그인 대기.
- 로컬 검수에서 중단한 것을 정정하고 main 반영 및 실제 Pages/Vercel 배포 검증까지 이어간다. Vercel 환경변수는 현재 로그인 확인이 필요하며 미등록 키를 추측하거나 공개하지 않는다.

### 작업 전 기록
- 요청: 사용하기 → 변수 입력 → ChatGPT/Claude → /api/run-prompt → 페이지 내 결과, 복사/다시 생성/다른 모델 실행. API 키는 Vercel 환경변수만 사용.
- 기준: 원격 main fetch 확인 d1cfa6b. 다른 작업 보존을 위해 현재 작업의 work/helper 독립 checkout 사용. 기존 저장/검색/삭제/전체보기/복사/Firebase 문서 유지. 현재 수정/즐겨찾기 기능은 없어 보완한다.
- 대상: prompt.html, prompt-runner.js 신규, api/run-prompt.js 신규, vercel.json, 실행 기능 테스트와 배포 안내. 쿠폰/카탈로그/견적서/기존 API 변경 금지. 데이터 마이그레이션/삭제 없음.
- 방식: {{변수명}} 입력, 결과는 텍스트로 안전하게 표시. 공개 Pages의 기존 Vercel 호스트 사용. 별도 실행 암호(RUN_PROMPT_ACCESS_TOKEN)로 유료 API 접근 보호; 공급자 API 키는 클라이언트로 전달하지 않는다.
- 미확인: Vercel 키/모델 이용 권한과 운영 배포 후 실호출. 이번 지시는 파일 수정/검수/설정 안내이며 원격 push/운영 배포는 수행하지 않는다.


### 작업 후 기록
- 수정: prompt.html, vercel.json, WORKLOG.md. 신규: prompt-runner.js, api/run-prompt.js, docs/PROMPT_RUNNER.md, tests/run-prompt.test.mjs, tests/prompt-browser.cjs. 삭제 파일 없음.
- 사용하기/중복 변수를 한 번만 입력/{{변수명}} 치환/실행 내용 확인/ChatGPT·Claude/페이지 내 텍스트 결과/복사/동일 입력 재생성/다른 모델 실행 구현. 기존 UI와 문서 version 1 유지. 기존 코드에 없던 수정·즐겨찾기 추가. ID·생성일·알 수 없는 기존 필드 유지 및 다른 기기의 동시 편집 충돌 차단.
- 공급자 API 키는 서버 환경변수에서만 읽음. 별도 32자 이상 실행 암호 필수, 허용 origin 검사, 입력 제한, 55초 서버 제한, 공급자 오류 비노출, 잘린 결과 표시, 안전한 텍스트 출력. 결과/실행 암호를 저장소에 기록하지 않음.
- 검수 통과: JavaScript 문법, git diff --check, Node API 테스트 6개(상위 테스트 포함). API 인증·CORS·메서드·입력·키 누락·양사 요청/응답·오류·타임아웃·불완전 결과 검사.
- 독립 Chrome 모의 Firestore/모의 API 검수 통과: 저장/수정/즐겨찾기/검색/삭제/새로고침, 알 수 없는 필드 보존, 다른 기기 변경 충돌 시 작성 내용 보존, 반복 변수와 특수문자 치환, HTML 미실행, 결과 복사, 동일 입력 재생성·모델 전환, 실패 시 이전 결과 보존, 암호 미저장, 390px 가로 넘침 없음. 모바일 실행 패널 캡처 육안 확인. 운영 Firestore 변경/마이그레이션 0건.
- 기존 상품 추출 API·쿠폰·카탈로그·견적서 파일 변경 없음. 다른 작업자 변경과 충돌 없음. 특이사항 없음(아래 미검증 범위 제외).
- commit SHA: 미커밋, 기준 d1cfa6b. 원격 push/Pages·Vercel 배포/환경변수 등록은 수행하지 않음. 실제 유료 API와 운영 Firebase 기기간 동기화·Pages 동작·Safari/카카오 실기기는 미검증. 배포 단계 및 필수/선택 환경변수는 docs/PROMPT_RUNNER.md에 기록.


---

## 2026-09-22 — Codex (견적서·거래명세서 입력 및 카드 수수료 구조 개편)

### 작업 전 기록
- 사용자 확정 요구: 계좌이체 금액을 기본으로 유지하고 카드 선택 시 품목 합계의 4%를 한 번만 계산. 품목 계산과 카드 수수료 계산을 분리하고 카드 안내·수수료·최종 카드 금액을 연한 빨간색으로 표시.
- 문서 종류를 견적서/거래명세서로 선택하고 제목과 파일명에 반영. 파일명은 날짜 없이 `스타벅스 텀블러 문서종류 - 수신인.xlsx`, UTF-8 기준 88바이트 이하.
- 품목은 상품명/규격/수량/단가/비고만 유지. 배송비·각인은 필요할 때 일반 품목으로 직접 입력. 상품 자동완성 제거, 체크 선택 삭제와 순서 이동 지원.
- 모든 입력은 페이지를 다시 열면 초기화하고 이전 로컬 입력을 복원하지 않음. 원본 XLSX, 증빙 선택, Firestore 한 건 덮어쓰기, 저장 후 미리보기, 자동 다운로드 없음은 유지.

### 작업 후 기록
- `quote.html` v3.0.0 구현. 문서 종류 선택, 초기 품목 3개, 체크 선택 삭제, 위·아래 순서 이동, 일반 텍스트 상품명·비고, 배송비 전용란 제거, 페이지 재진입 초기화를 반영.
- 카드 선택 시 화면에 품목 합계의 4% 수수료와 카드 최종 금액을 표시. XLSX는 14~22행 사용자 품목, 23행 품목 총액, 24행 카드 수수료, 25행 카드 최종 총액으로 분리하고 26~27행 원본 계좌정보를 보존.
- 수수료 공급가액·부가세는 반올림된 수수료 총액을 기준으로 한 번만 계산. 카드 안내, 수수료명·금액, 카드 최종 금액은 연한 빨간색으로 표시.
- 파일명은 날짜 없이 `스타벅스 텀블러 견적서 - 수신인.xlsx` 또는 `스타벅스 텀블러 거래명세서 - 수신인.xlsx`이며 확장자 포함 UTF-8 88바이트 이하로 자동 제한.
- 실제 저장 검수: 품목 105,000원 → 카드 수수료 4,200원(공급가액 3,818원·부가세 382원) → 카드 최종 109,200원 확인. 원본 계좌정보·도장 유지, 자동 다운로드 0건.
- 거래명세서 제목·안내 문구·파일명, 새로고침 후 빈 입력 3줄 초기화, 선택 삭제와 순서 이동 확인.
- 남은 이슈: GitHub Pages 배포 작업이 진행 중이라 공개 주소 최종 재검수 대기.

## 2026-09-22 — Codex (견적 품목 구조·결제 선택·도장 겹침 수정)

### 작업 전 기록
- 실제 배포 미리보기에서 원본 도장이 공급자 성명 `박강식`을 가리는 문제 확인.
- 사용자 요구: 품목 한 줄은 `상품명 / 규격 / 수량 / 단가 / 비고`만 사용. 각인은 자동 항목이나 별도 단가가 아니라 필요할 때 사용자가 하나의 품목으로 직접 입력. 비고 자동 문구 금지 및 텍스트 직접 입력. 결제 방식은 현금/카드 선택형.
- 대상: `quote.html`, `WORKLOG.md`. 원본 XLSX, Firestore 덮어쓰기, 증빙 선택, 복사, 미리보기와 다른 HELPER 기능은 유지.

### 작업 후 기록
- `quote.html` v2.3.0: 각인 단가 입력·계산·자동 행을 제거하고 품목을 `상품명 / 규격 / 수량 / 단가 / 비고` 5개 필드로 정리.
- 비고는 일반 텍스트 입력값만 XLSX 비고 열에 기록하며 기존 자동 현금/카드 문구를 제거.
- 결제 방식을 현금/카드 선택형으로 추가하고 선택 금액을 합계·공급가액·부가가치세에 반영. 카드 선택 시 별도 품목을 자동 생성하지 않음.
- 미리보기에서 셀 글자와 테두리를 원본 도장보다 위에 표시하고, 도장을 성명 오른쪽으로 이동·축소하여 `박강식` 이름과 겹치지 않도록 수정.
- 삭제 기능: 각인 단가 전용 필드와 자동 각인 행.
- 검수 결과: 공개 Pages에서 실제 저장 후 품목 필드 5개, 각인 전용 필드·자동 행 0개, 현금/카드 선택, 비고 직접 입력 반영, 다운로드 0건 확인. 도장은 성명 오른쪽에 54.6×41.6px로 표시되고 `박강식` 이름이 가려지지 않는 것을 화면에서 확인. 테스트 입력값은 빈 비고·현금 선택으로 복원 후 다시 저장.
- 남은 이슈: 특이사항 없음.

## 2026-09-21 — Codex (완성본 도장·테두리·전체 높이 복원)

### 작업 전 기록
- 사용자 제공 실제 화면에서 완성본 미리보기의 원본 도장 이미지가 빠지고, 병합된 셀의 오른쪽 테두리가 끊기며, 72vh 내부 세로 스크롤 때문에 하단 계좌정보·합계가 바로 보이지 않는 문제 확인.
- 사용자 요구: 저장하기 후 사용자 원본 XLSX로 만든 파일 완성본 전체를 정확히 미리보기. 자동 다운로드 없음.
- 대상: `quote.html`, `WORKLOG.md`. 실제 XLSX 생성·Firestore 저장·복사·원본 파일과 다른 HELPER 기능은 유지.

### 작업 후 기록
- `quote.html` v2.2.1: ExcelJS `worksheet.getImages()`와 실제 이미지 위치 범위를 읽어 원본 PNG 도장을 동일한 시트 좌표에 겹쳐 표시.
- 병합 셀의 오른쪽/아래 테두리를 병합 범위 끝 셀에서 읽도록 수정하여 오른쪽 선과 외곽선 누락을 복구.
- 미리보기 72vh 높이 제한과 내부 세로 스크롤 제거. 완성 시트 28행 전체가 페이지 안에 펼쳐지고 작은 화면의 가로 스크롤만 유지.
- 독립 생성 파일 재열기 검수: 원본 도장 PNG 1개(11,892 bytes), 도장 데이터 URL, 병합 범위 오른쪽 테두리, 하단 계좌정보·합계 렌더링 확인. 자동 다운로드 코드 0건, 인라인 JavaScript 문법과 `git diff --check` 통과.
- 기능 커밋 `d7158fa` main push 후 공개 Pages 실제 저장 검수: 도장 이미지 로드 및 84×64 표시, 오른쪽 병합 셀 테두리 표시, 계좌정보·합계까지 전체 높이 표시, 내부 세로 스크롤 없음, 완성본 미리보기 1개 확인. 다운로드 미발생 유지.

---

## 2026-09-21 — Codex (자동 다운로드 제거·저장 후 원본 미리보기 복구)

### 작업 전 기록
- 사용자 정정: `저장하기`는 생성된 견적서를 화면에서 미리 보여줘야 하며 파일을 자동 다운로드하면 안 됨. 다운로드가 필요할 때는 사용자가 별도로 요청함.
- 문제: 직전 v2.3에서 사용자 요청 없이 `저장하기`에 XLSX 자동 다운로드를 추가하여 확정된 저장/미리보기 흐름을 반대로 구현함.
- 결정: 자동 다운로드 함수와 호출을 전부 제거. `저장하기`는 사용자 등록 원본 XLSX에 입력값을 반영해 Firestore `quote-current-v1`에 덮어쓴 뒤, 방금 생성된 실제 XLSX 버퍼를 읽어 원본 시트 형태로 화면 미리보기만 표시한다.
- 대상: `quote.html`, `WORKLOG.md`. 복사하기, 품목·계산·증빙·파일명, 회사정보·계좌정보·서식·병합·인쇄 구조와 다른 HELPER 기능은 유지.

### 작업 후 기록
- `quote.html`을 실제 XLSX 직접 렌더링이 검증된 v2.2 커밋 `723db04` 상태로 복구. `downloadWorkbook`, Blob URL, `<a download>`, 자동 클릭 코드 0건.
- 저장 성공 후 Firestore 덮어쓰기와 `renderWorkbookPreview(made.buffer, made.name)`만 실행. 미리보기는 생성한 실제 XLSX 첫 시트의 셀·병합·열너비·행높이·글꼴·정렬·테두리·채우기를 읽어 표시.
- 인라인 JavaScript 문법과 `git diff --check` 통과. 기능 커밋 `efac6ca` main push 후 공개 Pages에서 실제 저장 클릭 검수 완료.
- 최종 동작: Firestore 저장 성공 후 완성 XLSX 전체 미리보기 1개 표시. 회사명 `디스이스마인`, 등록번호 `771-09-00823`, 계좌정보, 수신인, 상품, 날짜를 확인. 검수 전후 Downloads의 기존 XLSX 2개 파일명·수정시각·크기가 변하지 않았고 신규 파일도 없어 자동 다운로드 미발생 확인.

---

## 2026-09-21 — Codex (프롬프트 저장소 실제 반영)

### 작업 전 기록
- 사용자 정정: 최종 목적은 헬퍼에 프롬프트 저장소를 만드는 것. 버그 재검증 기록만 반영한 것으로 완료 처리하지 않는다. 기존 첨부 prompt.html을 기준으로 구현하고 메인 진입 링크를 추가한 뒤 main에 커밋/푸시한다.
- 기준: origin/main 3be182d, 독립 작업 트리. 대상 prompt.html 신규, index.html·WORKLOG.md 수정. 다른 작업자의 견적서 변경과 기존 메뉴·쿠폰·카탈로그·TO DO 데이터 보존.
- 기능: 제목/태그/출처 URL/원문 저장, 검색, 전체보기, 복사, 삭제, Firebase 실시간 기기 간 동기화. 기존 couponWork/helper-prompt-library-v1 문서가 있으면 유지하며 트랜잭션으로 동시 변경을 보존.
- 검수: 문법, 독립 브라우저 PC/모바일 동작, 실제 서버 저장/조회/삭제와 Pages 반영 확인. 검수 데이터는 고유 표시한 임시 항목만 사용하고 삭제한다.

### 작업 후 기록
- 신규: prompt.html. 수정: index.html, WORKLOG.md. 삭제 파일 없음. 첨부 원본의 UI와 기능을 반영하고 메인 업무 도구 첫 카드에 프롬프트 저장소 링크 추가.
- 원문 앞뒤 공백과 줄바꿈 보존, 출처 링크 HTTP/HTTPS 제한, 자동 복사 제한 시 전체 원문 펼침과 수동 복사 안내 보완.
- 검수 통과: index/prompt 인라인 JavaScript 문법, git diff --check. 독립 Chrome 실제 Firebase 임시 1건 저장, 두 번째 화면 실시간 조회, 전체보기, 원문/공백 보존, 클립보드 일치, 제목 검색, 새로고침 후 조회, 390px 가로 넘침 없음, 삭제의 두 화면 동기화 확인.
- 임시 검수 항목 삭제 완료. 기존 프롬프트/쿠폰/카탈로그/TO DO 데이터 변경 없음. PC/모바일 공통 페이지이며 실제 모바일 Safari 실기기 미검증.
- 배포: 다른 작업자의 9039531 변경을 충돌 없이 rebase하여 보존. 기능 커밋 dda32821aaab5e018bc127a3c296780ca1476a02 main push 완료. 공개 Pages prompt.html에서 실제 Firebase 저장/두 화면 동기화/검색/복사/새로고침/모바일 너비/삭제 재검증 통과. 검수 임시 항목 정리 완료. 공개 index.html의 prompt.html 진입 링크 확인. 특이사항 없음.

---

## 2026-09-21 — Codex (등록 원본 XLSX 직접 생성·다운로드)

### 작업 전 기록
- 사용자 최종 지시: 별도 표나 브라우저 재현본을 만들지 않고, Firestore에 저장된 사용자 원본 `스타벅스 대량구매 견적서 - 그린베이 250521.xlsx`를 그대로 불러와 입력값만 넣은 실제 XLSX를 사용.
- 문제: v2.1/v2.2의 웹 미리보기는 원본과 같은 파일을 보여주는 것이 아니라 브라우저에서 다시 표현한 화면이라 불필요하고 오해를 만듦.
- 결정: 웹 미리보기 UI·렌더러를 전부 제거. `저장하기`에서 등록 원본 XLSX를 열어 지정 셀만 변경한 뒤 실제 XLSX를 즉시 다운로드하고, 같은 파일을 기존처럼 Firestore `quote-current-v1`에 덮어쓴다.
- 유지: 기본 3품목/최대 5품목, 계산, 증빙 선택, 복사하기, 파일명 규칙, 회사정보·계좌정보·서식·병합·인쇄 구조. 다른 HELPER 기능과 데이터는 변경하지 않는다.

### 작업 후 기록
- 수정 파일: `quote.html`, `WORKLOG.md`. `quote.html` v2.3. 신규/삭제 저장소 파일과 Firestore 데이터 변경 없음.
- 웹 미리보기 관련 카드·CSS·함수·ExcelJS 참조를 전부 제거하고, 검증된 v2.0 원본 XLSX 생성 코드로 복원. `저장하기` 성공 흐름에 생성된 실제 XLSX Blob 다운로드만 추가.
- 저장 동작: Firestore `quote-template-v1`의 사용자 등록 원본을 열어 입력 셀을 반영한 뒤 `스타벅스 대량구매 견적서 - 수신인 YYMMDD.xlsx`를 바로 다운로드하고, 동일한 바이트를 `quote-current-v1.generated.base64`에 덮어쓴다.
- 원본 보존 검수: 사용자 원본과 실제 생성 XLSX의 ZIP 내부 파일을 전수 비교. 추가된 디렉터리 표기 2개 외 내용이 달라진 파일은 입력 대상인 `xl/worksheets/sheet1.xml` 1개뿐이며, 다른 시트·스타일·테마·문서속성 파일은 동일. 별도 견적서 양식 생성 없음.
- 검수: 인라인 JavaScript 문법, `git diff --check`, v2.0 기준 diff 확인 통과. 다운로드 함수에서 실제 XLSX MIME, 생성 파일명, 클릭, 임시 URL 해제를 독립 실행으로 확인. 웹 미리보기 관련 문구·함수·DOM·ExcelJS 참조 0건.
- 기존 기능 유지: Firestore 최신본 덮어쓰기, 복사하기, 증빙 선택, 계산, 품목 추가/삭제, 원본 파일 관리.
- 공개 Pages 검수: 기능 커밋 `3be182d` 배포 후 `저장하기` 클릭으로 `/Users/kangsik/Downloads/스타벅스 대량구매 견적서 - 샘플 거래처 260921.xlsx` 87KB 실제 다운로드와 Firestore 저장 완료 문구 확인.
- 다운로드 파일 재열기: 시트 `견적서1`, `Sheet2`, `Sheet3` 유지. 수신인 `샘플 거래처 님`, 회사명 `디스이스마인`, 등록번호 `771-09-00823`, 카카오뱅크 계좌, 상품명, 수량 11, 총액 240,500원 확인. ZIP 무결성 오류 없음. 사용자 원본과 내부 비교 결과 내용 변경 파일은 `xl/worksheets/sheet1.xml` 하나뿐이며 추가 디렉터리 표기 외 삭제 없음. main push와 공개 실제 다운로드 검수 완료.

---

## 2026-09-21 — Codex (사용자 원본 XLSX 직접 미리보기)

### 작업 전 기록
- 사용자 정정: 고객에게 전달할 견적서는 사용자가 제공한 `스타벅스 대량구매 견적서 - 그린베이 250521.xlsx`를 기초로 해야 하며, 별도로 임의 디자인한 견적서는 사용할 수 없음. 원본 회사정보·계좌정보·셀 구조·서식이 공신력의 기준임.
- 문제: v2.1에서 실제 생성 XLSX는 등록된 원본을 복제해 값만 바꾸지만, 저장 후 화면 미리보기는 입력값으로 별도 HTML 표를 구성해 원본 파일과 다른 모양으로 표시됨. 사용자가 실제 전달 파일까지 임의 양식으로 오해할 수 있는 잘못된 표현임.
- 목표: v2.1의 임의 표 미리보기를 제거하고, 저장 성공 직후 방금 생성된 실제 XLSX 버퍼를 브라우저에서 다시 열어 첫 시트의 셀·병합·열너비·행높이·글꼴·정렬·테두리·채우기와 값을 렌더링한다.
- 대상: `quote.html`, `WORKLOG.md`. 기존 원본 XLSX 복제·셀 값 교체·Firestore 동일 문서 덮어쓰기·복사하기·증빙 선택·계산·파일명은 유지. 다른 HELPER 기능과 데이터 변경 금지.
- 검수 예정: 실제 생성 XLSX와 화면 미리보기의 수신인·회사정보·계좌정보·품목·각인·배송비·합계 일치, 병합/테두리/열·행 구조, PC/모바일 스크롤, 저장 실패 시 미리보기 미갱신, Firestore 생성 파일 재열기.

### 작업 후 기록
- 수정 파일: `quote.html`, `WORKLOG.md`. 신규/삭제 파일과 데이터 변경 없음. v2.1에서 만든 별도 HTML 요약표 렌더러는 제거.
- `quote.html` v2.2: 저장 직후 `made.buffer`로 생성된 실제 XLSX를 ExcelJS로 다시 열고 첫 시트를 렌더링. 화면용 별도 품목표를 만들지 않고 XLSX 셀 값, 병합 범위, 열너비, 행높이, 글꼴, 굵기, 글자색, 채우기, 정렬, 줄바꿈, 네 방향 테두리를 읽어 표시한다.
- 고객 전달 XLSX 생성 방식은 그대로 유지: Firestore `quote-template-v1`에 등록된 사용자 원본을 JSZip으로 복제한 뒤 수신인·날짜·품목·각인·배송비·합계 셀만 변경하고 같은 `quote-current-v1`에 저장한다. 원본 회사정보·등록번호·소재지·전화번호·계좌정보·병합·인쇄 구조를 다시 만들거나 대체하지 않음.
- 독립 readback 검수: 실제 Firestore 생성 파일 `firestore-quote-readback.xlsx`를 ExcelJS로 다시 열어 시트 `견적서1`, 제목 `견 적 서`, 수신인, 회사명 `디스이스마인`, 등록번호 `771-09-00823`, 카카오뱅크 계좌, 상품명, 19열 제목 병합과 28행 렌더링을 확인. 인라인 JavaScript 문법 및 `git diff --check` 통과. 공개 화면 육안 검수에서 A10 엑셀 날짜 일련번호와 합계 수식 소수값이 보이는 차이를 찾아 원본 표시처럼 `9/21/26`, 정수 천단위 금액으로 보정.
- PC/모바일: 실제 시트 비율을 임의 재배치하지 않고 원본 열너비를 유지하며, 작은 화면에서는 미리보기 컨테이너 내부만 가로/세로 스크롤하도록 처리.
- 공개 Pages 검수: 기능 커밋 `aeb2244`, 날짜/금액 표시 보정 커밋 `723db04` 배포 후 실제 저장 클릭 성공. 화면에서 제목, 원본 회사정보·등록번호·소재지·전화번호·계좌정보, 수신인, 상품·각인·배송비, 날짜 `9/21/26`, 정수 합계가 생성 XLSX와 함께 표시됨을 확인. 날짜 일련번호 `46286` 미노출. DOM상 미리보기 카드와 실제 시트 표는 각각 1개이며 중복 없음. main push 및 공개 Pages 최종 동작 확인 완료.

---

## 2026-09-21 — Codex (견적서 저장 후 엑셀 미리보기)

### 작업 전 기록
- 사용자 요청: `견적서 만들기`에서 `저장하기`를 누르면 생성된 엑셀 내용을 바로 확인할 수 있는 미리보기를 표시.
- 배경/목표: 현재는 Firestore 덮어쓰기 완료 문구만 표시되어 저장 결과를 화면에서 확인하기 어려움. 저장 성공 직후 파일명·수신인·견적일·품목/각인/배송비·공급가액·부가세·현금/카드 합계를 엑셀 시트 형태로 화면 아래에 표시한다.
- 대상 파일: `quote.html`, `WORKLOG.md`. 기존 XLSX 생성·Firestore `catalogShares/quote-current-v1` 덮어쓰기·복사하기·원본 파일 관리·계산식과 파일명 규칙은 유지한다. `index.html`, 상품/쿠폰/카탈로그 및 다른 기능은 변경하지 않는다.
- 결정된 방식: 별도 다운로드나 ZIP 없이 브라우저 안에서 저장된 값의 읽기 전용 미리보기를 제공한다. 저장 실패 시 미리보기는 갱신하지 않으며, PC와 모바일 모두 가로로 잘리지 않게 표 영역만 스크롤 가능하게 한다.
- 미확정/주의: 브라우저 미리보기는 XLSX 프로그램 자체를 삽입하는 방식이 아니라, XLSX에 기록한 동일 데이터를 확인용 표로 표현한다. 실제 XLSX 파일 생성과 Firestore 저장 구조는 변경하지 않는다.

### 작업 후 기록
- 수정 파일: `quote.html`, `WORKLOG.md`. 신규/삭제 파일 없음. `index.html`, 상품·쿠폰·카탈로그 데이터와 다른 HELPER 기능은 변경하지 않음.
- `quote.html` v2.1: Firestore 저장 성공 후 화면 아래에 `저장된 엑셀 미리보기`를 표시. 생성 파일명, 수신인, 견적일, 이메일, 선택 증빙, 상품·각인·배송비 행, 공급가액·부가가치세·현금·카드 합계를 보여준다.
- 저장 실패 시 미리보기를 갱신하지 않으며, 성공한 `quote-current-v1` payload만 표시. 기존 XLSX 생성, 같은 Firestore 문서 덮어쓰기, 복사하기 보호, 원본 파일 관리, 다운로드 없음 정책을 유지.
- 모바일 대응: 표의 최소 너비는 유지하고 표 영역만 가로 스크롤하며, 합계는 2열로 재배치. 페이지 자체의 레이아웃은 기존 반응형 구조를 유지.
- 검수: 인라인 JavaScript 문법 검사, `git diff --check` 통과. 저장 완료 뒤 미리보기 호출 순서 확인. 샘플 1품목에서 상품·각인·배송비 3행, 빈 품목 제외, 네 합계, 파일명, HTML 특수문자 이스케이프, 두 증빙 표시를 독립 실행으로 확인. 반응형 CSS와 표 스크롤 규칙 확인.
- 데이터 영향: 배포 전 독립 검수에서는 운영 데이터를 쓰지 않음. Pages 반영 후 기존 샘플 거래처 값(상품 20,000원, 각인 1,500원, 수량 11, 배송 4,000원, 두 증빙 선택)을 같은 `quote-current-v1`에 다시 덮어써 실제 저장 흐름을 확인했으며 새 이력은 생성하지 않음.
- 공개 Pages 검수: v2.1 미리보기 요소 반영 확인. 저장 클릭 후 파일명, 수신인, 견적일, 이메일, 두 증빙, 상품·각인·배송비 3행, 공급가액 218,636원, 부가세 21,864원, 현금 240,500원, 카드 250,120원이 화면에 표시되고 페이지 전체 가로 넘침이 없음을 확인. 실제 모바일 Safari 실기기는 미확인.
- 충돌/특이사항: 작업 시작 시 main과 origin/main 일치, 기존 작업 트리 깨끗함. 요청 범위 외 변경 없음. 기능 커밋 `ed3430a`, 배포 기록 커밋 `7dc1d43`을 main에 push했고 GitHub Pages 배포 성공 및 공개 동작 확인.

---

## 2026-09-21 — Codex (다품목 간이 견적서와 증빙서류 통합)

### 작업 전 기록
- 사용자 요청: 현재 등록된 기존 XLSX 양식을 유지하면서 다품목 간이 견적서를 생성해 Firestore에 저장하고, 복사한 작업지시서를 Codex에 붙여넣어 후속 작업을 맡길 수 있게 개선.
- 품목 입력: 기본 3칸. 품목별 상품명·규격·수량·상품 단가·각인 단가를 독립 입력하고 `+`/`-`로 추가·삭제. 빈 품목은 출력하지 않음.
- 계산: 품목별 상품금액과 각인금액을 부가세 포함 금액으로 받아 공급가액과 부가가치세를 자동 분리. 배송비, 현금 총액, 카드 추가율 적용 총액까지 자동 합산.
- 문서/파일명: 이번 단계는 견적서만 구현. 엑셀 제목은 `견 적 서`, 파일명은 `스타벅스 대량구매 견적서 - 수신인 YYMMDD.xlsx`. 파일명 날짜는 다운로드 당일 기준.
- 수신인: 특정 개인 이름 `엄상아`가 들어간 예시/기본값 제거. 수신인과 이메일은 사용자가 직접 입력.
- 증빙서류: 사용자가 제공한 사업자등록증·카카오뱅크 통장사본은 XLSX 내부에 삽입하지 않고 Firestore에 각각 독립 파일로 등록. 화면에서 필요한 서류 포함 여부만 선택.
- 보존/저장: 사업자정보·계좌정보·기존 서식·병합·인쇄 구조 보존. 민감한 이미지 파일을 공개 Git 저장소에 추가하지 않음. 기본 XLSX·사업자등록증·통장사본은 Firestore에 별도 저장. 작성 중 입력은 로컬에 보존하고 `저장하기` 때 견적 XLSX를 생성해 고정된 현재 작업 문서에 매번 덮어씀(이력 누적 없음, 버튼 상시 사용 가능).
- 전달: 최종 동작 버튼은 `저장하기`와 `복사하기` 두 개. `복사하기`는 이미지 추출기의 `지시서 복사`처럼 최신 Firestore 작업 번호·수신인·이메일·생성 XLSX·증빙 선택 여부를 구조화해 클립보드에 복사. 다운로드·ZIP·메일 발송·ChatGPT 자동 열기 없음.
- 대상 파일: `quote.html`, `WORKLOG.md`. 신규 사용자용 기준 템플릿 XLSX는 검증 후 현재 등록 템플릿을 교체하는 데 사용. `index.html`, 상품/쿠폰/카탈로그 데이터 및 다른 기능 변경 금지.
- 검수 예정: 기본 3품목, 추가/삭제, 빈 품목 제외, 검색 결과를 지정 품목에 입력, 공급가액/부가세/현금·카드 합계, 기존 샘플행 제거, 파일명, 증빙 선택, 반복 저장 덮어쓰기, 로컬 입력 복원, GPT 지시서 복사, PC/모바일 레이아웃, Firestore 저장 XLSX 재열기와 시각 검수.

### 작업 후 기록
- 수정 파일: `quote.html`, `WORKLOG.md`. `index.html`, 쿠폰·상품·카탈로그 데이터, 다른 HELPER 기능은 변경하지 않음. 신규/삭제 저장소 파일 없음.
- `quote.html` v2.0: 기본 품목 3칸, 최대 5품목 추가/삭제, 품목별 상품명·규격·수량·상품 단가·각인 단가 입력. 기존 카탈로그 상품명 자동완성과 제안가 자동입력 유지.
- 공급가액·부가가치세·현금 합계·카드 추가율 합계를 자동 계산. 출력 XLSX에는 상품과 각인을 독립 행으로 기록하고 배송비 포함. 기존 견적서의 14~25행 샘플값을 전부 비운 뒤 입력값만 기록하며 서식·병합·사업자정보·계좌정보·다른 시트는 유지.
- 수신인 예시의 특정 개인명 제거. 저장 파일명은 `스타벅스 대량구매 견적서 - 수신인 YYMMDD.xlsx`, 엑셀 제목은 `견 적 서`로 고정.
- 최종 동작 버튼은 `저장하기`, `복사하기` 두 개. 저장 버튼은 항상 사용 가능하고 `catalogShares/quote-current-v1` 동일 문서에 최신 입력과 생성 XLSX를 매번 전체 덮어씀. 번호별 이력·ZIP·로컬 다운로드·메일 발송 없음.
- 복사하기는 저장 후 최신 작업 번호, 생성 파일명, 수신인·이메일, 품목, 선택 증빙 Firestore 경로를 GPT 작업지시서로 복사. 저장 뒤 입력을 변경하면 다시 저장하기 전에는 복사하지 못하게 보호.
- Firestore 원본 등록 및 readback 완료: `quote-template-v1` 29,944 bytes, `quote-business-registration-v1` 66,456 bytes, `quote-bankbook-v1` 119,476 bytes. 사업자등록증·통장사본은 XLSX 내부나 공개 Git 저장소에 넣지 않고 각각 독립 문서로 저장.
- 공개 Pages 실제 검수: 원본 3개 등록 완료 표시 확인. 샘플 거래처 1품목(상품 20,000원, 각인 1,500원, 수량 11, 배송 4,000원) 저장 후 같은 `quote-current-v1` 문서에 재저장하여 수량 11 최신값으로 덮어쓰기 확인. 생성 XLSX 88,916 bytes 재다운로드·ZIP 무결성 검사 통과.
- 계산 readback: 현금 240,500원, 공급가액 218,636.36원, 부가세 21,863.64원, 카드 250,120원(4%). XLSX 재열기에서 수신인·품목·수량·단가·각인·배송비·합계와 수식 결과 일치, 수식 오류 0건, 렌더링 육안 검수 통과.
- 독립 Chrome 1280px/390px 검수: 기본 3품목, 추가/삭제, 빈 품목 제외, 4개 합계, 저장 생성, 증빙 선택, 파일명, GPT 지시서 복사, 가로 넘침 없음 모두 통과. 실제 모바일 Safari 실기기 미확인.
- 배포: 기능 커밋 `7058f99`, 각인 행 규격 공란 처리와 최종 기록 커밋 `e74013e`를 main에 push. GitHub Pages v2.0 실제 화면 반영 확인.
- 남은 주의: 현재 Firestore `catalogShares` 규칙은 인증 없이 읽기/쓰기를 허용하므로 세 원본과 최신 견적 파일도 경로를 아는 사용자가 접근할 수 있음. 사용자가 정확한 프로젝트·경로와 민감 파일 업로드 위험을 확인하고 명시 승인한 뒤 등록함.

---

## 2026-09-21 — Codex (서버 표시 설정 이벤트 중복 등록 재검증)

### 작업 전 기록
- 사용자 요청: index.html 서버 표시 설정 apply() 실행마다 click 이벤트가 중복 등록된다는 이전 검토를 확인하고 최소 수정, 작업 전/후 기록, 테스트 후 main 커밋/푸시. prompt.html 및 다른 기능 변경 금지.
- 기준: origin/main f4007c17694d459f890b3a642299646c233a624c의 깨끗한 별도 clone. 기존 작업 폴더/다른 작업자 변경 보존, 충돌 없음.
- 실제 코드 확인: main 및 참조 대화 첨부 index.html 모두 apply 함수가 끝난 뒤 초기화 구간에서 click 이벤트를 등록함. 해당 표시 설정 코드는 cc090ca7부터 존재. 이전 대화의 중복 등록 판정과 실제 소스가 불일치함.
- 결정: 반복 클릭, 리스너 등록 횟수, 저장/새로고침, 최소 1개 서버 유지, PC/모바일 표시를 검증하고 문제가 재현되지 않으면 index.html은 불필요하게 수정하지 않고 WORKLOG.md만 갱신한다.
- 보존: 모든 코드/데이터/링크/디자인/버전. prompt.html은 현재 main에 없으며 첨부본을 이번 작업에서 신규 반영하지 않는다. 원복 요청 없음.
- 미확인: 실제 브라우저 반복 동작 및 공개 Pages 소스 일치 여부. Firebase/쿠폰/TO DO 데이터는 검수 중 쓰지 않는다.

### 작업 후 기록
- 판정: 요청된 중복 등록 버그는 현재 main 및 원본 첨부본에 존재하지 않음. apply()는 카드 표시/버튼 active/grid만 갱신하며, click 등록은 함수 바깥 초기화 구간에서 버튼당 1회 수행됨. 앞선 ChatGPT 검토의 오판을 정정한다.
- 실제 수정 파일: WORKLOG.md만. index.html 원본 유지, 신규/삭제 소스 파일 없음, prompt.html 추가/수정 없음, 다른 기능/버전/링크/디자인 유지.
- 검수: main·첨부본·캐시 회피 요청으로 받은 공개 Pages의 표시 설정 스크립트 완전 일치. index.html 인라인 스크립트 전체 Node 문법 검사 통과.
- 독립 Chrome 브라우저 검수: 1280px/390px 각각 30회 반복 클릭, 매번 active/실제 표시 카드 일치, 버튼당 리스너 1개 유지, 클릭당 localStorage 저장 1회. 마지막 서버 숨김 차단(추가 저장 없음), 새로고침 후 선택 유지, 1개 서버 grid 1열 확인. 모두 통과.
- 검수 경계: 공개 Pages에서 내려받은 HTML을 독립 브라우저에 제공하고 관련 없는 module 스크립트/외부 요청은 차단하여 운영 Firebase 데이터를 보호함. 실제 모바일 Safari 실기기 및 Firebase 통합 동작은 이번 범위 밖이며 미검증.
- 데이터 변경 0건, 기능 충돌/예상 외 변경 없음. git diff --check 통과. 코드 배포 변경 없음, 공개 Pages 대상 코드 일치 확인 완료.
- commit/배포: 검수 기록 커밋 `66256facbb55d9f268469e4dc0cfe5d219977854` main push 성공 확인. 이 후속 기록도 main에 반영. 코드 변경이 없어 기능 재배포 대상 없음.
- 남은 문제: 이번 표시 설정 범위 특이사항 없음. 첨부 prompt.html은 main에 없지만 사용자 지시대로 반영하지 않음.

---

## 2026-09-19 — ChatGPT (47건 전수 재검수 / 원본 ZIP 순서 기준)

### 작업 전 기록
- 사용자 요청: 이전 순서 오류 수정만으로는 불충분하므로 47건 전체를 다시 전수검사.
- 기준: 기존 CSV/JSON/이전 추출값을 정답으로 사용하지 않고 원본 Drive `47.zip` 내부 이미지 47장을 ZIP 내부 순서 그대로 1~47로 고정해 Coupon No. 17자리, 등록코드 8자리, 유효기간 시작일을 다시 육안 대조.
- 서버는 검수 완료 전 수정하지 않음. 원본 ZIP 및 기존 Drive 파일 삭제/변경 금지.

### 작업 후 기록
- 원본 ZIP 이미지 수 47장 확인.
- 원본 47장을 1~47 전건 육안 재검수. 현재 `coupons1.json` 47건과 Coupon No./등록코드가 전건 일치함을 확인.
- #11 원본 재확인: `56135082968201711 / 54118218`.
- 유효기간 시작일: 1~19번은 2026-09-13, 20~47번은 2026-09-12. 범위 `260912~260913`.
- 무결성: Coupon No. 17자리 전건, 등록코드 8자리 전건, 중복 0, 누락 0, no 1~47 연속.
- 오늘의 커피 쿠폰 없음. 전건 에코 텀블러 음료 쿠폰/파트너 제조 음료 한 잔 계열로 확인.
- 서버값과 원본이 일치하여 `coupons1.json`은 이번 재검수에서 재수정하지 않음.
- 검수본은 이전 파생본을 재사용하지 않고 원본 ZIP 순서대로 전체 이미지를 `001.jpg~047.jpg`로 다시 패키징하여 `47_260919_260912~260913_verified_RECHECK.zip` 생성.
- Google Drive 업로드 완료: Drive ID `1pTpKXz0XeYDlBqdow1mzETFF5sOxuq_N`.
- 원본 `47.zip` 및 기존 검수 파일은 삭제/이름변경/덮어쓰기 하지 않음.

---

## 2026-09-19 — ChatGPT (47건 서버1 순서 오류 긴급 수정)

### 작업 전 기록
- 사용자 제보: HELPER 서버1 화면의 #11이 원본 쿠폰 이미지와 맞지 않음.
- 원본 ZIP 11번째 이미지 `Screenshot_20260919_005533_KakaoTalk.jpg`를 직접 재확인한 결과 Coupon No. `56135082968201711`, 등록코드 `54118218`가 맞음.
- 원인 확인: 47건의 값 자체는 추출됐으나, 검수 시 파일명을 오름차순으로 정렬한 결과와 원본 ZIP 내부 순서를 혼동하여 no 1~47이 통째로 역순 매핑됨. ORDER_INTEGRITY 위반으로 단건 수정이 아니라 전체 순서 재검수/교체가 필요함.
- 대상: `coupons1.json`, 원본대조 검수 ZIP, WORKLOG. 원본 ZIP과 기존 Drive 파일 삭제/변경 금지.

### 작업 후 기록
- `coupons1.json` 47건을 원본 ZIP 순서에 맞게 전체 역순 재정렬하고 no 1~47 재부여.
- 수정 후 #11 = `56135082968201711 / 54118218`로 원본과 일치 확인.
- READBACK: 총 47건, no 연속성 정상, Coupon No. unique 47, 등록코드 unique 47, 중복 0.
- 수정 커밋: `d379c3616cf2a657daa55e50d36ad65e6cf9739b`.
- 원본대조 크롭 ZIP도 순서를 바로잡아 `47_260919_260912~260913_verified_CORRECTED.zip` 생성. 001~047이 원본 ZIP 순서와 1:1 대응하도록 기존 검수 크롭 순서를 역전해 재패키징.
- Google Drive 업로드 완료: `47_260919_260912~260913_verified_CORRECTED.zip` / Drive ID `1O9JDgn7vnFU_oZX05SJ_ZR-Lzl0KZ4cT`.
- 원본 `47.zip` 및 기존 Drive 파일은 삭제/이름변경/덮어쓰기 하지 않음.
- 재발방지: ZIP 내부 순서를 SOURCE ORDER로 별도 고정하고, 파일명 sort 결과를 no 부여 기준으로 사용하지 않는다. 서버 반영 전 최소 3점(first/middle/last) 원본 직접 대조로 순서 무결성을 확인한다.

---

## 2026-09-19 — ChatGPT (47건 쿠폰 전수검수 / coupons1 전체교체)

### 작업 전 기록
- 원본: Google Drive `47.zip` (file ID `1awwWgyeOx6a-jQq6hoaxuHMSn8zWzVuh`), 이미지 47장.
- 사용자 지시: Coupon No.(17자리) + 등록코드(8자리) + 사용 유효기간 시작일 추출, 원본 전수 육안대조 필수, 중복 검사, 오늘의 커피 쿠폰이 있으면 별도 CSV, `coupons1.json` 전체교체.
- 정확성 기준: SOURCE_OF_TRUTH / PAIR_BY_IMAGE / CHECK_EVERY_DIGIT / FULL_SOURCE_RECHECK / NO_GUESS / COUNT_MATCH 준수. 오류·불확실 값이 남으면 서버 반영 금지.
- 기존 `coupons1.json` 51건을 먼저 읽고 구조 확인 후 교체 진행. 기존 서버와 신규 47건 간 Coupon No./등록코드 중복도 함께 확인.

### 작업 후 기록
- 원본 ZIP 1회 다운로드·해제, 이미지 수 47장 확인.
- 47건 모두 원본 화면을 육안 확인하여 Coupon No. / 등록코드 / 유효기간 시작일을 Pair 단위로 확정. Coupon No. 17자리 47/47, 등록코드 8자리 47/47, 중복 0, 누락 0, 빈값 0, no 1~47 연속 확인.
- 유효기간 시작일 범위: 2026-09-12 ~ 2026-09-13 → 배치명 `47_260919_260912~260913`.
- 오늘의 커피 쿠폰: 0건. 전건 쿠폰 안내가 에코 텀블러 음료 쿠폰/파트너 제조 음료 한 잔 용도로 확인되어 별도 CSV 생성 없음.
- 기존 서버 51건과 신규 47건 간 Coupon No. 중복 0, 등록코드 중복 0.
- `coupons1.json`을 신규 47건으로 전체교체. commit `3b6107e95e7833f6e12984ada1ef9eabf2f2b3bb`.
- READBACK: 47건, no 1~47 연속, Coupon No. unique 47, 등록코드 unique 47, 첫/마지막 데이터 확인 완료.
- 검수 ZIP 로컬 생성: `47_260919_260912~260913_verified.zip` (47장). Drive에는 원본 ZIP의 검수 완료 사본을 같은 최종 배치명으로 생성(file ID `1xe0YAulHa532w7JqxAWzU2WLinxCIcC-`). 원본 파일은 삭제·이름변경·덮어쓰기 하지 않음.
- 남은 주의: Drive 커넥터 제약으로 로컬에서 001.jpg~047.jpg로 재명명한 ZIP 자체를 업로드하지 못해, Drive 보관본은 원본 47장 파일명을 유지하는 검수 완료 사본이다. 데이터/서버 검수에는 영향 없음.

---

## 2026-09-18 — Claude (WORKLOG 누락분 소급 정리: 2026-09-10 ~ 09-18)

### 작업 전 기록
- 사용자 확인 요청: "업무일지 작성 안한거같으니 최근까지 어떻게 개발되어왔나 한번 봐바" — WORKLOG.md 최신 기록이 2026-09-07(카탈로그 이미지 반복 버그 후속 검증)에서 멈춰 있는데, 실제 GitHub `main` 커밋은 2026-09-18(오늘)까지 계속 올라온 것을 확인함.
- 확인 방법: raw WORKLOG.md에서 최신 날짜가 2026-09-07임을 확인 → GitHub `commits/main` 페이지에서 실제 커밋 목록 확인 → 클라우드 세션에서 저장소를 clone하여 `git log --since=2026-09-08`로 2026-09-09~09-18 사이 실제 커밋 26건을 대조.
- 이 항목은 당시 작업자(ChatGPT/Codex)가 남기지 않은 작업 전/후 기록을 지금 시점에 **commit 메시지만 근거로** 소급 정리한 것. 실제 요청 배경, 확정 경위, 원복 요청, 검수 과정 등 대화 맥락은 당시 작업자만 알고 있어 이 기록에는 빠져 있음. 정확한 배경이 필요하면 해당 시점 ChatGPT/Codex 대화 기록을 확인해야 함.
- 대상 파일: `WORKLOG.md`만 수정(신규 소급 기록 추가). 코드/데이터 파일은 이번 정리에서 손대지 않음. 기존 항목은 삭제·수정하지 않고 최상단에 새 항목만 추가함.

### 작업 후 기록 (commit 기준 소급 정리, 최신순)

**쿠폰 서버 배치 교체**
- 2026-09-18 `3e17db8` — coupons1을 51건 검증 배치로 전체교체.
- 2026-09-16 `d6e1c5a` — coupons2를 41건 검증 배치로 전체교체.
- 2026-09-15 `4060bd3` — coupons3를 52건 검증 배치로 전체교체.
- 2026-09-15 `c1cf45e` — coupons3를 288건 신규 배치로 전체교체.
- 2026-09-15 `24427d0` — coupons3에 288건 검증 배치 이어붙이기.
- 2026-09-13 `9e5e862` — coupons2를 105건 검증 배치로 전체교체.
- 2026-09-11 `65de2ff` — coupons2 배치 교체(2026-09-11자).
- 2026-09-10 `0c04f19` — coupons1을 249건 검증 배치로 전체교체.

**기능 개발**
- 2026-09-15 `2b0d88b`/`fce2d7a` — 홈페이지 히어로에 상시 TO DO 패널 추가(초기 로컬 저장) 후 Firestore 문서로 동기화, 기존 로컬 목록 마이그레이션 지원(`d8c5f2e`/`1270d06`은 배포·인수인계 기록 커밋).
- 2026-09-14 `78f86db` — `image-arrange.html` 카드 높이/줄맞춤 통일, 캡션 단어 줄바꿈 보존(`80f16ae`는 배포 검증 기록).
- 2026-09-13 `17b2d32` — `image-maker.html`에 상품정보 제목 자유 입력 + 드래그로 위치 조절 가능한 가로 나눔선 2개 추가.
- 2026-09-13 `17dd117` — `image-maker.html` 제목 입력을 체크박스 옆으로 옮겨 클릭 즉시 직접 편집되도록 변경.
- 2026-09-09 `3dc8d53` — `image-arrange.html` 설명형에서 설명·가격 겹침 버그 수정, 설명형 전용 글자 크기 슬라이더 추가, 이미지형의 상품명/가격/설명을 모두 선택 입력으로 전환.
- 2026-09-09 `1c4566c` — `share.html`에 발행자 프로필(상호명/이메일/연락처/사업자번호/주소/계좌/직인) 저장·선택 표시 기능, 1글자부터 검색, 수량 직접 입력 추가.
- 2026-09-09 `97d36eb`/`ad46604` — 스타벅스 공식 머그 목록과 대조해 카탈로그 상품 19개의 이미지를 공식 이미지로 통일(Firestore 데이터 변경, 저장소 코드 변경 없음).

- 위 커밋은 모두 GitHub `main`에 반영 완료된 상태(SHA는 `git log` 기준). 배포/실사용 검증 세부 내역은 각 커밋 시점 작업자의 기록이 없어 이 소급 정리에서는 재검수를 수행하지 않았고, commit 존재 여부만 확인함.
- 남은 문제: 이 기간 작업들의 실제 "작업 전 기록"에 있었어야 할 대화 맥락(왜 바꿨는지, 확정 경위, 원복 요청 여부, 실패·재발방지 사항 등)은 이 소급 정리로 복원되지 않음. 앞으로는 각 작업자가 HARD RULE대로 작업 전/후 기록을 그때그때 남길 것.

---

## 2026-09-15 — Codex (TO DO 서버 저장)

### 작업 전 기록
- 사용자 컴퓨터 교체 예정: Firebase 저장으로 새 PC에서도 같은 목록 사용 승인. index.html/WORKLOG.md 대상, 기존 배포 기능 후속 배포 포함. 최신 main d8c5f2e, 충돌 없음.
- couponWork 내 별도 helper-hero-todos-v1 문서 사용, 쿠폰/카탈로그 문서 및 보안 규칙 변경 없음. 기존 공개 헬퍼와 동일 공유 접근, 개인 계정별 목록 아님.
- 최초 서버 생성 시 현재 로컬 목록(없으면 초기 7개) 사용, 로컬 원본 보존. 서버가 이미 있으면 이 PC 목록 가져오기 버튼으로 누락 항목만 추가. 트랜잭션으로 동시 수정 보호, 서버 저장 성공 후 완료 반영.
- 기록 삽입 첫 명령이 인코딩 오류로 실패한 채 후속 편집이 실행됨. 검수 전 즉시 기록 복구. 별도 검수 문서로 실제 두 브라우저 검수 예정.

### 배포 확인
- 기능 commit fce2d7a main push 완료. 공개 GitHub Pages HTML을 내려받아 검수한 index.html과 전체 일치 확인. 운영 TO DO 문서는 검수 브라우저에서 초기화하지 않음. 실제 사용자 PC 목록의 최초 업로드 여부는 사용자 브라우저에서 서버 저장 완료 표시 확인 필요.

### 작업 후 기록
- index.html: 로컬 전용 TO DO를 Firebase 독립 문서로 변경. 초기 로컬 목록 마이그레이션(빈 목록 포함), 기존 로컬 원본 유지, 서버 기존 목록이 있을 때 누락 항목 가져오기, 서버 저장 상태 표시, 트랜잭션 및 onSnapshot 적용. 기존 레이아웃/링크/쿠폰 동기화 코드 유지. 신규/삭제 소스 파일 없음.
- 실제 Firebase 별도 검수 문서 + 독립 Chrome 2개 검수 통과: 로컬 1개 마이그레이션, 다른 브라우저 조회, 추가/완료 양방향 반영, 새로고침, 전체 완료 후 빈 목록 유지, 동시 추가 2건 모두 보존, 기존 서버 목록에 PC 목록 가져오기. 검수 문서 삭제 완료. 쿠폰/카탈로그 데이터 변경 0건.
- git diff --check 통과. 최신 main 기준 충돌 없음. 모바일 기존 CSS 유지, 실제 Safari 실기기 미확인. 운영 문서 최초 생성은 사용자가 기존 PC로 접속할 때 수행하도록 공개 코드만 확인하고 검수 브라우저에서 운영 기본값을 먼저 생성하지 않음.
- 중요 인계: 컴퓨터 교체 전 기존 PC 브라우저에서 헬퍼 새로고침 후 '서버 저장 완료' 확인 필요. 이미 서버 목록이 있으면 '이 PC 목록 가져오기'로 기존 추가 항목 보충 가능. 목록은 헬퍼 공용이며 별도 로그인/개인별 분리 없음.


---

## 2026-09-14 — Codex (메인 히어로 TO DO)

### 작업 전 기록
- 요청: 히어로 오른쪽 빈 공간에 TO DO, 초기 7개 항목, 체크 즉시 완료/제거, 새로고침 후 유지, 간단한 추가 기능, 모바일 아래 배치.
- 대상: index.html, WORKLOG.md. origin/main 80f16ae 기준 별도 codex/hero-todo 작업 트리. 기존 변경 충돌 없음.
- 유지: 기존 히어로 문구/톤, 대시보드 표시 설정·Firebase 동기화, 모든 도구/판매처 링크, 쿠폰/카탈로그 데이터. 원복 요청 없음, 관련 없는 수정 금지.
- 방식: 독립 localStorage 키에 남은 항목 배열 저장, 빈 배열도 보존. 저장 실패/손상 시 안내하며 손상된 원본 값을 덮어쓰지 않음. PC 2열, 620px 미만 1열.
- 검수 예정: 7개 항목, 완료/추가/공백 입력/저장 유지/전체 완료, PC·모바일 배치와 기존 링크·표시 설정. 실제 모바일 Safari와 공개 배포는 별도 미확인. 현재 요청은 수정 및 검토이며 push/배포는 하지 않음.

### 배포 작업 전 기록
- 2026-09-15 사용자 `ㅇ 투두패널 올려`로 실제 배포 승인. 최신 origin/main 80f16ae와 기준 동일, 수정 2개 파일만 확인, 충돌 없음. 검수한 패널을 main에 반영하고 공개 페이지에서 체크/추가/새로고침/모바일 배치를 재확인.

### 배포 작업 후 기록
- 2026-09-15 기능 commit 2b0d88b를 main에 push 완료. 공개 GitHub Pages cache-busting 접속에서 TO DO 코드와 실제 화면 확인.
- 공개 페이지 독립 Chrome 검수 통과: 초기 7개, 체크 후 새로고침 6개 유지, 추가 후 새로고침 유지, 전부 완료 후 빈 배열 유지, 390px 아래 배치/가로 넘침 없음. PC/모바일 스크린샷 저장. 사용자 브라우저 데이터 변경 없음.
- 수정 index.html/WORKLOG.md, 신규·삭제 소스 파일 없음, 서버 데이터 변경 0건, 충돌/특이사항 없음. 모바일 Safari 실기기 미확인, Firebase 상태는 검수 시 연결 중이므로 통합 동기화 검증 주장은 없음.

### 작업 후 기록
- 수정 파일: index.html, WORKLOG.md. 신규/삭제 소스 파일 없음. 초기 7개, 체크 즉시 제거, 입력 추가, 브라우저 저장, 빈 목록 보존, 다른 탭 저장 변경 반영 구현.
- PC 히어로 2열 / 620px 미만 아래 배치. 기존 녹색/흰색 톤과 메인 패널/링크/대시보드 스크립트 유지. 쿠폰/카탈로그/서버 데이터 0건 변경.
- Chrome 실제 클릭 검수 통과: 초기 7개, 체크 후 새로고침, 추가 후 새로고침, 공백 입력 방지, 전체 완료 후 새로고침, 320/390/619/620/878/1100px 가로 넘침 없음과 배치, 기존 대시보드 표시 토글 저장, 모든 기존 href 동일, 저장 실패 시 목록 유지, 손상 저장값 보존. uncaught JS 오류 없음. PC/390px 스크린샷 육안 확인.
- 최초 자동화 check()는 완료 즉시 DOM이 제거되는 동작 때문에 시간 초과. 실제 click()과 남은 개수/새로고침 검증으로 수정하여 전체 통과. 최종 검수는 외부 요청을 차단한 독립 브라우저에서 수행, Firebase 통합 동기화 성공을 주장하지 않음.
- git diff --check 통과. 기능 충돌/예상 외 파일 변경 없음. commit/push/공개 배포 없음. 기준 commit 80f16ae. 미확인: 실제 모바일 Safari, 공개 GitHub Pages, Firebase 실시간 통합. 브라우저 간/기기 간 TO DO 동기화는 요청 범위 밖.
- 검토용 outputs에 수정 index.html, WORKLOG.md, patch, PC/모바일 미리보기와 검수 결과 제공.

---

## 2026-09-14 — Codex (이미지 배열 카드 정렬 / 검토용)

### 작업 전 기록
- 요청: 스크린샷의 이미지형 5열 배열에서 제목의 한 글자 줄바꿈, 설명 높이, 가격선과 카드 높이를 통일. 미리보기 검토 전 배포 금지.
- 대상: image-arrange.html, WORKLOG.md. 최신 origin/main 9e5e862 기준 별도 codex/uniform-image-cards 작업 트리. 기존 변경 충돌 없음.
- 방식: 기존 contain 사진/중앙 배치와 사용자 확대·위치 보존, 제목 단어 단위 최대 2줄, 모든 카드에 공통 제목/설명/가격 영역 적용. 설명은 가장 긴 내용에 맞춰 공통 높이를 확보해 내용 보존. 사진만 출력 시 캡션 공간 없음.
- 유지: 원본 이미지, 상품명/설명/가격 데이터, 설명형, 열/간격, 사진 조절, 순서/선택/삭제, PNG 저장. 신규 기능/데이터 수정/삭제/배포 없음. 원복 요청 없음.
- 주의: 원본 업로드 사진은 제공되지 않아 첨부 화면의 사진 영역을 검수용으로만 사용. 실제 사용자 사진 내부 여백과 수동 배율은 임의 변경하지 않음.

### 후속 작업 전 기록
- 사용자: 정렬뿐 아니라 내용이 이상하므로 전부 수정한 뒤 전달. 공식 250g 원두 상세페이지 10종을 실제 열어 설명 대조. 원문에도 피니로 등 오타가 있어 원문 의미를 보존한 짧은 문구로 재작성. 가격 유지.
- 범위: 이미지형 제목의 두 줄 균형과 설명의 단어 단위 줄바꿈. 실제 상품 설명은 서버에 존재하지 않는 사용자 입력이므로 수정 문구 파일과 미리보기에 제공하고, 헬퍼에서 임의 자동 치환하지 않음.

### 배포 작업 전 기록
- 사용자 `ㅇㅋ ㄱ` 승인으로 검토한 수정본을 배포. 최신 origin/main 9e5e862 확인, 수정 대상 2개 파일 외 변경 없음. 기존 데이터 유지. 공개 페이지 실제 검수 예정.

### 배포 작업 후 기록
- 기능 commit 78f86db, 사용자 승인 후 main push 완료. 공개 GitHub Pages에 cache-busting 접속해 captionWordLines 존재와 실제 렌더링 확인.
- 배포된 페이지에서 10개 상품 재현, 2~6열 가격선, 직접 지정 상품명 줄바꿈, 설명형/사진 전용, 390px, 배율/초기화, PNG 실제 다운로드 860×767 및 육안 검수 통과. JS 오류 없음. 서버 데이터 변경 0건. 모바일 Safari 실기기 미확인. 그 외 특이사항 없음.
- 설명 수정본은 제공 파일에 반영되어 있으며 사용자 브라우저의 기존 입력 문구를 강제로 덮어쓰지 않음. VER-2026.09.14-78f86db.

### 작업 후 기록
- 후속 완료: 공식 250g 상세페이지 10건 전수 대조 후 설명을 의미 보존해 재작성(근거 URL은 outputs/coffee-copy.md/json). 공식 원문의 피니로 등 오타도 그대로 복사하지 않음. 판매가격은 사용자 입력값 유지, 현재 가격 확인 주장 없음.
- image-arrange.html: 설명 단어 우선 줄바꿈, 두 줄 제목의 길이 균형, 상품명 textarea로 수동 줄바꿈 지원. 임의 의미 분석을 보장하지 않으며 디카페인/하우스 블렌드 및 파이크 플레이스/로스트는 수정본에서 직접 줄 지정. 일반 사용자 입력 자동 치환 없음.
- v2 검수: 10개 가격선, 의미 단위 지정 제목, 2~6열, 설명형, 사진만 출력, 입력/배율 초기화, 390px, PNG 실제 저장 및 육안 확인 통과. 기존 파일을 보존한 card-preview-v2.png(860×767) 제공. 여전히 스크린샷 사진 재현본이며 원본 해상도 납품을 주장하지 않음. 배포 없음.
- 수정: image-arrange.html, WORKLOG.md. 신규/삭제 소스 파일 없음. 제목 단어 우선 줄바꿈 및 2줄 초과 말줄임, 전체 카드 공통 제목/설명/가격 높이와 행 간격 적용. 설명 원문은 생략하지 않음. 기존 정사각형 contain/중앙 배치와 수동 사진 조절 보존.
- 검수: 로컬 Chrome에서 첨부 화면 사진 영역 10장으로 재현. 2~6열 가격 기준선 일치, 고아 글자 드/트 없음, 사진 전용 캡션 공간 없음, 설명형 렌더링, 상품명 입력, 배율 변경/초기화, 390px 화면, 실제 PNG 다운로드(860×767) 및 육안 검수 통과. JavaScript 실행 오류 없음, git diff --check 통과.
- 데이터 변경 0건. 충돌/특이사항 없음. 원본 업로드 파일 대신 화면 추출 사진을 사용한 검수용 미리보기이며 실제 사용자 세션/모바일 Safari 실기기/드래그 정렬은 미검수. 관련 처리 코드는 변경하지 않음.
- 기준 commit 9e5e862. 신규 commit/push/배포 없음(사용자 검토 대기). 결과 PNG와 변경 소스 사본/patch를 현재 작업 outputs에 제공.


STARBUCKS HELPER 전체의 **공용 업데이트·업무 작업일지**. ChatGPT / Claude / Codex가 같은 저장소와 HELPER 업무를 이어서 작업하므로, HELPER 관련 업무는 작업 전과 작업 후 모두 이 파일에 남긴다.

## WORKLOG 역할 HARD RULE
- [GLOBAL_SCOPE] `WORKLOG.md`는 쿠폰 전용이 아니라 **STARBUCKS HELPER 전반**의 공용 기록이다. 쿠폰, 카탈로그, 공유페이지, 계산기, 서버, 데이터, UI, 버그수정, 성능개선, 배포, 운영정책, 파일 구조 변경 등 HELPER 관련 모든 업무를 기록한다.
- [NOT_COUPON_INSTRUCTION] `WORKLOG.md`는 쿠폰 실행 작업지시서가 아니다. 쿠폰 추출·검수·크롭·서버 반영 실행 규칙은 `COUPON_WORK_INSTRUCTIONS.md`와 `image-extractor.html`의 생성 지시서에서 관리한다.
- [NO_ROLE_MIX] 쿠폰 실행용 상세 프롬프트/원본 데이터 전체를 WORKLOG에 복사하지 않는다. 반대로 HELPER 개발·운영 이력, 대화에서 확정된 요구사항, 변경 이유, 원복 요청, commit 정보는 쿠폰 작업지시서에 섞지 않는다.
- [SHARED_SOURCE] 이 파일은 ChatGPT / Claude / Codex가 모두 읽는 **공동 인계 원장**으로 취급한다. 한 작업자가 알고 있는 HELPER 업무 맥락을 다음 작업자가 잃지 않게 하는 것이 목적이다.

## 작업 전/후 기록 HARD RULE
- [READ_FIRST] HELPER 관련 업무를 시작하기 전에 최신 WORKLOG를 먼저 읽는다.
- [PRE_WORK_LOG] 실제 수정 전에 이번 대화에서 나온 업무 내용을 최신 항목에 `작업 전 기록`으로 남긴다. 최소 포함: 사용자 요청, 왜 필요한지, 문제 증상, 목표, 대상 파일/기능, 유지할 기존 기능, 원복 요청, 금지사항, 사용자가 확정한 방식, 미확정/주의사항.
- [CONVERSATION_CAPTURE] 대화에서 나온 업무상 중요한 판단을 결과만 축약하지 말고 기록한다. 특히 **왜 바꾸는지 / 무엇을 되돌리는지 / 어떤 방식이 문제였는지 / 새로 확정한 운영규칙 / 실패나 사고에서 나온 재발방지 조건**을 다음 작업자가 이해할 수 있게 남긴다. 단순 잡담은 제외한다.
- [WORK] WORKLOG를 읽고 작업 전 기록을 남긴 뒤 실제 수정/검수/배포를 수행한다.
- [FINAL_RECHECK] 최초 사용자 요청, 대화에서 확정된 조건, 기존 WORKLOG의 제약을 다시 대조한다. 검수 전 완료 선언 금지.
- [POST_WORK_LOG] 완료 선언 전에 같은 최신 항목을 `작업 후 기록`으로 마무리한다. 최소 포함: 실제 수정 파일, 실제 변경, 복구/삭제/유지한 기능, 테스트/검증 결과, PC/모바일 영향, 데이터 영향, commit SHA, 배포상태, 남은 문제/다음 작업.
- [READBACK] WORKLOG 저장 후 다시 읽어 작업 전/후 기록이 실제 반영됐는지 확인한다.
- [NO_SILENT_WORK] HELPER 관련 실제 변경을 했는데 WORKLOG 기록 없이 끝내는 것을 금지한다.

---

## 2026-09-13 — Codex (체크박스 옆 제목 직접 편집)

### 작업 전 기록
- 사용자 후속 요청: 별도 `항목 제목` 문구/입력칸 대신 체크박스 옆 `재질`, `크기` 자체를 직접 수정.
- 대상: image-maker.html, WORKLOG.md. 같은 제목 입력 요소를 체크박스 옆으로 이동하고 밑줄로 편집 가능함을 표시. 기존 제목 값/캔버스 연동, 글꼴 대상 선택, 내용 입력, 나눔선, 저장 유지.
- 최신 main 83af8f0과 일치/작업 트리 깨끗함. 서버 데이터 수정 없음. 이미 승인된 기능의 후속 수정/배포. 사용 중인 창은 강제 새로고침하지 않음.

### 작업 후 기록
- image-maker.html: 체크박스 옆 제목 자체가 입력란이 되도록 이동. 별도 `항목 제목` 라벨/고정 재질·크기 라벨 제거. 기존 입력 ID와 값/렌더 연동 유지. 신규/삭제 파일 없음.
- 사용자 추가 확인: `그냥 바로 재질 에 입력하면 바로 수정되게` — 제목 클릭 후 직접 타이핑 시 즉시 미리보기 갱신.
- 로컬 Chrome 검수: 제목 직접 클릭/타이핑, 항목 제목 문구 없음, 체크박스와 제목 같은 줄 정렬, 편집 중 체크박스 상태 보존/독립 선택, 자유 입력/치수/나눔선 PC·터치/초기화/PNG·JPG 실제 저장 통과. 서버 데이터 0건 변경, 충돌/특이사항 없음. 모바일 Safari 실기기 미확인.
- 기능 커밋: 17dd117. origin/main 배포 완료. 공개 GitHub Pages에서 제목 직접 타이핑/체크박스 독립 선택/같은 줄 배치와 기존 편집·드래그·PNG/JPG 저장 전체 재검수 통과.

---

## 2026-09-13 — Codex (상품정보 항목 수정과 나눔선 이동)

### 작업 전 기록
- 사용자 요청: 원두·머그·푸드·가방에 맞게 재질/크기 제목과 내용을 수정하고, 가로 나눔선 위치를 드래그로 조절.
- 대상: image-maker.html, WORKLOG.md. 최신 origin/main 기준 별도 작업 트리 사용, 기존 변경과 충돌 없음.
- 방식: 제목 입력란, 기존 치수 입력을 유지하는 직접 입력 모드, 미리보기의 두 나눔선 포인터 이동과 자동 위치 복원.
- 유지: 사진 업로드/배율/배치, 글꼴, 재질 버튼, 사용 안내, PNG/JPG 저장. 원본 사진과 서버 데이터 변경 금지. 원복 요청 없음.
- 주의: 화면 배율/모바일 터치, 제목/내용 출력, 이미지 저장을 검수하고 배포 상태를 기록.

### 작업 후 기록
- 수정 파일: image-maker.html, WORKLOG.md. 신규/삭제 파일 없음. 재질/크기 제목 자유 입력, 크기 내용 직접 입력 모드(기존 치수 값 보존), 가로 나눔선 2개 드래그/터치 이동 및 자동 위치 복원 추가.
- 검수: Chrome 실제 브라우저에서 제목/내용 캔버스 반영, 직접 입력↔치수 전환 시 값 유지, PC 드래그, 390px 모바일 화면 폭/실제 터치 이벤트 좌표 환산, 초기화, PNG/JPG 다운로드 통과. 실제 두 파일 1000×1000 확인 및 PNG 육안 확인. JS 실행 오류/변경 공백 오류 없음.
- 초기 모바일 테스트의 합성 포인터 이벤트는 활성 포인터가 없어 capture 오류를 발생시켰으며, 실제 CDP 터치 이벤트로 교체하여 통과. 제품 코드 오류 아님.
- 기존 사진/글꼴/재질 버튼/안내/저장 흐름 유지, 서버·쿠폰·카탈로그 데이터 0건 변경. 충돌 없음. 모바일 Safari 실기기 미확인.
- 기능 커밋: 17b2d32. 최초 push는 자동 승인 검토에서 명시적 배포 승인 부족 사유로 거부되었으나, 후속 사용자 `ㅇㅇ` 및 `맞아 해줘` 승인 후 d94ec40까지 origin/main 반영 완료.
- 공개 GitHub Pages image-maker.html에서 새 코드 확인 및 실제 브라우저 전체 회귀(제목/자유 내용/치수 전환/PC 드래그/모바일 터치/초기화/PNG·JPG 실제 다운로드) 통과. 첫 검수는 배포 직전 캐시에서 새 입력란이 없어 시간 초과했으며, 배포 갱신 후 전체 재통과. 미확인: 모바일 Safari 실기기. 그 외 특이사항 없음.

---

## 2026-09-09 — Codex (이미지 배열 설명형 글자 겹침 수정)

### 작업 전 기록
- 사용자 요청: HELPER의 `이미지 배열` 설명형에서 긴 설명이 가격과 다음 상품 영역을 침범하는 문제를 한 번에 수정하고, 내용에 맞춘 글자 축소 또는 글자 크기 조절 기능을 제공. 추가로 이미지형은 상품명·가격 없이 사진만 가로 배열한 현재 상태에서도 저장할 수 있게 하고, 필요할 때는 사진 아래에 설명을 선택 입력할 수 있게 함.
- 문제 확인: 첨부 화면의 데비 머그 414ml 예시에서 설명 줄 수를 기준으로 계산한 행 높이와 실제 설명·가격 기준선 사이 여백이 부족해 마지막 설명과 가격이 겹침. 긴 상품명 높이도 행 계산에 포함되지 않음.
- 확정 방식: 설명형 각 상품 행을 상품명 줄 수·설명 줄 수·선택 글자 크기로 다시 계산해 내용이 길면 행 높이가 자동 증가하도록 수정. 배치 설정에 설명형 전용 글자 크기 슬라이더를 추가하고 선택값을 기존 로컬 설정에 함께 저장. 이미지형의 상품명·가격·설명은 모두 선택 입력으로 전환하고, 입력된 항목만 사진 아래에 출력하며 같은 가로줄의 높이를 자동 정렬.
- 대상: `image-arrange.html`, `WORKLOG.md`. 사진 추가/위치/배율, 상품 순서, 가로 열 수/간격, 선택 초기화·삭제, PNG 저장과 기존 입력 데이터는 유지.
- 주의사항: 사용자가 올린 사진과 입력값을 삭제하거나 서버 상품 데이터를 변경하지 않음. 긴 설명·두 줄 상품명·최소/최대 글자 크기, 연속 상품 간 겹침, PC/모바일 조작과 PNG 출력을 검수.

### 작업 후 기록
- 실제 변경 파일: `image-arrange.html`, `WORKLOG.md`.
- 설명형은 상품명 실제 줄 수와 설명 실제 줄 수를 선택 글자 크기의 줄높이로 계산하고, 마지막 설명 뒤에 가격 여백을 확보한 높이로 상품별 행을 자동 확장. 긴 설명·두 줄 상품명·가격·다음 상품이 서로 겹치지 않게 함.
- 배치 설정에 설명형 전용 14~22px 글자 크기 슬라이더를 추가하고 기본 20px 및 선택값을 기존 localStorage 설정에 보관. 이미지형에서는 해당 설정을 숨겨 기존 흐름을 유지.
- 추가 요청 반영: 이미지형의 상품명·가격·사진 아래 설명을 모두 선택 입력으로 변경. 사진만 추가한 상태에서도 PNG 저장을 허용하고, 빈 항목은 출력하지 않으며 불필요한 하단 공간도 제거. 입력한 설명은 각 사진 아래 자동 줄바꿈하고 같은 가로줄에서 가장 긴 문구의 높이에 맞춰 다음 줄을 배치하여 2~6열 가로 배열을 유지.
- 유지 기능: 사진 다중 추가, 확대/축소·위치 이동, 순서 변경, 자동/2~6열, 간격, 선택 초기화·삭제, 상단 제목, 설명형 필수 입력 검사와 PNG 파일명/저장 방식 유지. 서버·카탈로그 데이터 영향 없음.
- 검증: 두 인라인 JavaScript 구문 검사와 변경 공백 검사 통과. 로컬 브라우저에서 사진 3장·상품 정보 전부 빈 이미지형을 구성해 저장 버튼 활성화와 설명 입력란 노출을 확인. 행 높이 계산에서 캡션 상단/하단 여백과 가로줄별 최대 캡션 높이를 포함하는지 확인. PC와 모바일은 같은 반응형 편집 UI와 860px PNG 캔버스를 사용.
- 기능 커밋 `3dc8d53`. 사용자 `ㄱㄱ` 승인 후 원격 `main`에 반영 완료. GitHub Pages 실제 HELPER 배포 확인은 후속 검수에 기록.
- 남은 문제: 브라우저 자동화의 다운로드 이벤트 수신은 로컬 iframe 환경에서 시간 초과했으나, 저장 버튼 활성화와 기존 `canvas.toBlob` 다운로드 경로 유지 및 구문 검증은 확인됨.

---

## 2026-09-09 — Codex (공식 스타벅스 머그 이미지 대조·통일)

### 작업 전 기록
- 사용자 요청: 스타벅스 공식 머그 목록(`product_list.do?CATE_CD=product_mug`)과 현재 서버 카탈로그 상품을 대조해 이미지 출처와 구성을 통일.
- 배경/확인: 원격 `main` `1c4566c294d2f754b868c05aed1ed4c7961c6ada`에서 새 작업 브랜치를 생성. 공식 머그 JSON은 현재 40개, 내부 작업 카탈로그와 공개 공유본 `c6xxh9`는 각각 250개이며 정규화한 상품명이 정확히 일치하는 항목은 19개로 확인. 일부 일치 상품은 스타벅스 공식 이미지, 일부는 무신사 이미지를 사용 중.
- 확정 방식: 상품명과 용량까지 정확히 일치하는 항목만 공식 상품코드의 상세 페이지 이미지로 교체하며, 유사명·불확실한 상품은 변경하지 않음. 변경 전 서버 문서를 백업하고 내부 작업본과 현재 공개 공유본을 함께 맞춘 뒤 재조회하여 검증.
- 대상: Firestore `catalogShares/work-k4m8q2x7n9v3c6p5r8t1`의 `catalogData.products`, `catalogShares/c6xxh9`의 `products`, 본 WORKLOG. 상품명/가격/분류/태그/정렬/공유 ID와 기존 UI 코드는 유지.
- 주의사항: 공식 목록에 없는 과거·한정 상품은 임의 추정하지 않음. 로컬 업로드 이미지나 수동 편집 이미지가 있는 상품은 덮어쓰지 않으며, 운영 데이터 반영 후 변경 상품 수·이전/신규 URL·PC/모바일 표시를 확인.

### 작업 후 기록
- 실제 변경: Firestore 내부 작업본과 공개 공유본 `c6xxh9`에서 공식 머그 40개 중 상품명·용량이 정확히 일치한 19개를 확인. 기존 이미지가 이미 동일한 7개는 유지하고, 나머지 12개의 `images`/내부 `remoteImages`를 각 공식 상세 페이지의 전체 이미지로 교체하여 총 93개 공식 이미지로 통일. 내부 작업본 19개에는 공식 상품 상세 URL도 저장.
- 변경 상품: 스태카 컬러 머그볼 260ml 3P, 안녕 서울/부산 머그 355ml, 배꽃 데미머그 89ml 2P, 한글 시온 머그 414ml, 워시드 블루 머그 앤 코스터 세트, 코리아 라온 머그 414ml, 스태카 머그볼 260ml 2P, SS 블랙/민트/그린 데비 머그 414ml, 한글 데미 머그 89ml. 공식 이미지 수는 상품별 3~10개.
- 유지/제외: 공식 목록의 나머지 21개와 카탈로그의 불일치·과거·세트 상품은 추정하지 않아 미변경. 전체 250개 상품의 이름/가격/분류/태그/정렬/ID와 공유 ID, UI 및 저장·공유 기능은 유지. 공개 공유본에는 내부 관리용 `remoteImages`/`sourceUrl` 필드를 추가하지 않아 기존 문서 구조를 유지.
- 검증: 반영 후 두 Firestore 문서를 다시 읽어 19개 전체의 이미지 배열과 공식 URL 일치, 250개 상품 수 유지, 대상 이미지/출처 외 상품 데이터 불변을 확인. 공식 이미지 93개 모두 HTTP 이미지 응답 정상. 실제 공개 공유 화면에서 250개 로드 및 변경 상품의 1/3~1/10 이미지 이동 버튼/카운터 노출을 확인.
- PC/모바일 영향: 데이터만 변경되어 레이아웃과 조작 방식 변화 없음. 공개 페이지는 같은 URL에서 즉시 새 이미지를 읽음.
- 데이터/배포 상태: 운영 Firestore 작업본과 `https://runipokr-dotcom.github.io/STARBUCKS-HELPER/share.html?id=c6xxh9`에 반영 완료. 변경 전 REST 문서는 작업 중 임시 백업으로 보관. 저장소 코드 변경 없음. 작업 기록 커밋 `97d36eb`은 원격 `codex/starbucks-mug-image-unification` 브랜치에 게시했으며, 원격 `main` 직접 반영은 자동 승인 검토에서 보류됨.
- 남은 문제: 이번 요청에서 제공된 머그 카테고리만 대조. 다른 공식 카테고리는 별도 대조가 필요함.

---

## 2026-09-09 — Codex (발행자 프로필 / 견적서 선택 표시)

### 작업 전 기록
- 사용자 요청: 현재 main과 실제 배포 UI를 먼저 확인하고 상호명 영역에 프로필 불러오기·상세정보 팝업을 통합. 상호명/이메일/연락처/사업자번호/주소/은행/계좌번호/예금주/직인 및 표시 여부를 프로필별 localStorage에 보관하고 선택 정보만 최종 견적서 하단에 출력. 1글자 검색과 +/-를 유지한 수량 직접 입력도 적용.
- 배경/확인: main `0dc0e6bbdc77a36125c09c83b6e43b5542cd9bd2` 새 복제. 공개 Pages의 share.html이 main과 바이트 단위 일치. 이전 대화 첨부 스크린샷의 장바구니/상호명/저장 완료는 share.html과 일치하며, 대화의 할인율 언급은 스크린샷과 불일치. 삭제된 quotation.html을 복구하거나 과거 파일로 덮어쓰지 않음.
- 확정 방식: 기존 상호명 줄에 작은 불러오기/설정 컨트롤, 상세 필드는 모달. 고객사가 자기 고객에게 제공하는 견적서이므로 특정 발행자의 상세정보를 고정하지 않으며 전부 선택 입력/표시. 검색은 현재 미구현이므로 공유 상품 필터에 1글자부터 검색 추가.
- 대상: share.html, WORKLOG.md. 상품 데이터/카테고리/가격계산/정렬/이미지/URL 추출/게시·주문저장 동선은 보존. 게임 3종 및 쿠폰 작업지시서 변경 금지. 서버에 프로필 원본·직인을 저장하지 않음.
- 검수: 프로필 전환·재로드·표시 선택·이미지 업로드/삭제·저장 오류, 검색/수량/합계/필터/정렬/이미지 및 기존 저장 성공/실패를 확인. 운영 주문을 테스트로 생성하지 않음.
- 배포: 우선 수정과 검수 결과를 준비. 원격 main 변경 여부를 최종 재확인.
- 후속 작업 전 기록: 사용자가 “ㅇㅋ”로 변경 브랜치 GitHub 업로드 및 검토 요청(PR) 생성을 승인. 코드 변경 없이 원격 main 재확인, 기존 검수 브랜치 게시, draft PR 생성과 이력 갱신을 진행. main 병합/운영 배포는 이번 승인 범위에 포함하지 않음.

### 작업 후 기록
- 실제 변경 파일: `share.html`, `WORKLOG.md`. 상호명 옆 네이티브 프로필 선택/설정 버튼, 상세정보 dialog, 프로필 이름 기준 저장/갱신/복원, 마지막 선택 재접속 복원, 이번 견적만 적용/취소를 구현.
- 상호명/이메일/연락처/사업자번호/주소/은행/계좌번호/예금주/직인 및 개별 출력 표시를 localStorage 키 `starbucks-helper-issuer-profiles-v1`에 보관. 프로필별 기존 제안가 표기 설정도 복원. 직인은 5MB 이하 PNG/JPG/WebP를 최대 320px PNG로 정규화하며 교체/삭제/저장 실패/손상 저장값을 처리.
- 최종 JPG는 기존 상품행·합계·색상·640px 논리폭(2배 출력)을 유지. 선택된 연락처/상세정보만 하단에 줄바꿈하고 직인을 비율 유지해 배치. 기본 1줄 연락처의 높이 유지. 저장하기 → JPG 다운로드 → 기존 Firestore 주문 생성 → 저장 완료 흐름 보존. 이미지 준비 중 중복 저장/프로필 전환 방지.
- 공유 상품 필터에 1글자부터 상품명/분류/태그 검색 추가. 수량 입력은 1~999999 정수 직접 입력, 기존 +/- 유지, 빈 값/음수/소수/범위 초과 복원. 기존 공급가액/부가세 계산과 필터·정렬·이미지 로직 보존.
- 검수: 최종 코드 인라인 JS 구문/공백 검사 통과. 공개 상품 3개 복사본과 서버 저장 대역을 사용한 브라우저 회귀 36개 통과(검색/선택/분류/태그/수량 경계값/공급가액·부가세/정렬 3종/드래그/이미지 전환/프로필 A↔B/저장 실패/직인 업로드·삭제·취소/모두 숨김/JPG/주문 저장 성공·실패). 첫 재실행에서 이전 프로필의 제안가 설정을 고려하지 않은 테스트 초기조건을 바로잡고 전체 재통과.
- 추가 실검수: 재접속 시 마지막 프로필과 직인·표시 복원, PC 및 390×844 모바일 UI 확인. 모바일 문서 폭이 viewport를 넘지 않고 dialog가 내부 스크롤됨. 실제 저장 버튼으로 JPG가 Downloads에 생성됨(브라우저 다운로드 이벤트 대기는 시간초과했으나 2026-09-09 18:37:53에 생성된 85,901바이트 JPG 파일로 확인). 긴 주소/직인 겹침·잘림 없음.
- 데이터 영향: 운영 주문 생성/상품 수정 없음. 상품 데이터·카테고리·가격계산·정렬·이미지·URL 추출·게시 코드와 게임/쿠폰 규칙 파일은 변경하지 않음. 신규 상세정보와 직인/프로필은 서버 전송하지 않으며 기존 주문 contact 필드는 유지.
- 기능 커밋: `4ee9adf`. 최종 작업 후 기록은 후속 로컬 커밋. 기준/마지막 확인 원격 main: `0dc0e6bbdc77a36125c09c83b6e43b5542cd9bd2`.
- 게시/배포 상태: 최초 원격 push는 승인 검토에서 거부되어 보류했으나, 후속 사용자 승인 후 `codex/issuer-profiles` 브랜치 업로드 완료. 검토용 draft PR #1: https://github.com/runipokr-dotcom/STARBUCKS-HELPER/pull/1 . 게시 직전 원격 main은 기존 기준 SHA와 동일. 기능/검수 커밋 `4ee9adf`, `b24c94b`를 게시했고 이 후속 기록은 추가 문서 커밋으로 반영. main 병합 및 운영 배포는 미실행.
- 후속 검수: 코드 변경 없음. WORKLOG readback/공백 검사 및 원격 브랜치 SHA 일치 확인. PC/모바일/상품·서버 데이터 영향 없음.
- 남은 작업: PR 리뷰 후 main 병합/배포. 실제 모바일 Safari의 파일 저장은 실기기 검증하지 못했으며, 현재 검수는 Chrome의 모바일 viewport 및 실제 JPG 다운로드 기준. 프로필은 다른 기기로 자동 동기화되지 않음.

---

## 2026-09-07 — Codex (이미지 배열 2열 옵션 추가)

### 작업 전 기록
- 사용자 확인 중 이미지형에 상품을 2개씩 배치하는 선택지가 빠진 점을 확인함.
- 목표: 기존 자동/3/4/5/6열과 모든 상품 카드·PNG 기능을 유지하면서 `2개` 수동 열 선택을 추가.
- 대상 파일: `image-arrange.html`, `WORKLOG.md`. 다른 HELPER 기능과 보호 대상 게임 파일은 변경하지 않음.

### 작업 후 기록
- `image-arrange.html` 배치 설정에 `2개` 버튼을 추가해 이미지형에서 상품을 2열씩 출력할 수 있게 함. 기존 자동/3/4/5/6열은 그대로 유지.
- JavaScript 문법, 변경 공백, 2열 옵션 존재 여부, 보호 대상 게임 파일 미변경 검사를 통과함.
- 기능 커밋: `664aef2`. WORKLOG 최종화 커밋은 본 커밋. GitHub `main` 푸시 예정.

---

## 2026-09-07 — Codex (상품 이미지 배열 자동 제작 확장)

### 작업 전 기록
- 사용자 요청: 기존 단순 이미지 합치기를 상품 4~6개용 제작 도구로 확장. 사진마다 상품명과 가격을 입력하면 `사진 → 상품명 → 가격` 순서의 가로 배열을 자동 생성하고, 별도의 설명형에서는 정사각형 사진 왼쪽과 자동 줄바꿈 설명 오른쪽을 한 행으로 출력.
- 필요한 이유: 네이버 상품 옵션처럼 동일한 정사각형 이미지 박스를 사용해 `DANTE SERIES :` 상품 묶음을 빠르게 PNG로 만들고, 수작업 정렬과 반복 편집을 줄이기 위함.
- 확정 방식: 이미지형/설명형 전환, 제목 수정, 정사각형 프레임, 상품별 45~250% 사진 크기 및 위치 조절, 상품 카드 단위 드래그 순서 변경, 숫자 가격의 원화 서식, 자동/3~6열, 3단계 간격, 선택 상품 초기화·삭제, 860px PNG 출력.
- 유지 조건: `index.html`의 기존 진입점과 다른 쿠폰/카탈로그/공유/계산기 기능은 유지. `territory-game.html`, `TERRITORY_GAME_PLAN.md`, `TERRITORY_GAME_WORKLOG.md`는 수정하지 않음.
- 제외/보류: PNG에서 작동하지 않는 쿠팡 클릭 링크, QR, ZIP, 웹 카탈로그 자동 연동, 강제 다운로드 하위 폴더 생성은 이번 범위에서 제외. 브라우저가 허용하는 기본 다운로드 방식과 제목·형태·날짜 기반 파일명을 사용.

### 작업 후 기록
- `image-arrange.html`을 상품 카드 편집 방식으로 개편. 여러 사진을 올리면 사진별 상품명·가격·설명 입력란과 정사각형 미리보기 카드가 자동 생성됨.
- 이미지형은 사진·상품명·가격을 가로 배열하고, 설명형은 사진 왼쪽/상품명·설명·가격 오른쪽의 세로 반복 구조로 출력. 설명은 엔터와 한글·영문 장문 자동 줄바꿈을 지원함.
- 사진별 45~250% 확대·축소, 사진 박스 내부 위치 이동, 개별 초기화, 선택 상품 일괄 초기화·삭제를 구현. 이동 손잡이로 PC 드래그 및 모바일 포인터 순서 변경을 지원함.
- 자동 열은 1~5개를 한 줄, 6개 이상을 3열로 배치하며 3/4/5/6열 수동 선택과 좁게/보통/넓게 간격 선택을 제공. 제목과 형태·배치 설정은 로컬에 저장됨.
- PNG 저장 전에 상품명·가격, 설명형의 설명 누락을 검사하고 `제목_이미지형(또는 설명형)_YYYYMMDD.png` 형식으로 저장함.
- 검증: 두 인라인 스크립트 JavaScript 문법 검사, 변경 공백 검사 통과. 보호 대상 게임 파일 및 다른 HELPER 파일 변경 없음. 로컬 파일 URL은 브라우저 보안 정책으로 UI 자동검증이 차단되어 실제 모바일 사진 선택·드래그·PNG 저장은 배포 후 실기기 확인 필요.
- PC/모바일 및 데이터 영향: 브라우저 안에서만 동작하며 서버/Firebase/쿠폰/카탈로그 데이터 영향 없음.
- 기능 커밋: `7e80d9e` — 상품 이미지 배열 자동 제작 확장. WORKLOG 최종화 커밋은 본 커밋.
- 배포 상태: GitHub `main` 푸시 예정.

---

## 2026-09-07 — ChatGPT (서류만들기 → 이미지 배열 / PNG 전용 최종 반영)

### 작업 전 기록
- Google Drive의 `0001-image-arrange-feature.patch`, `0002-worklog-update.patch`를 확인하고 당시 최신 `main`과 충돌 여부를 대조함.
- 사용자 최종 확정: 기존 `quotation.html`과 그 안의 `territory-game.html` 숨은 진입점은 제거. 단, `territory-game.html`, `TERRITORY_GAME_PLAN.md`, `TERRITORY_GAME_WORKLOG.md` 파일 자체는 유지.
- 새 `image-arrange.html`은 이미지 여러 장 업로드 → 순서 변경 → 열 개수/간격 조절 → 860px 고정폭 PNG 저장만 제공. ZIP 다운로드와 JSZip 의존성은 최종 요구에 따라 제거.
- 쿠폰/카탈로그/공유/계산기 등 다른 HELPER 페이지와 데이터는 변경하지 않음.

### 작업 후 기록
- `image-arrange.html` 신규 추가: 다중 이미지 업로드, 드래그 및 ◀/▶ 버튼 순서 변경, 개별 삭제, 열 1~8개 조절, 간격 0~60px 조절, 860px 고정폭 캔버스 미리보기, PNG 다운로드 구현. ZIP/JSZip 코드는 제거.
- `index.html`: 기존 `서류 만들기`(`quotation.html`) 카드 1개만 `이미지 배열`(`image-arrange.html`) 카드로 교체. 다른 업무도구 및 판매처 링크는 유지.
- `quotation.html` 삭제. 이에 따라 해당 페이지 내부의 숨은 territory-game 진입점도 함께 제거됨.
- 게임 본체/문서 3종(`territory-game.html`, `TERRITORY_GAME_PLAN.md`, `TERRITORY_GAME_WORKLOG.md`)은 수정/삭제하지 않음.
- 기능 반영 커밋: `60ab4f929c0f5d95a68809669e15448d10aef331`(image-arrange 추가), `3138b1c985b30dcbac734b4a74e043eb43958a0a`(index 카드 교체), `7c4a171be38ea4695e28051d30ef858c6dd0656b`(quotation 삭제).
- 데이터/Firebase/쿠폰 서버 JSON 영향 없음. 기존 쿠폰/카탈로그/공유/계산기 파일 수정 없음.
- WORKLOG 최종 반영 커밋: 본 커밋.
- 남은 확인: 실제 모바일 브라우저에서 이미지 선택 → 배열 → PNG 저장 동작을 실사용 확인하면 됨. ZIP 관련 확인은 기능을 제거했으므로 불필요.

---

## 2026-09-07 — Codex (카탈로그 이미지 반복 버그 후속 검증 / WORKLOG 인계 보완)

### 작업 전 기록
- 사용자 증상: 무신사 상품에 실제 이미지가 최대 5장 있어도 카탈로그에서 `1,2,1,2,1`처럼 앞의 이미지가 반복됨.
- 사용자 목표: 중복 변형 URL이 아니라 서로 다른 원본 이미지를 최대 5장까지 수집하고, 기존 웹 카탈로그 전체를 재검수한 뒤 게시 공유 링크에서도 직접 확인.
- 유지 조건: 기존 상품 정보·정렬·가격·카테고리·디자인은 유지하고 이미지 배열만 중복 제거 및 보강. 온라인 API와 PC 로컬 추출 경로를 함께 수정.
- 추가 운영 논의: 매 업데이트 때 전체 데이터 복사·붙여넣기와 전체 이미지 재검수로 느려지는 문제를 줄일 필요가 있음. 변경 상품만 처리하는 증분 동기화·캐시·선택 재검수 방안은 논의했으나 이번 작업에서는 구현 요청이 확정되지 않아 코드 변경하지 않음.

### 작업 후 기록
- 스타벅스 식품 상세 URL도 상품 추출 대상으로 인식하도록 보완 (`e7002d1`).
- 무신사 이미지 URL의 썸네일·쿼리 변형을 같은 원본으로 판별하여 카탈로그 및 편집 화면의 반복 이미지를 제거 (`1e70441`, `5544997`).
- 무신사 갤러리 수집 중 중복 URL 5개에서 조기 종료하지 않고, 서로 다른 원본 이미지가 최대 5장 모일 때까지 계속 탐색하도록 온라인 API와 PC 로컬 추출기를 함께 수정 (`7734b37`).
- 수정 파일: `api/extract.js`, `local-md-helper.py`, `catalog-editor.html`, `share.html`.
- 합성 갤러리 테스트에서 `1,2,1,2,1` 변형 뒤에 위치한 실제 3~5번 이미지를 포함해 서로 다른 원본 5장 수집 확인.
- 웹 카탈로그 전체 재검수 결과: 검사 214, 정상 214, 매칭불일치 0, 실패 0, 원본링크없음 36. 게시 공유 페이지의 5장 상품 3개를 직접 순환해 각 5장 모두 서로 다른 원본임을 확인.
- PC/모바일 및 데이터 영향: 게시 데이터는 정상 동기화됐고 기존 상품 필드에는 변화 없음. 공개 공유 페이지에서도 정상 표시 확인.
- 기능 커밋 `e7002d1`~`7734b37`은 GitHub `main` 배포 완료. 누락 작업일지 보완 커밋은 `84b8f5d`; 최신 HARD RULE 형식 정리 커밋은 본 커밋.
- 남은 문제: 증분 동기화·변경 상품 전용 이미지 재검수는 미구현이며, 사용자가 구현을 확정하면 별도 작업 전 기록 후 진행.

## 2026-09-06 — ChatGPT (작업지시서 툴 실제 반영 여부 점검)

### 작업 전 기록
- 사용자 증상: HELPER의 `작업지시서` 툴이 앞서 수정한 v2.9 내용으로 실제 교체되지 않은 것처럼 보임.
- 점검 목표: GitHub `main`의 `image-extractor.html` 실제 버전, HELPER 공개 GitHub Pages의 실제 제공 버전, `index.html`에서 연결되는 경로, 캐시/service worker 영향 여부를 대조한다.
- 유지할 기능: v2.9의 쿠폰 추출 효율화, 서버 미반영/1서버/2서버, 서버 반영 시 `이어붙이기/전체교체` 선택, 최소 Drive 보관 정책, 원본 삭제 방지 규칙을 보존한다.
- 수정 원칙: 원인을 확인하기 전 작업지시서 로직을 임의로 다시 작성하지 않는다. 공개 페이지가 구버전이면 캐시/링크/배포 경로를 최소 수정한다.
- 완료 조건: 실제 공개 HELPER에서 v2.9 작업지시서 툴이 열리는 상태를 확인하고, 필요 시 캐시버스터 또는 링크를 수정한 뒤 재확인한다.

### 작업 후 기록
- GitHub `main`의 `image-extractor.html` 자체는 이미 **v2.9**이며 `이어붙이기 / 전체 통으로 교체` UI와 작업지시서 v2.4 로직이 정상 존재하는 것을 READBACK으로 확인.
- `index.html`이 기존에는 `image-extractor.html`을 버전 파라미터 없이 연결하고 있어 모바일/PWA 환경에서 이전 페이지가 보일 가능성을 줄이기 위해 `image-extractor.html?v=2.9-260906`으로 캐시버스터를 추가.
- HELPER 메인 `index.html` 버전을 **v4.8 → v4.9**로 올리고 Updated를 2026-09-06으로 갱신. 기존 대시보드/쿠폰/카탈로그/계산기/기타 링크와 기능은 유지.
- PWA service worker를 `starbucks-helper-pwa-v6 → v7`로 올리고, 메인에서 `sw.js?v=7`을 등록하도록 변경하여 기존 워커/캐시가 최신 페이지 전환을 방해할 가능성을 낮춤.
- 수정 파일/커밋: `index.html` → `0d34fab2a40e5286ed31daa7b59610225f7de7f9`, `sw.js` → `235189274887be27cfcea104f315ad8126553a21`.
- GitHub Pages `pages build and deployment` run 185가 head SHA `235189274887be27cfcea104f315ad8126553a21` 기준 **completed / success**로 완료된 것을 확인.
- `image-extractor.html` 내용 자체는 이번 점검에서 재작성하지 않았고 v2.9 로직을 그대로 보존. 데이터/쿠폰 서버 JSON 영향 없음.
- 모바일에서 HELPER 메인 → 이미지 추출기를 다시 열면 버전 파라미터가 붙은 최신 v2.9 경로로 진입하도록 조치 완료.

---

## 2026-09-06 — ChatGPT (WORKLOG 역할 재정의 / HELPER 전체 공용 일지 강화)

### 작업 전 기록
- 사용자 피드백: 쿠폰용 `작업지시서`와 `WORKLOG`가 섞여 보이면서 처리 맥락이 무거워지고 역할이 혼동됨.
- 사용자 확정: **작업지시서는 쿠폰 추출·크롭 업무에만 사용**하고, `WORKLOG.md`는 **STARBUCKS HELPER 전반의 공용 업무/업데이트 일지**로 사용해야 함.
- ChatGPT / Claude / Codex가 같은 저장소를 이어서 작업하므로, HELPER 관련 업무는 작업 시작 전 최신 WORKLOG를 읽고, 실제 작업 전에 이번 대화의 요구사항과 결정사항을 기록하며, 작업 후 결과까지 다시 갱신해야 함.
- 단순 최종 결과만 남기는 것이 아니라 대화에서 나온 이유, 원복 요청, 금지사항, 실패 원인, 새 운영규칙 등 다음 작업자가 알아야 할 업무 맥락도 기록해야 함.
- 쿠폰 작업지시서 전문이나 원본 데이터 전체를 WORKLOG에 넣는 방식은 금지. 두 문서 역할을 철저히 분리.

### 작업 후 기록
- `AGENTS.md`와 `CLAUDE.md`의 WORKLOG 규칙을 HELPER 전역 기준으로 재작성.
- `PRE_WORK_LOG / CONVERSATION_CAPTURE / POST_WORK_LOG / READBACK / NO_SILENT_WORK` 하드룰 추가.
- `WORKLOG.md` 헤더를 개발이력 전용 표현에서 **HELPER 전체 공용 업데이트·업무 작업일지**로 확장.
- 쿠폰 작업지시서와 WORKLOG의 역할 분리 원칙은 유지하되, WORKLOG 범위를 쿠폰/개발 수정에 한정하지 않고 HELPER 관련 모든 업무로 명확히 확장.
- `AGENTS.md` 커밋: `53f915aa2dceef72cf2d96283e5b5d754620c054`.
- `CLAUDE.md` 커밋: `86ececb337997af44f20383ebecc1577c86bf9b4`.
- `WORKLOG.md` 커밋: 본 커밋.
- 기존 `image-extractor.html` v2.9 및 `COUPON_WORK_INSTRUCTIONS.md` v4.1의 쿠폰 실행 규칙은 이번 작업에서 변경하지 않음.

---

## 2026-09-06 — ChatGPT (이미지 추출기 v2.9 / 쿠폰 처리 효율화 / 서버 반영 방식 복구)

### [작업완료일지]

**요청 / 배경**
- 259건 쿠폰 작업이 지나치게 오래 걸려 정확성은 유지하면서 중복 처리 시간을 줄이는 방식이 필요했음.
- v2.7~v2.8 과정에서 사라진 서버 JSON 반영 방식 선택인 **기존 뒤에 이어붙이기 / 전체 통으로 교체** UI와 작업지시서 규칙을 원복 요청함.
- HELPER의 쿠폰 **작업지시서**와 저장소 **WORKLOG 작업일지**가 혼재되지 않도록 역할을 철저히 분리 요청함.
- 원본 Drive 파일을 잘못 삭제한 사고 재발 방지를 위해 원본/관련 없는 파일 삭제 방지 규칙이 필요했음.

**현재 버전 / 최종 상태**
- `image-extractor.html`: **v2.9 / Updated 2026-09-06**.
- 생성 작업지시서 버전: **v2.4**.
- `COUPON_WORK_INSTRUCTIONS.md`: **v4.1 / Updated 2026-09-06**.

**실제 변경**
1. `image-extractor.html` v2.8 → v2.9
   - 서버 미반영 / 1서버 / 2서버 단일 선택 유지.
   - 서버 1/2 선택 시에만 `기존 뒤에 이어붙이기` / `전체 통으로 교체` 단일 선택 UI가 나타나도록 기존 기능 복구.
   - 이어붙이기: 기존 JSON 보존, 마지막 no 다음부터 신규 데이터 추가, 기존/신규 중복 교차검사, READBACK에서 기존 prefix 불변 확인.
   - 전체교체: 현재 서버를 이번 배치로 완전 교체, no 1부터 재부여, READBACK에서 전체 건수/내용 확인.
   - 서버 미반영이면 Drive에 CSV + verified ZIP, 서버 반영이면 verified ZIP만 저장하는 v2.8 최소 보관 정책 유지.

2. 쿠폰 처리 효율화
   - 원본 ZIP `1회 다운로드 + 1회 압축해제` 후 로컬 원본 재사용.
   - 1차 순회에서 데이터 초벌 추출과 검수 크롭 생성을 동시에 처리.
   - 자리수/중복/빈값/건수/no 연속성은 스크립트 무결성 검사로 한 번에 처리.
   - 전체 OCR을 두 번 처음부터 반복하는 대신, 1차 값과 원본/검수 크롭을 나란히 놓고 **전건 육안대조 1회** 수행.
   - 확대 원본 재오픈은 불일치·미인식·저화질·패턴 이상 등 예외에 집중.
   - 이미 전건 대조가 끝난 뒤 단일 값 오류 수정 시 수정건 + 인접 Pair + 전체 기계적 무결성만 재검사. 순서/Pair/건수 문제가 의심될 때만 전체 육안대조 재수행.
   - 정확성 하드룰 `SOURCE_OF_TRUTH / PAIR_BY_IMAGE / FULL_SOURCE_RECHECK / CHECK_EVERY_DIGIT / NO_GUESS`는 유지.

3. 원본 보호
   - `[NO_SOURCE_DELETE]` 추가.
   - 원본 ZIP, 기존 Drive 파일, 관련 없는 파일 자동 삭제/이름변경/덮어쓰기 금지.
   - 삭제 지시가 있더라도 정확한 파일명과 file ID를 확인한 대상만 처리하도록 규칙화.

4. 작업지시서 / WORKLOG 분리
   - `COUPON_WORK_INSTRUCTIONS.md`는 쿠폰 실작업 실행 규칙 전용 문서로 정리.
   - `WORKLOG.md`는 저장소 개발·수정 이력 전용으로 역할 명시.
   - `AGENTS.md`, `CLAUDE.md`에도 `[INSTRUCTION_ROLE] / [WORKLOG_ROLE] / [NO_MIX]` 하드룰 추가.
   - 기존 259건 WORKLOG 항목의 `작업지시서` 표현은 이후부터 사용하지 않고 배치 결과 요약 형식만 사용하기로 확정.

**수정 파일 / 커밋**
- `image-extractor.html` → `b8874cd264aae43fd4770c1e3ea67dfa9110c767`
- `COUPON_WORK_INSTRUCTIONS.md` → `5af734be85270ed66b01e9560001b3b13a352e02`
- `AGENTS.md` → `0daafcdcb656e888f108c5c1e68649cc8ce0f3c3`
- `CLAUDE.md` → `aebdc37b41faf9e56384919396c827c272f93264`
- `WORKLOG.md` → 본 커밋

**검증**
- `image-extractor.html` READBACK: v2.9 헤더 및 효율화/append-or-replace 복구 표시 확인.
- `COUPON_WORK_INSTRUCTIONS.md` READBACK: v4.1 및 작업지시서/WORKLOG 역할 분리 문구 확인.
- 기존 서버 미반영/1서버/2서버 단일 선택 구조와 Drive 최소 보관 정책 유지.
- 기존 HELPER 다른 페이지 및 쿠폰 서버 JSON은 이번 개발 수정에서 건드리지 않음.

**남은 확인**
- 실제 모바일 화면에서 서버 1/2 선택 시 이어붙이기/전체교체 패널 표시와 선택 상태를 실사용 확인하면 됨.

---

## 2026-09-06 — ChatGPT (쿠폰 259건 서버 미반영 배치)

### [작업완료일지]

**배치 실행 요약**
- 원본: Google Drive `259.zip` / 259장.
- 작업: Coupon No.(17자리) + 등록코드(8자리) + 사용 유효기간 시작일 전수 추출 및 원본 재대조.
- 서버 정책: **미반영**. `coupons1.json` / `coupons2.json` 등 GitHub 서버 JSON 수정 없음.
- 최종 보관: Google Drive에 CSV + 원본대조 크롭 ZIP.

**실제 처리 / 검수**
- 원본 ZIP 내부 이미지 수 259장 확인.
- 259건 전체를 원본 이미지 기준으로 2-pass 재추출/대조하고, 1차·2차 결과가 다른 항목은 더 넓은 원본 영역으로 재검수.
- 최종 충돌/불확실 항목은 원본 확대 확인으로 확정.
- 최종 건수: 259 / 259.
- Coupon No. 259개 모두 17자리, 중복 0, 누락 0.
- 등록코드 259개 모두 8자리, 중복 0, 누락 0.
- 최종 no 1~259 순서 유지.
- 유효기간 시작일 범위: **2026-08-07 ~ 2026-08-12** → 파일명 `260807~260812`.
- CSV 필드는 `no / coupon_number / registration_code`만 저장. 유효기간 필드 없음.
- 원본대조 크롭 259장 생성, `001.jpg` ~ `259.jpg`로 no와 1:1 대응 확인.

**최종 산출물**
- CSV: `259_260906_260807~260812.csv`
- 원본대조 ZIP: `259_260906_260807~260812_verified.zip`
- Google Drive CSV ID: `19D6OwCOUEjrTbe5qa9k3P5L5ooFKwBOP`
- Google Drive verified ZIP ID: `1PslIP1tOBV7mH0J0tVIdj0cmMNySO-rc`
- GitHub 서버 JSON: **미반영 / 수정 없음**.

**종료 전 최종검수**
- CSV 행수 259 확인.
- Coupon No. unique 259 / 등록코드 unique 259 확인.
- ZIP 내부 파일 259개 확인, 첫 파일 `001.jpg`, 마지막 `259.jpg` 확인.
- 서버 미반영 조건 준수.
- Google Drive에 CSV와 verified ZIP 업로드 성공 확인.

**주의 / 남은 정리**
- Drive 업로드 과정에서 verified ZIP이 한 번 중복 업로드되어 두 번째 파일을 중복본으로 구분했음.
- 이후 Drive 정리 과정에서 원본 `259.zip`을 잘못 영구삭제하는 사고가 발생했으며, 이 사고를 계기로 v2.9에 `[NO_SOURCE_DELETE]` 하드룰을 추가함.

---

## 2026-09-06 — ChatGPT (이미지 추출기 v2.5→v2.8 / WORKLOG 절차 강화)

### [작업완료일지]

**요청 / 목적**
- 쿠폰 작업 결과물을 나중에 다시 원본대조할 수 있도록 이미지 추출기 작업 흐름을 단순화하고 보관 규칙을 확정.
- Coupon No. / 등록코드 외에 사용 유효기간 시작일을 원본에서 전수 추출하되, 유효기간은 데이터 필드가 아니라 파일명 범위 계산에만 사용하도록 변경.
- 이후 ChatGPT / Codex / Claude가 이전 작업을 놓치지 않도록 `WORKLOG.md`를 작업 전 읽고 작업 후 반드시 기록하는 절차를 HARD RULE로 강화.

**현재 버전 / 최종 상태**
- `image-extractor.html`: **v2.8 / Updated 2026-09-06**.
- v2.5 커밋: `ba1ce68e04a1bc2dc2e88e58117adc9bdbce2ce5`
- v2.6 커밋: `0d8f0166f7e8145db2c7d0c57644c4c8d887b90f`
- v2.7 커밋: `4c00c9c73089222f66b3224ddc6e6673120ee27b`
- v2.8 커밋: `5230d72672eb02d861ca8dcbe26822f77a1e877e`
- WORKLOG 절차 강화: `AGENTS.md` 커밋 `1c6907d71a6229d805a295cf914b7c0b33701e09`, `CLAUDE.md` 커밋 `41f20ea1456ee4a7deba61821cd7b9be3f1470f3`.

**버전별 변경 내용**
1. **v2.5 — 배치 파일명 / 검수 산출물 도입**
   - 배치명 규칙 `총개수_작업일자_유효기간시작일최소~최대` 도입.
   - 원본대조용 크롭 ZIP과 CSV를 같은 배치명으로 관리하는 규칙 추가.
   - 서버 JSON 파일명 `coupons1.json`, `coupons2.json` 복수형 규칙 명시.

2. **v2.6 — 유효기간 시작일 자동 추출**
   - 사용자가 유효기간 범위를 직접 입력하는 UI를 제거.
   - ChatGPT가 모든 원본 쿠폰에서 사용 유효기간 시작일을 전수 확인하여 최소~최대값을 계산하도록 변경.
   - 날짜 하나라도 불확실하면 추측하지 않고 파일명 확정을 중단하도록 규칙 추가.

3. **v2.7 — 단일 선택 / 자동 산출물 구조**
   - `이미지 크롭` / `쿠폰 데이터 추출`을 동시에 선택하지 못하고 하나만 선택하도록 변경.
   - ZIP / CSV / Drive 저장 체크박스 제거. 쿠폰 데이터 추출 시 필요한 결과물을 자동 결정하도록 변경.
   - 서버 반영도 `미반영 / 1서버 / 2서버` 중 하나만 선택.
   - 데이터 추출 작업에서도 원본대조 크롭을 자동으로 생성하도록 고정.

4. **v2.8 — 최소 보관 정책 확정**
   - 서버 미반영: Google Drive에 **CSV + 원본대조 크롭 ZIP**만 보관.
   - 1/2서버 반영: Google Drive에 **원본대조 크롭 ZIP만** 보관하고 JSON은 GitHub 서버 파일에 직접 반영.
   - 별도 JSON 백업 파일은 Drive에 저장하지 않음.
   - 원본대조 크롭 ZIP은 서버 반영 여부와 관계없이 항상 보관.
   - 유효기간 시작일은 CSV/서버 JSON 필드에 넣지 않고 파일명 계산에만 사용.

**수정 파일**
- `image-extractor.html` — v2.5 → v2.8.
- `AGENTS.md` — WORKLOG HARD RULE 강화.
- `CLAUDE.md` — WORKLOG HARD RULE 강화.
- `WORKLOG.md` — 본 작업완료일지 추가.

**WORKLOG HARD RULE — 앞으로 필수**
1. `[WORKLOG_READ_FIRST]` 코드/설정/페이지 수정 전에 `WORKLOG.md` 최신 항목을 먼저 읽고 현재 버전 / 최근 변경 / 미해결 이슈를 확인한다.
2. `[WORK_ORDER_FIRST]` 현재 요청의 목표 / 대상 파일 / 허용 변경 / 건드리지 않을 것 / 완료조건 / 실제 테스트 기준을 확인한다.
3. `[PRESERVE_EXISTING]` 기존 기능·데이터 구조·디자인 및 다른 작업자의 미완료 변경을 보존한다.
4. `[FINAL_RECHECK]` 수정 후 최초 요청과 WORKLOG 제약을 다시 대조하고 회귀검수한다. 검수 전 완료 선언 금지.
5. `[WORKLOG_WRITE_LAST]` 완료 선언 전에 WORKLOG 최상단에 작업완료일지를 남긴다. 이전→신규 버전 / 수정 파일 / 변경 내용 / 검증 / 영향 / commit SHA / 배포 / 남은 문제를 포함한다.
6. `[WORKLOG_READBACK]` WORKLOG 저장 후 다시 읽어 실제 반영을 확인한다.

**검증 / 영향**
- `image-extractor.html` v2.8 GitHub 저장 후 READBACK으로 버전/헤더 반영 확인.
- 기존 HELPER 메인 구조 및 다른 업무도구 파일은 수정하지 않음.
- 모바일 중심 UI 구조 유지. 선택지를 줄여 잘못된 산출물 조합이 생성될 가능성을 낮춤.

**남은 문제 / 다음 작업자 참고**
- 새 쿠폰 작업을 실행할 때 생성된 v2.3 지시서가 위 v2.8 저장 정책과 일치하는지 실제 결과물 기준으로 계속 검증할 것.
- HELPER 코드 수정은 반드시 위 WORKLOG HARD RULE 절차를 따른다.

---

## 2026-09-04 — ChatGPT (야간 추가 작업 / 최종 인계)

**현재 버전 / 최종 상태**
- 고객용 페이지 `share.html`: **v1.2 / Updated 2026-09-04**.
- 고객 모바일 UI 압축 반영 커밋: `f3c05e7` (`Compact customer catalog on mobile`).
- 모바일 견적서 JPG 다운로드 수정 최종 반영 커밋: `cd041b2` (`Fix mobile quote image download`).
- 위 작업을 위해 사용한 one-shot GitHub Actions workflow는 작업 후 삭제 완료. 최종 정리 커밋: `d6940c3`, `3e54144`.
- 현재 고객 페이지와 에디터의 기존 기능/데이터 구조를 유지하는 것을 HARD RULE로 함.

**이번 추가 작업 핵심**
1. **고객 모바일 페이지 UI 압축 — `share.html` v1.1**
   - PC 화면은 변경하지 않고 `@media (max-width:720px)` 범위만 조정.
   - 대분류/소분류/정렬 필터를 가로 스크롤형으로 압축.
   - 필터 패딩·간격, 제안가 표시, 선택 도구 영역 높이를 축소.
   - 상품 2열은 유지하면서 카드 여백/가격 간격을 줄이고 상품명은 최대 2줄 표시.
   - 하단 선택 바를 얇게 하고 ↑ 버튼이 하단 바와 겹치지 않도록 위치 조정.
   - 목적: 모바일 첫 화면에서 필터가 대부분을 차지하던 문제를 줄이고 첫 상품 행이 더 빨리 보이게 함.
   - 기능/Firestore/상품 선택/정렬/저장 로직은 변경하지 않음.

2. **모바일 `저장하기 → 견적서 JPG 다운로드` 수정 — `share.html` v1.2**
   - 모바일에서 다운로드 페이지/저장 동작까지 갔지만 실제 JPG 다운로드가 막히는 증상 대응.
   - 기존 비동기 Blob 다운로드 흐름 대신 `canvas.toDataURL("image/jpeg", 0.92)` 기반 동기 Data URL 생성으로 변경.
   - `저장하기` 클릭 후 첫 `await` 이전에 `downloadBasketImage()`를 실행하여 브라우저의 사용자 클릭 제스처 안에서 다운로드가 시작되도록 순서 변경.
   - 이후 Firestore 주문 저장은 기존대로 계속 진행.
   - 상품 선택, 수량, 공급가액/부가세/합계, 고객정보, 주문 저장 기능 유지.
   - 코드 기준 구조/반영 검수 완료. 단, 모바일 브라우저별 실제 다운로드 허용 동작은 실기기 재확인 필요.

3. **오늘 중간 카탈로그 보강 작업**
   - 스타벅스 food 상세 추출 지원: `e7002d1`.
   - 반복/중복 카탈로그 이미지 제거 및 과거 중복 이미지 스캔: `1e70441`, `5544997`, `7734b37`.
   - 앞선 카테고리/무신사 다중 이미지/PC 로컬 추출기 수정 내용은 아래 기존 ChatGPT 항목 참고.

**이번 추가 작업에서 직접 바뀐 핵심 파일**
- `share.html` — 모바일 UI 압축 + 모바일 견적서 다운로드 수정, 현재 v1.2.
- 작업용 임시 workflow — 사용 후 삭제 완료, 저장소에 남기지 않음.

**오늘 남은 작업 / 다음 작업자 TODO — 우선순위 순**

### TODO 1 — 카탈로그 에디터 성능 최적화 (최우선)
사용자 체감: 현재 HELPER의 `catalog-editor.html`이 이전보다 무거워진 느낌이 있음.

점검 대상:
- `catalog-sync.js`의 `autosave()` 후 전체 catalog deep clone / stringify / Firestore write 빈도.
- 현재 450ms debounce가 실제 편집 패턴에서 충분한지 확인하고 연속 변경을 더 효율적으로 묶을 수 있는지 검토.
- 데이터가 실제로 바뀌지 않았을 때 Firestore write 생략 가능 여부.
- 자기 기기가 쓴 Firestore snapshot을 다시 받아 전체 catalog 교체 + `render()` 하는 불필요한 왕복 여부.
- 상품 하나 수정에도 전체 가격 규칙 적용 / 전체 DOM render가 반복되는지.
- `remoteImages/images` 등으로 동기화 payload가 과도하게 커지는지.
- snapshot/event listener 중복 등록 여부.
- PC/모바일에서 필요 없는 초기 기능까지 동시에 로딩하는지.

성능 최적화 HARD RULE:
- UI 디자인 변경 금지.
- 기존 기능 삭제/축소 금지.
- 상품 데이터/카테고리/가격 계산/정렬/이미지/URL 상품등록/무신사·스타벅스 추출/모바일-PC 동기화/고객 공유/저장 기능 전부 유지.
- Firestore 데이터 구조 임의 변경 금지.
- 데이터 손실 금지.
- 추측으로 뜯어고치지 말고 **실제 병목 확인 → 최소 수정 → 회귀검사** 순서로 진행.

### TODO 2 — 기존 상품 카테고리 소급 보정 확인
- 기존 콜드컵/워터보틀/글라스 등 과거 등록 상품이 신규 자동분류 규칙과 다르게 남아있는지 확인.
- 현재 데이터가 이미 정상이라면 불필요한 전체 rewrite를 하지 말 것.

### TODO 3 — JS 캐시버스터 운영 정리
- `catalog-editor.html`에서 `catalog-online-import.js`, `catalog-sync.js` 수정 시 `?v=...`를 함께 올려 모바일/PC에 구버전 JS가 남는 문제 방지.
- service worker 캐시와 중복/충돌 여부도 같이 확인.

### TODO 4 — 실사용 회귀 테스트
- 모바일 고객 페이지: 필터/정렬/선택/수량/바구니/↑ 버튼/저장하기/JPG 실제 다운로드.
- Firestore 주문 저장과 JPG 다운로드가 둘 다 성공하는지 실기기 확인.
- PC 에디터: 로컬 helper 기반 무신사 추출, 스타벅스 추출, 다중 이미지, 이미지 재검수, 자동분류, 가격정책, 게시하기.
- 모바일 에디터: Vercel 온라인 추출 및 PC와 동기화.

**다음 작업자 필수 절차**
1. 이 `WORKLOG.md` 최신 항목을 먼저 읽는다.
2. 작업 시작 전에 현재 사용자 요청의 목표 / 대상 파일 / 허용 변경 / 건드리지 않을 것 / 완료조건 / 실제 테스트 기준을 확인한다.
3. 작업 후 최초 요청을 다시 읽고 종료 전 최종검수를 수행한다.
4. 오류/미완료가 있으면 수정 후 다시 검수한다.
5. 마지막에 작업완료일지를 이 WORKLOG 맨 위에 추가한다: 변경 파일 / 실제 변경 / 테스트와 결과 / PC 영향 / 모바일 영향 / commit SHA / 배포상태 / 남은 문제.
6. 검수 전에는 완료 선언하지 않는다.

---

## 2026-09-04 — ChatGPT

**이번 작업 핵심**
- 카탈로그 정렬을 `카테고리 우선순위 → 선택한 정렬 기준`의 2단계 구조로 변경함. 우선순위는 `텀블러/보온병 → 머그 → 액세서리 → 티바나(차) → 시럽 → 우산 → 비아 → 원두 → 리유저블 → 기타` 순서 (`553847f`).
- 카테고리 별칭 정규화 추가: `악세사리/악세서리 → 액세서리`, `티바나 → 티바나(차)`, `텀블러 → 텀블러/보온병`.
- 무신사 다중 이미지 누락 원인 확인: 일부 상품은 대표 이미지 외 추가 이미지가 `prd_img` 경로에 있어 기존 `thumbnailImageUrl + goodsImages` 방식만으로는 1장만 추출됨.
- Vercel 온라인 추출기 `api/extract.js`를 v1.1로 수정하여 동일 상품번호의 `goods_img/prd_img` 이미지를 보조 수집하고 최대 5장까지 반환하도록 변경 (`35b53f9` 포함, 최종 반영 범위 `0874303`).
- `catalog-sync.js`에 **이미지 재검수** 기능 추가: 기존 상품의 `sourceUrl`을 다시 조회하고 상품번호를 우선 매칭한 뒤, 가격·카테고리·태그·수량 등 기존 데이터는 유지하면서 부족한 원본 이미지만 최대 5장까지 보강. 처리 결과를 `검사 / 보강 / 정상 / 매칭불일치 / 실패 / 원본링크없음`으로 표시 (`0874303`).
- 신규 등록 상품 자동분류 규칙 강화: `콜드컵/콜드 컵/워터보틀/워터 보틀/텀블러/보온병 → 텀블러/보온병`, `글라스/머그 → 머그` (`8091c6a`).
- PC에서 무신사 가져오기는 Vercel API가 아니라 로컬 `local-md-helper.py`를 사용하므로, PC용 추출기도 v1.2로 맞춰 동일 상품번호의 `prd_img`를 최대 5장까지 추출하도록 수정 (`cb6c957`).
- 실제 테스트 대상 무신사 상품 `5492977`은 페이지상 대표 포함 총 4장의 갤러리 이미지가 확인됐고, 로컬 추출기 재시작 후 이미지 재검수로 기존 1장짜리 상품을 보강하는 흐름을 사용함.

**바뀐 파일**
- `catalog-sync.js`
- `api/extract.js`
- `catalog-online-import.js`
- `local-md-helper.py`

**주의 / 미해결**
- 기존 등록 상품 전체의 카테고리를 상품명 기준으로 소급 재분류하는 로직은 `catalog-sync.js`의 `normalizeCatalogCategories()`에 아직 최종 반영되지 않음. 현재 `8091c6a`는 신규 온라인 등록 쪽 자동분류 강화가 중심이므로, 기존 콜드컵/워터보틀/글라스 데이터가 잘못 분류돼 있으면 별도 소급 보정이 필요함.
- `catalog-editor.html`의 외부 JS 쿼리 버전(`?v=...`)을 수정 때마다 올리는 캐시버스터 운영은 아직 미적용. 수정 후 브라우저에서 이전 JS가 남는 현상이 있었으므로 강력 새로고침 필요.

**다음 작업자 참고**
- 작업 시작 시 `WORKLOG.md`를 먼저 읽고 위 미해결 2개(기존 상품 소급 카테고리 재분류, JS 캐시버스터)를 우선 확인할 것.
- 이미지 관련 수정 시 온라인 API와 PC 로컬 추출기 두 경로를 항상 같이 점검할 것.

## 2026-09-04 — Codex

**후속 수정**
- 에디터 첫 화면에서도 최상단 버튼이 바로 보이도록 스크롤 표시 조건을 제거함. 고객 페이지도 동일하게 항상 표시 (`a16a7e8`).

**최종 VER**: `VER-2026.09.04-a16a7e8`
**기능 완료 커밋**: `a16a7e8` — 최상단 이동 버튼 항상 표시 최종 조정

**오늘 반영한 내역**
- 카탈로그 편집 기능 확장: 대·소분류 관리, 정렬, 가격 정책, 웹판매가, 10원 단위 반올림 (`223d568`~`31ccd32`)
- 고객용 상품 안내 및 Firestore 게시·주문 저장, 견적서 이미지 저장, 고객 화면 정렬·선택·드래그 기능 (`368e058`~`1b23273`)
- 모바일·PC 간 카탈로그 동기화와 기존 데이터 보호 (`8526c1a`~`d442e0e`)
- Vercel 온라인 상품 추출 API 및 모바일 무신사·OneLink·스타벅스 링크 가져오기 (`c8dfba5`~`3d70c7b`)
- 쿠폰 문구 제거, 상품명 줄바꿈, 카테고리 자동 분류와 가격 정책 보정 (`3d70c7b`, `304a8bf`)
- 고객 링크 미리보기 문구를 `디스이스마인 제공`으로 변경 (`db46136`)
- 고객 상품 이미지 회전 보기와 중복 상품 경고 추가 (`c3d3cf4`)
- 에디터·고객 페이지에 작은 고정 ↑ 버튼 추가. 300px 아래에서 표시되며 누르면 부드럽게 최상단으로 이동 (`e60441c`)
- 에디터에서 상품 가져오기 성공 시 주소 입력칸 자동 초기화. 모바일 온라인 가져오기의 기존 초기화 동작도 유지 (`e60441c`)
- Claude와 Codex가 공통으로 읽고 갱신할 `WORKLOG.md`, `CLAUDE.md`, `AGENTS.md` 추가 (`e60441c`)

**검증 및 배포 상태**
- `catalog-editor.html`, `share.html` 인라인 스크립트 문법 검사 통과
- 변경 공백 검사 통과
- GitHub `main` 반영 예정

**다음 작업자 참고**
- 작업 시작 전에 이 파일의 최신 항목과 현재 Git 상태를 확인할 것.
- 새 기능의 최종판이 나오면 `VER-날짜-기능완료커밋` 형식으로 커밋 번호를 함께 기록할 것.
- `catalog-preview.html` 및 `.bak`, `__pycache__/`는 기존 미완료/비관리 파일이므로 이번 커밋에서 제외함.

## 2026-09-04 — Claude

**바뀐 파일**: `catalog-editor.html`, `share.html`, `firestore.rules`(이전 세션에서 반영 완료)

**이번에 한 작업**
- `cleanProductName()` 정규식이 "에코텀블러음료쿠폰"만 매칭해서 "+텀블러음료쿠폰(카톡발송)"처럼 "에코" 없는 변형은 안 지워지던 버그 수정 (에코를 옵션으로 변경). 단, 이미 등록된 기존 상품명은 소급 반영 안 됨 — 이름 필드 재저장 필요.
- `share.html`(고객용 페이지)에 "제안가 표기" 체크박스를 필터 줄 오른쪽 끝에 추가 (기존에 바구니 안에 있던 "제안가 포함하기"를 이걸로 통합·이동).
- "저장하기" 클릭 시 실제 파이어스토어 주문 기록 저장은 유지하면서, 추가로 **견적서 이미지(JPG) 자동 다운로드** 기능 구현. 캔버스에 텍스트만 그려서 만들기 때문에(상품 사진 없음) 무신사 이미지 CORS 문제와 완전히 무관함. 포함 내용: 선택 상품명(2줄 자동 줄바꿈)·수량·라인별 금액, 공급가액/부가세/합계, 상호명·이메일·연락처.
- `share.html`의 상품 카드 레이아웃을 에디터(`catalog-editor.html`)와 동일하게 맞춤: 카드 상단에 드래그 손잡이(☷) + 선택 체크박스 + 번호. 대분류 필터 아래에 소분류 필터, 정렬(가격높은순/최근추가순/최근수정순), 전체선택/전체선택해제 버튼 추가. 드래그앤드랍 순서변경 추가 — 고객이 바꾼 순서는 `customOrder`(id 배열)로 로컬에 기억해서, 파이어스토어 실시간 동기화로 새 데이터가 들어와도 순서가 안 깨지게 처리함.
- `catalog-editor.html`: "고객 링크 복사" 버튼 → "게시하기"로 이름 변경. `publishShare()` 페이로드에 상품별 `updatedAt` 추가 (share.html의 "최근수정순" 정렬에 필요).

**다음에 참고할 것**
- 이 클라우드 세션은 `gstatic.com`/`firestore.googleapis.com` 접속이 막혀 있어서 실제 파이어스토어 저장/실시간동기화/이미지다운로드 흐름은 직접 테스트 못 함. 코드 리뷰 + Playwright로 캔버스 렌더링·정렬·드래그 로직만 검증했음. **실사용 테스트는 항상 사람이 직접 확인 필요.**
- `catalog-editor.html.bak`, `local-md-helper.py.bak`, `__pycache__/`가 저장소 폴더에 남아있음 — 클라우드 세션 쪽 도구로는 삭제 권한이 없어서 그대로 둠. 필요하면 직접 삭제.
- 미해결/보류 논의 없음 — 이번 라운드 요청은 다 반영 완료.
