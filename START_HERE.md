# STARBUCKS HELPER — START HERE

이 파일은 HELPER 작업자가 **작업 전에 가장 먼저 읽는 현재 운영 기준**이다.
상세 변경 이력은 `WORKLOG.md`에 남기되, 여기 내용과 충돌하면 **현재 사용자 확정사항인 이 파일을 우선 확인**하고 필요하면 WORKLOG 최신 항목과 실제 코드를 교차검사한다.

## 1. 작업 시작 순서 — HARD RULE
1. `START_HERE.md` 전체를 읽는다.
2. `WORKLOG.md` 최신 항목을 읽는다.
3. 실제 대상 파일의 현재 main 코드를 확인한다.
4. 수정 전 WORKLOG에 작업 전 기록을 남긴다.
5. 최소 수정 후 회귀검수한다.
6. WORKLOG 작업 후 기록 + commit SHA + 남은 문제를 남기고 READBACK한다.

과거 WORKLOG 판단이 현재 코드와 다를 수 있으므로 **WORKLOG 문장만 믿고 수정하지 않는다. 실제 main 코드가 최종 확인 대상**이다.

## 2. HELPER의 실제 업무 흐름
STARBUCKS HELPER는 단순 쿠폰 페이지가 아니라 디스이스마인 운영 업무 도구다.

사입/원가·마진 계산
→ 상품 원본 링크·이미지 수집
→ `catalog-editor.html` 상품 등록/분류/가격/이미지 관리
→ `catalog-sync.js` 기기간 동기화
→ 고객용 `share.html` 게시/선택/수량/견적·주문
→ `quote.html` 견적서·거래명세서
→ 쿠폰 추출·검수·서버 운영
→ 상품 이미지/각인 시안
→ 프롬프트 저장소/TO DO

기존 기능을 삭제하거나 다른 흐름으로 대체하지 말고 이 연결을 유지한다.

## 3. 반복 금지 실수

### 쿠폰
- SOURCE OF TRUTH는 원본 쿠폰 이미지다. 기존 CSV/JSON/OCR 결과를 정답으로 간주하지 않는다.
- 이미지 1장 = 데이터 1건. Coupon No.와 등록코드는 같은 원본 이미지에서 확인한다.
- OCR은 초벌일 뿐 최종 확정이 아니다.
- 서버 반영 전 전건 원본 재대조, 자리수, 중복, 건수, 순서를 확인한다.
- 원본 파일 삭제 금지.
- **쿠폰1/2 계열:** Coupon No. 17자리.
- **쿠폰3 서버:** 별도 양식이며 Coupon No. 16자리가 정상. 17자리로 보정/추정하지 않는다.

### 상품/카탈로그
- **QR 고정 공개 카탈로그 URL:** `https://runipokr-dotcom.github.io/STARBUCKS-HELPER/share.html?id=c6xxh9`
- 위 URL은 이미 QR로 사용 중인 운영 주소다. 사용자가 명시적으로 변경을 지시하지 않는 한 `shareId=c6xxh9`, `share.html` 경로, GitHub Pages 프로젝트 경로를 변경하거나 새 주소로 교체하지 않는다.
- 카탈로그 코드 최적화/리팩터링/게시 기능 수정 후에도 기존 QR 주소가 그대로 열리는지 확인한다.
- 상품 데이터·이미지·카테고리·정렬·가격 계산·PC/모바일 동기화·고객 공유 기능을 임의 삭제/축소하지 않는다.
- 카탈로그 성능 문제를 고칠 때 전체 데이터 구조를 갈아엎지 않는다. 실제 병목 측정 → 최소 수정 → 회귀검수 순서.
- 상품 하나 수정 때문에 불필요하게 전체 DOM render / 전체 cloud write가 반복되지 않는지 항상 확인한다.
- JS 수정 시 cachebuster도 함께 확인한다.

### 이미지/각인
- 상품 원본 이미지의 제품 픽셀, 로고, 형태, 비율을 임의 생성/변형하지 않는다.
- 생성형 이미지 작업과 원본 위 시안 오버레이 작업을 구분한다.
- 기본 상품 이미지 작업: 1000×1000, 순백 배경, 중앙 배치, 균일 여백, 수평/원근 정리. 원본 디테일 유지.
- 각인 시안은 화면 감으로 mm를 추정하지 않는다. 제품 실측 기준으로 px↔mm를 잡는다.
- SS 블랙 매트 데비 사례처럼 원본 제품을 잠그고 문구 레이어만 이동/크기 조정한다.
- 과거 대표 실수: 다른 제품 이미지로 바뀜, 25mm/5mm 실측 비율 왜곡, 위치 수정 중 원본 흐려짐/재생성.

### 견적/거래명세
- 사용자가 제공한 원본 XLSX 서식·회사정보·계좌정보·병합/인쇄 구조가 기준이다.
- 임의 양식으로 대체하지 않는다.
- 저장하기 동작과 자동 다운로드 여부는 WORKLOG 최신 확정사항을 확인한다.

## 4. 내부 접근 잠금
- 고객용 공개 QR 카탈로그 `share.html?id=c6xxh9`는 비밀번호 없이 공개 유지.
- 그 외 HELPER HTML 페이지는 `auth-gate.js`를 통해 내부 접근 비밀번호를 요구한다.
- 비밀번호 평문은 저장소에 넣지 않고 해시만 저장한다.
- 현재 방식은 GitHub Pages 정적 게이트이므로 일반 접근 차단/오접근 방지용이다. 저장소 소스 분석까지 막는 강한 인증이 필요하면 Vercel/Firebase 기반 서버 인증으로 별도 이전한다.
- `share.html`에 auth-gate를 삽입하거나 QR 주소를 변경하지 않는다.

## 5. 현재 중요 이슈
1. Firestore Security Rules가 내부 작업 데이터와 공개 share를 충분히 분리하지 못하고 있음. 인증/공개범위 설계 후 별도 개편 필요.
2. 카탈로그 편집이 데이터 증가와 함께 느려짐. 전체 stringify/write/render 최소화가 우선.
3. 공개 share의 주문 개인정보와 내부 견적 원본 파일 접근범위를 분리해야 함.
4. 각인 실측 시안을 위한 전용 도구가 필요함: 원본 잠금 + px/mm 기준점 + 전체/확대 출력.

## 6. 수정 원칙
- 요청 범위 밖 리팩터링 금지.
- 데이터 손실 금지.
- 다른 작업자의 미완료 변경 덮어쓰기 금지.
- UI 디자인은 사용자가 요청하지 않으면 유지.
- GitHub Pages/Vercel 현재 배포 구조를 깨지 않는다.
- 수정 후 PC/모바일 영향과 데이터 영향을 분리해서 기록한다.

## 7. 파일 역할
- `START_HERE.md`: 현재 운영 기준 / 반복 금지 실수 / 살아있는 핵심 상태.
- `WORKLOG.md`: 상세 작업 이력과 판단 근거.
- `COUPON_WORK_INSTRUCTIONS.md`: 쿠폰 실작업 지시서.
- `AGENTS.md`, `CLAUDE.md`: 작업자가 위 파일을 반드시 읽도록 강제하는 규칙.

이 파일은 확정 규칙이 바뀔 때만 갱신한다. 일회성 작업 내역은 WORKLOG에만 기록한다.
